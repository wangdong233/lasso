# Node
node_modules/
dist/
*.tsbuildinfo

# Env / secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Lasso runtime cache（页面/界面状态写磁盘，08 §0 原则 2）
.cache/
*.log

# v0.2 benchmark reports（A/B 实测产物，每次本地重跑覆写）
reports/

# IDE
.vscode/
.idea/
*.swp

# OS
Thumbs.db

# agent/devteam tooling state (not project source)
.devteam/
output/
.private/

# ProfileRegistry 运行态产物（P2 处置轮：dev 跑测残留在仓根曾被误 track 进 npm 包；
# 生产路径在 ~/.cache/lasso/profiles/，repo 根出现即异常残留）
profiles/
