# ft-00 终局汇总（doc/17 功能测试全流程执行，2026-08-18）

> **汇总员**：终局汇总官（ft-00）。**输入**：`ft-round1-{search,browse,infra,perf}.md` 四面板执行记录 + `ft-round1-verdict.md` R1 独立裁决记录 + doc/17 回填后正文 + 工作树源码锚点复核（ssrf-guard.ts:86 剥括号 / config.ts:330 mergedEnv / provider-registry.ts:183 null-safe / HighRiskGate parseEvalResult ×3 / TabSession listPageTargets 仅剩裁决注释——六处修复 + R1 追加修复全部在树）。**收敛状态**：**第 1 轮即 ALL-CLEAN 收敛**（R1 裁决 DECISION: ALL-CLEAN；无 round2 执行需求，watch 项按 03 §3.3 移交）。

---

## 0. 执行总览

| 项 | 值 |
|---|---|
| 版本 | lasso-mcp v1.17.0（commit 8112a5e 基线 + 本轮工作树修复，未 bump） |
| 轮次 | round1（四面板并行执行 + R1 独立裁决 + 终局汇总）——**1 轮收敛** |
| 面板 | ①搜索域（T-SEARCH/T-QUALITY/T-CB/T-LOCAL/T-FEED/T-SERPHTTP/key面）②浏览与交互域（T-BROWSE/T-REFS/T-LIFE/stealth）③基建与治理域（T-CLI/T-CONFIG/T-READ/T-OBS/T-RT/T-FOREST/T-SSRF/T-FALL）④性能与架构判定域（L-COST 14 + §6 38 条 + T-ELICIT） |
| 门禁终态 | `npm run build` 0 error；`npm test` **134 files / 2239 passed + 1 skipped（2240）**；`npm run check-invariants` **81/81**；`npm run inv-selftest` **20/20**（两轮独立复跑，/tmp/ft-verdict-gate{,2}.log 在档） |
| 基线变化 | 2227 → **2240（+13，只增不减 ✅）**：browse +4 / infra +8 / perf +1 |
| 产品缺陷 | **6 处修复闭环**（判用例→修产品→回归钉→真机复测→门禁）+ R1 追加 R-DEP-03 别名清除（行为零变化纯重构） |
| fail 终态 | **0** |

---

## 1. 各轮概况

### round1（2026-08-18 19:05–20:14，单轮收敛）

**四面板并行执行**（同一工作树；执行纪律 §0.2 全沿用：失败先判用例、串行 + ≥2s 间隔、资源三采样、面板收尾清残留、每条附关键输出）：

1. **搜索域（ft-round1-search.md）**：83 条 + 门禁基线核验。machine_mcp 命中为 API 主力源；zhipu/bing 退役语义（INV-80/54 墓碑）真机验证；fallback 链五形态分段全见（machine 直命中 / brave 剔除 / serp→browse / 全熔→replay / 熔断跳过）；缓存 TTL/freshness 门/content_blocks 四态/隐私红线（T-LOCAL 12/12 含 INV-81 三禁 grep 零命中）全过。**零 fail**；3 blocked（环境/上游，均有裸 curl 第三方取证）+ 4 ⏸（Brave key）。02 搜索域锚点 10/10 ✅。
2. **浏览与交互域（ft-round1-browse.md）**：42 条（面板口径）。T-BROWSE 25 + T-REFS 7 + T-LIFE 11 + L-COST 3 回填。extract 三档 byte-identical、refs 附录 50 cap 钉尾 + ref→click/fill 往返全链、stealth 16 路真机（webdriver=undefined）、hidden 启动零夺焦点、idle reaper 活动/闲置双臂、资源三采样 after(+90s)=0。**抓出 2 产品缺陷**（W-DEF-R11-1 doWait 假成功 🔴 / W-DEF-R11-2 tab_restore 恒 no-op 🟡）修复闭环；3 处初判 fail 判用例 bug 重测通过；清单勘误 F-R11-1/2/3。
3. **基建与治理域（ft-round1-infra.md）**：37 条。CLI/doctor/config/退役键/read_text 续页（@o1 字节精确相等）/admin 治理组（capability 启停 + tools/list_changed / caller cap 60s 滑窗 / SIGHUP 热更新）/RootRegistry 共享计数器/SSRF 拦截矩阵 + deny 扩展。**抓出 3 产品缺陷**（FT-DEF-1 doctor 不感知 file 键 🟡 / FT-DEF-2 SIGHUP TypeError 🟡 / **FT-DEF-3 IPv6 字面量绕过 SSRF 🔴 安全级**）修复闭环；T-FALL-05 waived（本机不可达不强造击穿）。
4. **性能与架构判定域（ft-round1-perf.md）**：L-COST 14 行 3 次取中位回填 + §6 38 条逐行判定 + T-ELICIT 单测 68/68 + 场景 0 降级红线真机。**抓出 1 产品缺陷**（**FT-DEF-1′ HighRiskGate 裸 JSON.parse 围栏响应 → gate_error 保守放行——C1 安全红线端到端失效 🔴**）TDD 修复 + INV-76(m) 防复发 + 真机双中复验；测量异常 5 例全部按「先判用例」处置（缓存污染三段法修正 / extract 姿势修正 / 9222 会话前占用保守不动 / 180s stdio 停摆隔离不复现判环境）。
5. **R1 独立裁决（ft-round1-verdict.md）**：**DECISION: ALL-CLEAN**。门禁两轮独立串行复跑全绿；六处修复逐一新鲜复审（03 §3.4「作者自批永不充分」签字——源码锚点逐读 + 回归钉逐验）；7 处初判 fail 判用例侧核验处置正确；R1 追加修复 R-DEP-03 别名清除（listPageTargets 删除 + listPages public 化，全仓 AST 0 命中）；02 终判 ✅27/⚠️11/❌0/⏸1；观察项逐条裁决（接受/watch 分类）；round2 复核清单移交。

**为何单轮收敛**：四面板发现的全部 6 处产品缺陷均在本轮内完成闭环（修复 + 单测钉 + 真机复测 + 门禁），R1 复审全部维持、零返工；剩余非 pass 项全部为环境/上游/人工/key 四类外部依赖（按 03 §3.3 处置），无产品侧待办阻断收敛。

---

## 2. 测试覆盖统计（用例数 × verdict）

### 2.1 分面板（各面板申报口径）

| 面板 | 执行 | pass | 修复后 pass | blocked | waived | ⏸ key缺失 | 备注 |
|---|---|---|---|---|---|---|---|
| 搜索域 | 83 | 74 | — | 3（env/上游） | 2（退役） | 4（Brave） | T-SEARCH 35 / T-QUALITY 5 / T-CB 10 / T-LOCAL 12 / T-FEED 8 / T-SERPHTTP 10 / T-CLI-08 3 子项；fail=0 |
| 浏览交互域 | 42 | 40 | 2（W-DEF-R11-1/2） | 0 | 0 | 0 | 另 3 处初判 fail 判用例 bug 重测通过；T-LIFE-02（visible 档）留专门窗口未计 |
| 基建治理域 | 37 | 36 | 3 缺陷含在 pass（FT-DEF-1/2/3） | 0 | 1（T-FALL-05） | 0 | T-CLI 7 / T-CONFIG 6 / T-READ 5 / 治理 9 / T-FOREST 2 / T-SSRF+T-FALL 8 |
| 性能架构域 | 22 | 18 | FT-DEF-1′ 含在 pass | 3（L02 env + T-ELICIT-07/08 人工） | 0 | 0 | L-COST 14 + T-ELICIT 8（6 单测 + 场景0 + 07/08） |

### 2.2 终态 verdict 矩阵（R1 裁决核可；L-COST 行跨面板合流后按 §5 终表计一次）

| verdict | 数量 | 明细 |
|---|---|---|
| **pass**（含 6 缺陷修复后复测 pass） | **≈170** | 功能面 159 + L-COST 11；单测面另有定向 12 spec 252 测 + serp 29 测 + 全量 2240 门禁 |
| pass-with-note（性能判读注） | 2 | L-COST-04（超起步 79-200% 判环境）/ L-COST-06（warp 场景差异判场景） |
| blocked（环境/上游） | 4 | T-SEARCH-14（ddg/brave 挡出口）/ T-SEARCH-15（archive.org 429）/ T-SERPHTTP-01（同因，与 L-COST-02 合并计数）/ L-COST-02（brave 429 命中不可复现）；L-COST-12 perf 侧 9222 被占已由浏览域补真值（2592/3181ms）转 pass |
| blocked（人工） | 2 | T-ELICIT-07/08（需 CC ≥2.1.76 客户端人工点选；场景 0 降级红线已真机 pass） |
| waived | 3 | T-SEARCH-02（zhipu 退役）/ T-SEARCH-06（bing 退役）/ T-FALL-05（本机不可达不强造击穿） |
| ⏸ key缺失 | 4 | T-SEARCH-05/08/09/23（Brave；假 key 422 计划级探测 T-CLI-08③ 已真机 pass 归 key 无效桶） |
| **fail（终态）** | **0** | 6 处判「用例对→产品缺陷」全部本轮内修复闭环；7 处初判 fail 判用例侧（F01/H02/A06 用例 bug、L05/L06 缓存污染、L13 姿势、T-QUALITY-02 清单错、T-FEED-08 口径错、门禁并行 flake） |

**覆盖面**：doc/17 §0.4 工具矩阵 17 工具全覆盖（交叉审「无有实现无用例」结论经真机复核成立）；CLI 子命令面 7 项全覆盖；U-11..U-17 旅程步在面板内同步覆盖；§5 L-COST 14/14 有终值；§6 38 条 39 行全判定。

---

## 3. §5 性能基线终表（合流后权威版 = doc/17 §5）

> 口径：ClashX TUN 网络；MCP stdio 真实 dist/index.js；每场景独立进程；串行 + 2s 间隔；浏览器场景资源三采样。

| ID | 场景 | 起步实测 | 本轮终值（多面板合流） | 状态 |
|---|---|---|---|---|
| L-COST-01 | 第一跳 machine_mcp | 1824ms/10 条；区间 1.4-4.1s | 搜索域 1509/1415ms；性能域 2439ms 中位［1981,2439,9747］（run3 瞬断级联 browse 9.7s 含在内） | pass（区间内） |
| L-COST-02 | 第一跳 serp_http EN（brave 级联） | 1908ms/20 条 | 命中不可复现（env）：三连 brave 429 + 裸 curl 429/ddg 202；serp_http 跳本身 1346-1418ms 快速失败；链降级正确 | **blocked(env)** 待网络恢复 |
| L-COST-03 | 第一跳 serp_http CN（baidu） | 1134-2283ms/17 条 | 932ms/18 条（搜索域）；795ms/20 条中位（性能域） | pass（优于起步） |
| L-COST-04 | 第一跳 browse_headless 兜底 | 5304-5856ms/0 条 | 7671/8012/10549/15952/17542ms 均 0 条（验证码/降级壳页）；资源 14→24→23 procs / 1127→1757→1665MB，关闭后 chrome 树=0 | pass-with-note（判环境：TUN+npx 冷启+429 双跳；形态同象） |
| L-COST-05 | 第二跳增量 EN cb=3 | p50≈+1.4s（+0.7~+2.6s） | +3676ms（搜索域，1 fail 快败+2 ok）/ +2661ms 中位（性能域 3×ok 6836 字符） | pass（中位超上沿 4% 判网络抖动；搜索域样本超 44%<50% 判读线——偏慢持续观察） |
| L-COST-06 | 第二跳增量 EN cb=5 | ~+900ms（warp 场景） | +4513ms（搜索域 2 fail+3 okT ~6k）/ +2368ms 中位（5 条仅比 3 条多 47% 时长——并发 3 摊薄可见） | pass-with-note（判场景：单位产出对齐，无产品回归证据） |
| L-COST-07 | 第二跳增量 CN cb=3 | ~+3-5s | +463ms 全失败快败 / +3900ms 混合 / +3530ms 中位（link?url= 壳→extract_failed 诚实标注） | pass（与起步一致） |
| L-COST-08 | fetch_feed GitHub atom | 2025ms/5 条 | 1878ms/10 条；1580ms 中位（run1 180s stdio 停摆判环境，隔离复测 1644ms 不可复现，server 30s 硬超时在位） | pass |
| L-COST-09 | fetch_feed 73KB 抢救 | 758ms + truncated_input | 364ms（408 期头字段抢救）/ 70ms/1 条 | pass（优于起步） |
| L-COST-10 | search_local history | 64ms/5 条 | 131/24/20ms；26ms 中位 | pass |
| L-COST-11 | search_local files（mdfind） | 571ms/5 条 | 712/650ms；605ms 中位 | pass |
| L-COST-12 | launch-chrome hidden 冷启动 | 本轮建立 | **2592ms / 3181ms**（浏览域两次独立冷启到探活 200；性能域 9222 被占 blocked，9223 空闲口旁证 ≤2s UP） | pass（基线建立） |
| L-COST-13 | read_text 续页（spill 翻页） | 本轮建立 | 4/2/4ms（基建域 190KB）；14ms 中位（性能域 1.2MB） | pass（基线建立：本地切片近零开销） |
| L-COST-14 | include_refs 抽取开销 | 本轮建立（预期≈0） | [+8,−18,+191]ms 中位≈0（浏览域 A/B×3）/ −242ms（性能域，顺序效应盖过增量） | pass（≈0 证实「仅 evaluate 顺带无第二跳」） |

**判读纪律四条核验**：① 第一跳 ≫ 第二跳未反转（本轮比值收窄至 ~1:1 属当日整体网络变慢，CN 侧仍低于完整链）；② serp_http vs browse 的 2.8× 优势 CN 侧 **13× 保持**（795ms vs 10.5s），EN 侧因 429 无法验证（待网络）；③ 快速失败不拖尾全过（SSRF 拒 2-27ms / serp 429 1.3-1.4s / 第二跳失败条即回；唯一 180s 样本判客户端 stdio 停摆、server 30s 硬超时在位）；④ 资源释放全 ✅（各面板终态 lasso 特征树 0 进程 / 台账空）。

---

## 4. §6 简单架构 38 条终判汇总（对照 02_简单架构清单）

**分布（39 行，R-FF-04≡R-DEP-03 共享结论）：✅27 / ⚠️11 / ❌0 / ⏸1（R-FF-05 未接入声明）。**

| 刻度 | 结果 |
|---|---|
| 🔴 硬不变量（3/3 ✅） | R-FF-01 分层方向（import 图实跑 137 模块/467 边：反向 0、runtime→channels internal 0、INV-35 门禁）；R-FF-02 循环依赖（值级 0 环；3 个 type-only 环留档待 dependency-cruiser 豁免）；**R-DEP-03≡R-FF-04 穿堂式=0（R1 裁决修复：TabSession.listPageTargets 别名删除 + listPages public 化后 AST 扫 0 命中）** |
| ⚠️ 11 项处置 | **本轮修 1**：R-DEP-03/R-FF-04。**记 watch 6**：R-INT-06（browseSingle ≥4 职责，nav-verify 拆分候选）/ R-DEP-04（schema↔interface 镜像 >60% + W1-DEF-8 前科，奇偶 INV 候选）/ R-CHG-03（mock≠真契约 4 例实证 V-1/V-2/F-2/FT-DEF-1′，INV-76(m)+upstream-mock 铁律已补）/ R-CI-07（冲突清单进 PR checkbox）/ R-CI-08+review#1（两套 tmp 根，统一 SPILL_ROOT，win 兼容隐患）/ R-INT-08 附注（新增 evaluate_script 消费点必经适配器进 PR checklist）。**接受理由 4**：R-INT-02（修改点 4 有 INV-81(f)/INV-76(k) 机械守护）/ R-INT-03≡R-DEP-01（3 席=BaseChannel 生命周期契约+注入位，02 §B-2 satisficing）/ R-INT-05≡R-CHG-01（版本合批放大属观测口径，按 feature 拆 commit 即生效）/ R-FF-03（未度量显式声明） |
| 分歧裁决 | R-INT-07 两面板分歧（browse ⚠️ vs perf ✅）→ **✅ + watch 注**：chrome ledger 消费面 ≥4 类各持 kill 权是单用户 CC 设计内（单写者台账 + kill 前 cmdline 归属验证红线在），多 server 并发互扰（OBS-R11-3，2 次实证）有测试出口 LASSO_LAUNCHED_CHROMES_PATH；若未来多实例产品化再升 ⚠️ |
| 起步最小集（02 §1 六条） | R-FF-01 ✅ / R-FF-02 ✅ / R-DEP-03 ✅（修复后）/ R-CHG-01 ⚠️（接受理由在格）/ R-CI-01 ✅ / review 三问 ⚠️（#1 命中 1 项记 watch）——**达标（无 ❌）** |
| 声明 | 阈值均为起点值未校准（02 §0.3）；判定证据全部 L1+（grep/AST 实跑/单测/真机），L0 注释不承载结论 |

---

## 5. 03 审查测试清单终审结论

**R1 裁决：DECISION = ALL-CLEAN**（判据三核全过：零 fail ✅ / 02 评估全绿或差距均有接受理由 ✅ / 03 全过 ✅）。

- **§1 六维**：1.1 规范（修复全沿既有范式零风格游离）/ 1.2 数据逻辑（本轮三处 🔴/🟡 缺陷全落本维且闭环——FT-DEF-3 把 mock 升级为 gotHost 契约断言、FT-DEF-1′ 补 INV-76(m)、INV-71 检测体+mutation 迁形被 selftest 抓出假绿即改即验，是 §2.1 项 3「测试必须能失败」的正面示范）/ 1.3 业务逻辑（targetId 连续性边界显式论证）/ 1.4 端到端（stdio 真机值级输出 jsonl 在档；三处争议点源码+真机双证落定）/ 1.5 性能（L-COST 14 行回填 + Heisenberg 纪律执行到位）/ 1.6 简单架构（见 §4）/ 1.7 冗余（doc 声称 vs 真机漂移已回写；死别名已删）。
- **§2 五阶段**：2.1 单测（+13 钉，selftest 20/20）→ 2.2 集成（真 server 流 + 契约同步 fixture 钉）→ 2.3 冒烟（真机值级断言）→ 2.4 性能（L-COST 表 + 资源三采样 released 全 ✅）→ 2.5 用户（场景 0 done；07/08 人工面 + T-LIFE-02 移交用户；**非作者签字 = R1 记录**）。
- **§3 闭环**：6 缺陷全部「判用例→修产品→回归钉→门禁」闭环 + §3.2 分类回溯（FT-DEF-3→1.2 项1/1.4；FT-DEF-1′→1.2 项1+1.4 项1；W-DEF-R11-1→1.2 项7）+ 防复发清单项（INV-76(m)/gotHost 契约断言/INV-71 迁形）；§3.4 新鲜复审者红线由 R1 独立签字满足；flaky 政策（24h 隔离）执行——门禁并行负载 9/12 红均隔离复跑全绿判用例侧，处置固化为「门禁必须串行跑」。
- **门禁独立复跑两轮**（R1 本机串行）：build ✅ / **2240** ✅ / **81 INV** ✅ / **selftest 20/20** ✅ ×2。

---

## 6. 修改清单与残留

**本轮工作树修改（19 M + 证据未跟踪件，git status 实核）**：
- 产品修复 8 文件：`src/channels/BrowseChannel.ts`（doWait）/ `src/logged-in/TabSession.ts`（守卫 2 targetId + 别名删除）/ `src/config/config.ts`（mergedEnv）/ `src/config/provider-registry.ts`（null-safe ×2）/ `src/doctor/doctor.ts`（zhipuEndpoint）/ `src/index.ts`（doctor 接线 + LoggedInChannel caller）/ `src/channels/LoggedInChannel.ts`（直调 listPages）/ `src/ssrf/ssrf-guard.ts`（IPv6 剥括号）/ `src/browse/HighRiskGate.ts`（parseEvalResult）。
- 守护体系：`src/invariants/check-invariants.mjs`（INV-76(m)/INV-71）、`scripts/inv-selftest.mjs`（mutation 迁形）。
- 测试 +13：browse-upstream-contract ×2 / tab-session ×2 / doctor-cli-config-file ×3 / runtime-hot-plug ×2 / ssrf-guard ×3 / high-risk-gate ×1。
- 文档：doc/17（状态列/§5/§6/勘误 F-a..F-d/F-R11-1..3/O-1/O-4/U-16 姿势注/§0.3 警示 + 本执行记录段）。

**残留清理终态**：各面板自 spawn 浏览器/server 全收（lasso 特征树 0 进程/0KB、台账 []、9223 等端口释放）；用户 Chrome（pid 2420 持 9222）与兄弟会话 chrome-devtools-mcp 树未触碰；replay fixture 为产品语义内资产保留并恢复真实 mtime；search-cache 为 7 天 TTL 产品缓存语义不清除。

---

## 7. 遗留与建议（round2 / 用户移交）

**A. 环境恢复回归（4）**：T-SEARCH-14 + T-SERPHTTP-01 + L-COST-02（TUN 出口解挡/ddg 202 与 brave 429 消退后）；T-SEARCH-15（archive.org 限流解除）；L-COST-12 9222 空闲口冷启真值（现值 2592/3181ms 已由浏览域建立）。
**B. 人工面（2+1）**：T-ELICIT-07/08——建议用户择窗在 CC ≥2.1.76 客户端按 c1 清单执行场景 1-3（FT-DEF-1′ 修复后该面才首次可测）；T-LIFE-02（visible 档专门窗口，防打断用户焦点）。
**C. watch 项裁决批次**：两套 tmp 根统一 SPILL_ROOT + O-1 截断帽（slice(0,N-1)+"…"）两处同改；R-DEP-04 schema↔interface 奇偶 INV；infra O-6（logged_in 死 CDP snapshot 假 worked 候选——W1-DEF-5 同类，round2 logged_in 专项）；OBS-R11-2（fill 可填性预检）/OBS-R11-4（steps per-step options）/OBS-R11-5（附录 href 绝对化）；R-CI-07 PR checkbox；O-7/O-8（cloud 键 file 解锁 / SIGHUP keys 语义）留产品 owner。
**D. 双盲验证（03 §3.6）**：派新 agent 复查 INV-76(m)/INV-71 迁形后的抓获力（防规则空转）；对 R1 的 R-DEP-03 修复做非作者复核（R1 亦为修复作者，需新鲜眼睛）。
**E. 持续观察**：L-COST-05/06 偏慢样本（搜索域超 44%/<50% 判读线，判环境+场景）；R-CHG-03 mock≠真契约趋势（第 4 例已现，铁律已补，新增 evaluate_script 消费点时 PR 检查）；timing-sensitive 用例建议迁移 15s 桶或放宽断言窗（expect-poll 80ms）。

**终局结论：doc/17 功能测试全流程第 1 轮 ALL-CLEAN 收敛——零 fail、门禁 2240+81+20/20 只增不减、6 产品缺陷修复闭环、02 简单架构 ❌=0、03 全过；遗留全部为环境/人工/key/观察四类外部依赖，无产品侧阻断项。**
