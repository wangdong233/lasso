# 19-最优性审查轮次 · 总结（00-总结）

> 汇总员：轮次汇总员。日期：2026-08-17。
> 输入：本目录全部 14 份在档文档（round1 四域调研/裁决/实施/审查/手测清单 + round2 四域复审/裁决），另含汇总员独立复核——对「第 2 轮 14 项是否实施」做了逐项源码 grep 核验，并重跑全量门禁（见 §5）。
> 基线事实：工作树 v1.11.0（HEAD v1.10.0，round1 改动未提交），门禁 1906 TS tests / 79 INV / 193 Rust tests 全绿。

---

## 1. 各轮概况

### 第 1 轮（2026-08-15 → 08-17）——裁决 TUNE 16 项 → 全部实施 → 审查 ROUND-PASS，剩余 0 项

| 阶段 | 文档 | 结果 |
|---|---|---|
| 四域调研 | round1-arch / round1-browser / round1-desktop / round1-search | 白盒对标 FastMCP·官方 SDK·agent-desktop·Peekaboo·pi·oculos·UI-TARS·defuddle·firecrawl·open-webSearch·chrome-devtools-mcp·Steel·patchright 等；全部锚点带源码行号。核心差距判定：desktop「能看不能点」（ax_act 占位 + CGEvent 无鼠标）、驱动层锁版滞后 11 个月/57 版（0.3.0 vs 1.7.0）、freshness 上游能力闲置、工程卫生债 |
| 裁决 | round1-verdict | **ROUND-TUNE（16 项：P0×3 / P1×5 / P2×6 / P3×2）**。抽验 ~25 锚点全部属实、零虚报。四维总评：技术选型/架构/范围**已最优**（历史「不做」决策全部被上游演化正向验证），差距集中在实施维度。拒绝清单 R1-R11（§4） |
| 实施 | round1-implementation | **16/16 全部落地**：v1.10.0 → v1.11.0（66 文件 +2644/-431），1801→1905 tests（+104）、78→79 INV（新增 INV-79；INV-11/73/76 语义修订）、cargo 193。T1 chrome-devtools-mcp 1.7.0 迁移（含 `--no-usage-statistics` 全 spec、Browserbase `--wsEndpoint`）；T3 ax_act 真实现（AXPress/AXSetValue 写后读回/AXScrollToVisible/三元组 stale）；T7 CGEvent 鼠标四路径；T8 skeleton+防环+wrapper 中和；T9 DDG 非 CJK 兜底；T10 LASSO_PROXY（logged_in 双负向测试钉死）；T13 inv-selftest 制度化（10/10 红转）。实施者纠正裁决官预检一处偏差（1.7.0 `wait_for.text` 实为 `array(string).min(1)`），后经独立验证正确 |
| 审查 | round1-review03（六维+五阶段，独立 tarball 白盒 + 真机实测） | 六维全过；**发现 2🔴 + 4 次要并全部修复**：F2🔴 act 与 walk() 编号漂移（真机 VSCode 12v14/26v27 实锤——T3×T8 同轮 diff 接缝错配，修复为 walk 单一真源+锁步编号，复测 12==12/24==24）；F3🔴 Safari profile 伪造 `Safari/27.0` token（修为冻结值 605.1.15 + INV-73 9b 机械化）；F1/F4/F5/F6 次要（文案指向不可达 action / 防环 O(n²)→HashSet / ddg region 硬编码 / unused import）。终裁 **ROUND-PASS（修复后）**，剩余调优项 0；遗留 7 项（真机手测 ⏳ 为主）入下轮输入 |

### 第 2 轮（2026-08-17）——复审 + 裁决 ROUND-TUNE 14 项；实施未发生

| 阶段 | 文档 | 结果 |
|---|---|---|
| 四域复审 | round2-arch / round2-browser / round2-desktop / round2-search | round1 十六项落地复核**无一返工**（T12/T16 达最优；T1/T4/T5/T10 达最优且多处超验收线——proxy 双负向、INV-79 守护、单一真源；desktop 五项全部高于最低验收线）。新发现集中四类实施尾巴：①装配接线遗漏（setMetrics 生产零调用、INV-79 未注册违规样本）；②链尾诚实性（档4 推断即 worked、wait 首命中即真、snapshot 截断不可区分）；③事件物理质量（drag 无插值、click 无 clickState、type 降级三环全死）；④已装依赖利用度（defuddle URL 空串 → ~30 站点 extractor 零激活 + separateMarkdown 闲置）。另两项量级发现：stdin-EOF 孤儿窗口（上游 SDK #2002 + 停机三路径盲区三方互证）、E1 实测 headless HTTP 层「Windows UA ↔ macOS sec-ch-ua-platform」OS 级矛盾 |
| 裁决 | round2-verdict | **ROUND-TUNE（14 项：T2-1..T2-14）**。16 条候选源码声称经裁决官逐条白盒/本地实测抽验 **16/16 坐实、零夸大**。四维总评：选型/架构/范围**维持已最优**（watch/NO-GO 复核零翻案且多数被新证据加固）；14 项全部实施层、零架构项、零范围项、零新依赖。裁决书自身预期：「若下一轮实施尾巴清偿后无新量级发现，循环应自然终止于 ROUND-CLEAN」 |
| 实施 / 审查 | ——（无在档文档） | **未发生**。汇总员独立核验（源码 grep + 门禁重跑）证实 14 项全部未实施（证据见 §2），版本仍为 v1.11.0 |

**轮次记录勘误**：编排器轮次记录为 `{"round":2,"verdict":"CLEAN","items":0}`，与在档 round2-verdict.md（ROUND-TUNE 14 项）及工作树证据不符。本总结以在档证据为准——第 2 轮的正确记录应为 `{"round":2,"verdict":"TUNE","items":14}`（待实施后更新）。

---

## 2. 终态：未收敛于零调优（诚实说明 + 剩余项与建议）

**结论：循环未达「第 X 轮零调优」终态。** 轮次上限 3 轮，当前消耗 2 轮；第 2 轮处于「裁决已出、实施未启动」状态。距全维最优的唯一距离 = round2 的 14 项实施尾巴。

**核验证据（汇总员 2026-08-17 逐项 grep + 全量门禁重跑，均坐实「未实施」）**：

| # | 检查 | 证据 |
|---|---|---|
| 1 | T2-1 mac_chrome profile | `src/browse/stealth-profiles.ts`/`src/index.ts` 无 `mac_chrome`，装配仍硬编码 `windows_chrome_120` |
| 2 | T2-3 defuddle URL 透传 | `src/browse/markdown-extractor.ts:102` 仍为 `Defuddle(html, "", {})` |
| 3 | T2-4 separateMarkdown | 同文件零命中 |
| 4 | T2-5 machine_mcp freshness / DDG `df=` | `MachineMcpSearchChannel.ts` 无 freshness；`serp/extract.ts` 无 `df=` |
| 5 | T2-6 档4 闭环 | `ScreenshotVlmProvider.ts` 无 `vlm_inference_only`，act 仍推断即 worked |
| 6 | T2-7 type 兜底 | `ax.rs` 无 `AXFocused` 兜底路径 |
| 7 | T2-8 鼠标物理质量 | `cgevent.rs` 无 clickState/插值/Duration |
| 8 | T2-9/11 find actions / truncated | `ax.rs` 无 truncated 信号；`action_names_of` 仅用于 act 前置校验 |
| 9 | T2-10 稳定性采样 | `DesktopChannel.ts` 无 consecutive 计数 |
| 10 | T2-12 stdin EOF | `src/index.ts` 无 `on("end")/on("close")` 停机钩子 |
| 11 | T2-13 setMetrics 接线 | `src/` 生产代码仅定义（ToolManager.ts:77）零调用 |
| 12 | T2-14 INV-79 样本 | `scripts/inv-selftest.mjs` VIOLATION_SAMPLES 无 INV-79（10 条不变） |
| — | 版本 | package.json / index.ts = 1.11.0（第 1 轮产物，无第 2 轮 bump） |

**剩余 14 项清单（round2-verdict §2，全部零新依赖、单点可回滚、不破 INV/tri-state/简单架构）**：

- **P1（6 项）**：T2-1 headless 默认 profile 宿主 OS 对齐（mac_chrome + darwin 装配分支 + UA-skew hint rider）；T2-3 defuddle URL 透传（激活 ~30 站点 extractor + 链接绝对化，禁 useAsync）；T2-4 defuddle separateMarkdown 接管转换档（GFM 表格/MathML→LaTeX 保真；与 T2-3 同 PR）；T2-6 档4 screenshotVlm 闭环（VLM→cgEvent 真执行或诚实 unknown——tri-state 链尾最后一块）；T2-7 ax 档 type 兜底（AXFocus+合成键盘，仅 ASCII）；T2-12 stdin EOF→shutdown 两行（CC 崩溃孤儿窗口 ≤1h→即时收尾）
- **P2（4 项）**：T2-5 freshness 补全（machine_mcp 透传 + DDG `df=`）；T2-8 cgevent drag 插值 + clickState + down→up 间隔；T2-13 `toolManager.setMetrics(metrics)` 一行接线；T2-14 INV-79 注册违规样本 + selftest 覆盖率报告
- **P3（4 项）**：T2-2 Stagehand R-ECO-6 档案更新 + BrowseChannel 陈旧注释（纯文本）；T2-9 find 命中节点附 `actions` 数组（R4 最小形态）；T2-10 wait/expect 连续 2 次命中才算；T2-11 snapshot 截断诚实信号 `truncated`

**建议**：

1. **按裁决书建议次序实施**：T2-12（两行、最高杠杆比）→ T2-3+T2-4（同 PR）→ T2-6/T2-7/T2-1 → P2/P3。每项独立过门禁，基线 **1906 tests / 79 INV 不减**（新增只增不减）。
2. **实施完成后进入第 3 轮复审**（上限内）。round2-verdict 自身的收敛轨迹预期：实施尾巴清偿后无新量级发现即自然终止于 ROUND-CLEAN。
3. 随实施**回填 round1-manual-test-checklist** 仍 ⏳ 的真机项（A1-A8 部分 / B1/B2/B5 / C1-C4 / E1-E5；D1 本机 macOS 12 N/A），守「不伪造已验证」纪律。
4. 顺手项：W-3 timing-flake 用例移入 timing-sensitive 分桶（round2-arch 在案）；R9 doctor 拆分维持「自然触碰时顺带拆」。

---

## 3. 四维终局最优性结论（以两轮在档裁决为据）

| 维度 | 结论 | 依据（两轮一致，无翻案） |
|---|---|---|
| **技术选型** | **已最优** | round1：所有 2025-2026 选型判断被生态正向验证——defuddle+turndown 是社区共识现代化路线（markitdown 属本地文件域被 INV-68 正确排除）；chrome-devtools-mcp 是四通道驱动层官方唯一选项；不跟进 Camoufox（上游自认断档）、不依赖 puppeteer-extra 生态（弃维 3 年）两次「不选」被上游演化证实；Brave 英文艺主力有基准背书。round2：锁版 1.7.0 = npm latest（零滞后）；SDK ^1.30.0 = v1 线拉满且与驱动层 bundle 同 era；DDG 兜底端点/selector 与社区标杆 open-webSearch 逐字一致；Rust 自持 CGEvent 路径价值因 nut.js 停更反升；2026-07 独立基准反向验证「不承诺 DataDome/Kasara」诚实定位。唯一量级差（defuddle 利用度一半）属实施层（T2-3/T2-4）。持续 caveat：SDK v1 维护死线 ≈2027-01（roadmap Q4 评估，§4） |
| **架构** | **已最优** | round1：CapabilityBag（audit trail+熔断自动联动）、doctor 内生自检、INV 静态守护三大特色机制在业界无「更优可直接替换」实践（FastMCP 的框架化方案更通用但更浅，TS SDK v1 无工具钩子，引入即违简单架构红线）；16 项调优 0 项涉及架构改动。round2：四通道+FallbackChain+INV 体系在 round1 落地后无结构性缺口；INV-79 把迁移面固化成守护、F2 单一真源重构超验收线；本轮唯一量级新发现（stdin-EOF 孤儿窗口）是上游 SDK 缺陷收尾（修复两行、与上游 #2002 同构）而非架构问题；档4/type 属「补全既有架构承诺」非改架构。无 shim CDP 控制面正确停在 watch v2.0 |
| **范围** | **已最优** | round1：全部「不做」决策（browserless 第五通道/Camoufox/SearXNG/search+scrape 融合/fetch 升级梯/agent 框架化/pi epoch 泳道）均有上游证据或 INV/简单架构红线支撑，无一推翻；desktop act 判定为「已声明未实施」的完成而非范围扩张。round2：watch/NO-GO 复核**零翻案**且多数被新证据加固（camoufox 维护者离场加固 NO-GO、firecrawl x402 反证 INV-58 边界、patchright 增益边际化降权、Steel #245 仍 open 背书现状）；新诱惑（CloakBrowser/nodriver 范式/MCP Tasks/UTCP）全部正确停在 watch/NO-GO；14 项调优零新通道、零新依赖、零新机制 |
| **实施** | **唯一活跃维度（当前差距所在）** | round1：16 项全部落地且过六维审查（2🔴+4 次要由审查发现修复），实施质量整体扎实（T1 契约纠错、T13 selftest、T11 保守三态、T15 接线超预期）。round2：识别 14 项实施尾巴（装配接线遗漏/链尾诚实性/事件物理质量/已装依赖利用度），**未实施**——这是当前与「全维最优」的唯一距离（§2）。两轮的实施差距全部可在「单人+AI 一轮一项」粒度内收敛 |

**总判**：选型/架构/范围三维经两轮独立生态复核稳定收敛于最优；实施维度 round1 已清偿量级差（16 项），round2 剩 14 项尾巴（无一项架构/范围级）。若第 3 轮实施清偿后无新量级发现，循环自然终止。

---

## 4. roadmap / watch / NO-GO 沉淀清单（两轮拒绝项合并，供未来版本）

### 4.1 roadmap（带时间窗）

| 项 | 来源 | 处置与依据 |
|---|---|---|
| SDK v2 迁移（R10/O-1） | R1 裁决；R2 复核 | **2026-Q4 评估**。v1 死线 ≈2027-01；官方 codemod 已 stable（成本从手工全量降为机械改+复核）、v2 双 era 同进程；但驱动层捆 SDK 1.30.0、CC 侧 era 未明——不提前。W-1 验收清单已具体化：①迁移前后跑官方 conformance 套件建 wire 基线 ②验证 v2 #2622（listChanged 静默覆写）对 CapabilityBag/INV-37 链的影响 ③codemod dry-run 评估 `server.tool(` 17 处转换率 ④保持旧 era 协议 |
| patchright L2 stealth 备选通道（R1） | R1 裁决；R2 **证据降权** | **v2.0**。2026-07 独立基准实测仅 +1 OK vs vanilla（25 vs 24）；其有效成分（真 Chrome 二进制）Lasso 经 T1+F1（1.7.0 headless 默认系统 Chrome stable）已拥有；属 Playwright 系 = automation-protocol 轴同挂。触发条件不变（L1 实测不够用），预期收益下修 |
| admin/doctor/wayback 补 outputSchema（R5） | R1 裁决；R2 无新证据 | **watch，捆绑 SDK v2 迁移一并做**（摊薄成本）。单 CC 客户端读 text envelope 现状无痛点 |

### 4.2 watch（触发条件写死）

| 项 | 来源 | 触发条件 / 依据 |
|---|---|---|
| PruningContentFilter 纯 TS port（R2） | R1 裁决；R2 维持 | doctor/实测出现 ≥3 个「defuddle 抽取失败或 markdown 档超 envelope 高频落盘」真实站点案例（T2-4 已拿更基础的转换保真杠杆） |
| stateId 句柄缓存 + LRU（R3 完整版） | R1 裁决；R2 维持 | T3「同序重编号+三元组比对」已覆盖安全语义；句柄缓存引入 invalidation 第二机制（R-INT-07 教训）。act 延迟实测成痛点再议 |
| 树级 actions 数组全量暴露（R4 完整版） | R1 裁决；R2 升级最小形态 | 最小形态（find 命中附 actions）已列为 T2-9 待实施；snapshot 全树暴露待 token 预算评估 |
| Steel per-session release + 钉镜像 tag（R6） | R1 裁决；R2 维持 | 上游 #245 仍 open（per-session 端点近期镜像失效）→ 全量 release 现状被上游状态背书；待 #245 修复或 Steel 下次触及时顺手做 |
| 无 shim 直连 CDP 控制面（nodriver 范式，W-B1） | R2 新增 | v2.0 议题：2026-07 基准唯一实证有效轴（协议层隐身），但 = 弃 chrome-devtools-mcp 全部工具面的架构级换血，与 38 条 R-INT 冲突；Python+AGPL 与 MIT/npm 分发冲突。触发：L1+T2-1 落地后实测仍高频拦截且用户刚需 |
| CloakBrowser（W-B2） | R2 新增 | 12 周 13.5k+★ 热度真实，但独立基准无 headroom（= curl_cffi 平手）、darwin-arm64 管线停更 ≥2 个月（与 macOS 主力直接冲突）、二进制自定义 license。触发：darwin 复活 + license 澄清 + 基准复测有真实增益 |
| MCP Tasks 扩展（`io.modelcontextprotocol/tasks`，W-2） | R2 新增 | spec 级长时任务范式与长时 browse/桌面操作语义匹配度高。触发：CC 客户端宣布支持，或 v2 迁移时一并评估 |
| doctor.ts 拆分（R9/O-2） | R1 裁决；R2 维持 | 功能是差异化资产；+67 行/轮的膨胀速度证实「下次自然加 check 时按 section 顺带拆」正确，单独搬家不抵扰动 |
| Electron DevTools 路线（agent-desktop `launch --cdp`，D9'） | R2 新增 | CDP-for-desktop 是重基建（进程启动参数控制）；Lasso 的 AX 内修复（wrapper 中和）已够用，无翻案必要 |
| npm 预编译二进制分发（D10） | R1 记档；R2 维持 | rust-helper 现要求用户 cargo build；agent-desktop postinstall 拉平台产物模式可参考。属发布工程轮次 |
| CGEventPostToPid 进程定向投递 / background-first（C-2） | R1 记档；R2 维持 | 生态主方向（Peekaboo v4.1/4.2 全押）但需 multi-session 安全基建（签名 receipts/session/generation），Lasso 无该威胁模型。触发：真实多任务干扰痛点出现 |
| Mojeek/Startpage 第二免费兜底引擎 | R2 新增 | 对标 free-search-mcp 仅 51★ 不足为据；待 SerpHealthMonitor 积累 ddg miss 率数据 |
| You.com 等零 Key 远程源填 L3 | R2 新增 | machine_mcp 已覆盖零配置场景；L3 填充无用户需求驱动 |
| T10 doctor proxy 用例 timing-flake（W-3） | R2 观察 | 全量并发 5169ms 失败/单跑 1648ms 过；随任一触及测试的 PR 移入 timing-sensitive 分桶（vitest.workspace.ts 有先例），不单独立项 |

### 4.3 NO-GO（确认不做 / 一句话否决）

| 项 | 来源 | 理由 |
|---|---|---|
| browserless 第五通道 / agent-browser CLI 范式（R7） | R1 裁决；R2 维持 | 面积扩张违 R-INT；CLI vs MCP 是产品形态选择非结构差距 |
| Camoufox（R7） | R1 裁决；R2 **加固** | 上游自认断档+性能下降；round2 主维护者官宣离场 |
| SearXNG 自托管通道 / search+scrape 融合 / fetch 内置浏览器升级梯（R8） | R1 裁决；R2 维持（x402 融合深化反证） | 运维成本违单人可持续；融合违 INV-58 族边界 + token 经济性劣；升级梯违 INV-23 |
| Windows/Linux 深度实装 / pi epoch·泳道并发 / oculos dashboard（R11） | R1 裁决；R2 维持 | 愿景明示 macOS 主力 + Win/Linux 适配（uia/atspi 诚实桩契约就绪）；单 CC 场景串行足够 |
| FastMCP 化 / 通用 middleware 管道 | R1 裁决；R2 维持（FastMCP 4 不改判） | 违简单架构红线；TS SDK v1 无工具钩子，单点 wrapHandler（T14）已是最小形态 |
| UTCP 第二协议 | R2 一句话否决 | CC 说 MCP；引入第二协议违 R-INT |
| markitdown 引入 | R1/R2 维持排除 | 本地文件域 + INV-68 禁 spawn python |
| llms.txt / `.md` 自动探测 | R2 否决 | CC 自己会拼；fetch_url 自动试别的 URL 属越权魔法（违 INV-23 精神）；仅做 description 提示级（T2-5 附注） |
| schema-based 结构化抽取 tool | R2 否决 | CC 即 schema 执行器（evaluate_script）；加层 = 过度设计 |
| StagehandChannel 重写对齐真实 REST | R2 不做 | 官方 REST 已上线但形状不匹配（sessions.* 生命周期、无 /verify 路由、v0 unstable）——重写即功能缩水；仅更新档案（T2-2），v1.8 决策点留准确底账 |
| sec-ch-ua header 侧注入（Network.setExtraHTTPHeaders） | R2 不做（等上游） | chrome-devtools-mcp 1.7.0 仍不暴露该工具；T2-1 用 profile 宿主对齐绕开，零新机制 |
| 7 级 scroll 瀑布 / auto-wait 默认开 / 签名 receipts+background-first 投递 / 剪贴板粘贴 type 兜底 | R2 不做（D9' 排除记录） | 档3 坐标滚动已可兜且瀑布违 R-INT；隐藏 5s 延迟负收益；multi-session 安全基建无威胁模型不跟；剪贴板污染用户数据面 |

---

## 5. 全量门禁终态输出（汇总员 2026-08-17 于当前工作树实跑）

```
$ npm run build            → 0 error（tsc 全绿）
$ npm test                 → Test Files 120 passed (120)
                             Tests 1905 passed | 1 skipped（1906 总量；本轮 0 fail，
                             round2 报告在案的 1 例 timing-flake 为负载相关、本次通过）
$ npm run check-invariants → All 79 invariants passed（含 INV-79；INV-11/73/76 语义修订后）
$ npm run inv-selftest     → 10/10 sampled pins flipped red under violation（工作树零污染）
$ cargo test（rust-helper）→ 193 passed / 0 failed（42+87+9+10+30+9+… 六个 test target）
版本三处同步               → package.json / src/index.ts LASSO_SERVER_VERSION = 1.11.0（INV-63）
```

基线对比：v1.10.0 = 1801 tests + 78 INV → **v1.11.0 = 1906 tests + 79 INV + 193 Rust**，全绿不减。

真机状态（round1-review03 归档，历史事实）：headless 冒烟 14/14（`round1-smoke-headless.mjs`）；httpbin 服务器端 UA 回显逐字节 = profile UA；find/act 编号 12==12、24==24（F2 修复后）；三条负向路径（press→ax_unsupported_action / bad-ref→invalid_params / out-of-range→stale_ref）。

诚实 pending（不伪造已验证，沿 uia/atspi 先例）：手测清单 `round1-manual-test-checklist.md` 中 A1-A8（部分）/ B1 / B2 / B5 / C1-C4 / E1-E5 仍 ⏳ 待真机回填；D1（macOS 15+ Event Synthesizing）本机 macOS 12 N/A。

---

## 汇总员裁定

- 第 1 轮完整闭环（调研→裁决 TUNE 16→实施 16/16→审查发现并修复 2🔴+4 次要→ROUND-PASS，剩余 0）。
- 第 2 轮裁决 ROUND-TUNE 14 项**未实施**，循环**未收敛**；轮次上限 3 轮内建议：实施 T2-1..T2-14 → 回填真机手测 → 第 3 轮复审，预期自然终止于 ROUND-CLEAN。
- 编排器轮次记录第 2 轮「CLEAN / items 0」应更正为「TUNE / items 14」。
