# doc/ 整理决议（2026-09-16 · DocAudit 轮 · 先报后删）

> 状态：**对抗轮已签章（2026-09-16，§5）——A1-A4 删除与 B2 已获准执行**；签章前未执行任何删除，签章后删除随本决议同 commit 落地。
> 盘点人：BUG-08 审查循环 doc 整理员（独立于五批实施者）。
> 盘点基准：HEAD `7ab999b` 工作树（v1.26.0 预写态；npm latest 1.25.0）。
> 判定标准（用户指令）：被代码/README/INV/兄弟文档引用 = 重要（保留或归档）；五批已完结的过程决议且被 supersede = 归档；过程性冗余（可再生机器产物）= 删除（git 历史可找回）。

## 0. 盘点总览

| 区域 | 文件数 | 体量 | 处置概要 |
|---|---|---|---|
| doc/ 根级 md（含 README） | 8 | ~129KB | **全保留**（渲染档文档组 7 件全部被引用或为生效裁决；README 为索引本轮刷新） |
| doc/usage/ | 3 | 80KB | 全保留（01/02 已对齐 2026-09-15 BUG-08 批；03 内容债在案） |
| doc/architecture/ | 2 | 76KB | 全保留（01 冻结基线；02 排期本轮增补 v1.19→v1.26 九行） |
| doc/testing/ | 1 | 176KB | 保留（2026-09-16 审查循环刚更新 R-INT-06 后记） |
| doc/bugs/ | 10 | 344KB | 全保留（BUG-01..08 档案 + 04-附录E；src 注释 ×多处 + README 引用在案） |
| doc/governance/（md） | 69 | ~1.5MB | 全保留（01-12 审计/裁决档案；src 注释引用最密） |
| doc/governance/08/verify-data/ | 38 | ~0.5MB | **删除候选 Tier A（9 件未跟踪 .log）+ Tier B（29 件已跟踪原始证据，两案待裁）** |
| doc/governance/05/.devteam/devteam.db | 1 | 450KB | **删除候选 A4**（agent 工具状态二进制，非文档；.gitignore 已列 .devteam/ 但该文件早于规则被 track，需 git rm --cached） |
| doc/history/01-功能测试执行记录/ | 252 | 3.6MB | 18 件 md（裁决蒸馏）保留；**234 件原始机器证据 = Tier A（65 未跟踪 .log）+ Tier B（169 已跟踪）** |
| doc/archive/ | 60 | 1.6MB | 保留（2026-08-27 已归档就位 + 索引在 doc/README；src 注释引用 research/16 §5 等 ×14） |
| doc/assets/ | 2 | 452KB | 保留（README 中英双语 `<img>` 引用） |
| 仓根杂散 | 1 | 1.8KB | **删除候选 A1**（w2-browse-stderr.log，未跟踪 stray） |
| **合计** | **446**（doc/）+1 仓根 | ~10MB | |

## 1. 现行权威（保留原位，共 ~95 件 md + 2 图）

### 1.1 根级渲染档文档组（7 件）——全部保留，理由逐件在案

| 文件 | 引用方（实证 grep） | 定性 |
|---|---|---|
| `渲染档设计决议.md` | src ×8（render-launcher/render-flags/render-guardian/render-doctor/chrome-ledger/chrome-stop/chrome-idle-reaper/index.ts） | 生效设计决议（v1.19.0 实施唯一依据，含 r2/r3 修订） |
| `渲染档-并行验收隔离配方.md` | README.md + README.en.md（正文链接）+ src ×3 + test/render-tier-acceptance.mjs | 用户向支持配方（活文档） |
| `需求-渲染档浏览器治理.md` | 渲染档设计决议（需求真源 R1-R7）+ test/render-tier-acceptance.mjs | 需求真源（R7 重开条件仍被 登记#10 引用） |
| `对接实施说明-渲染档x-media-gen-mcp.md` | 渲染档设计决议（接口契约真源）+ 并行验收隔离配方 | 消费方接口契约（media-gen-mcp attach 依据） |
| `性能准确率优化裁决表.md` | src（desired-hide-enforcer.ts）+ test ×5 | 裁决档案（PERF-1/2a/ACC-1①② 立即修项的出处） |
| `提案-render-stop端口作用域化.md` | 并行验收隔离配方 §0（「已于 2026-09-02 裁决并落地…§6」） | **历史里程碑但保留原位**：v1.20.0 `--stop` 端口作用域化行为的唯一裁决依据文档；仅 1 处引用，移动收益 < 引用破坏风险。对抗轮如坚持归档，须同步改 配方 §0 引用路径 |
| `登记问题-timing假红与退出契约与通用化.md` | 渲染档文档簇（#10 显式引用 需求 R7 + 设计决议行号） | 活登记台账（#8/#9/#10 三项全部**开项**，含 need-later 重开条件——非已完结过程件） |

### 1.2 其余保留件

- `doc/README.md`（索引——本轮刷新：补 governance/12、bugs/03-08、根级渲染档组、新鲜度表）
- `doc/usage/01-03`、`doc/architecture/01-02`、`doc/testing/01`、`doc/bugs/01-08+附录E`、`doc/governance/01-12`（md 主体）、`doc/archive/`（已归档冻结）、`doc/assets/`（README 图片）
- 佐证：src/ 引用 doc/ 路径 40+ 处（governance/05/06/10、bugs/03/05/07/08、usage/01、archive/research/16 等），README 引用 15+ 处——引用链完整，无一悬空（唯一歧义引用 governance/12 头部「doc/lasso-报告-2026-09-08」实指 **cc-control 仓**路径，非本仓死链，bugs/04:3 有全路径佐证）。

## 2. 删除清单（先报后删——待对抗轮复核签章后执行）

### Tier A：未跟踪本地残屑（75 件，~600KB）——建议全删

**特性**：`.gitignore` 的 `*.log` 规则使这些文件**从未进 git**——「git 历史可找回」对它们**不成立**，删除即永久；但它们是 stderr 运行尾流，其内容已被 18 件 md 裁决蒸馏（ft-00-总结 / wave1-wave2-summary / ft-round1-{search,browse,infra,perf,verdict} / silence-acceptance / v19-verify / v1x-silent-verify），独立价值≈0。**此项不可逆性已如实标注，请对抗轮特别核。**

| # | 路径 | 件数 | 理由 |
|---|---|---|---|
| A1 | `w2-browse-stderr.log`（仓根） | 1 | wave2 期在仓根跑测的 stray（2026-08-15）；与 doc/history 内同名文件**内容不同**（cmp 实证，各属一次运行），两份都是尾流 |
| A2 | `doc/history/01-功能测试执行记录/**/*.log`（含 silence-data/ v19/ v1x-data/ 子目录） | 65 | 未跟踪 stderr 尾流；md 裁决在档。对抗轮引用核（2026-09-16）：4 件被同目录 md 按名引用（ft1-s1/ft1-s3-stderr ← ft-round1-search.md【首行已内联引】、t-entry-04 ← wave1-gov-search.md、wave2-search ← wave2-gov.md）——引用形态均为「md 内联引用承载数据 + 文件名作指针」，删除后指针死化但数据不丢；抽样 3 份蒸馏前提成立 |
| A3 | `doc/governance/08-静默性全面审计/verify-data/*.log` | 9 | 同上（t1/t2/t2b/t4a-t4e2 的 server/cdm stderr）。对抗轮引用核（2026-09-16）：**verify.md :48/:66 证据清单明文引用 t1-server-stderr.log 与 t2-server-stderr.log**（初审只记了 .json 引用面）——B2 保留 verify-data 内 .json/.mjs 而 .log 删除后该两处指针死化；承载性数据（六维表/residual=0）在 verify.md 表格与 t*-result.json（保留）中，蒸馏前提成立 |

### Tier A 扩展：已跟踪的工具状态残屑（1 件）

| # | 路径 | 处置 | 理由 |
|---|---|---|---|
| A4 | `doc/governance/05-颠覆性调研/.devteam/devteam.db` | **本地删除（对抗轮勘误：无需 git rm --cached）** | 450KB SQLite 二进制 = 调研轮 agent 编排工具（devteam）的运行状态库，非文档。🔴对抗轮实测勘误（2026-09-16）：该文件**从未被 git track**（`git ls-files` 空 + `git log --all` 零历史；`.gitignore:30` 的 `.devteam/` 规则对其有效，`git status --ignored` 显示 `!!`）——初审「早于规则被 track」为陈旧记忆（governance/11 F12 曾于 2026-08 `git rm --cached` 过它，此后再未入库）。故处置 = 仅本地删除，且**不可从 git 历史找回**（本就无历史）；全仓 grep 零引用 |

### Tier B：已跟踪原始机器证据（198 件，~2.5MB）——两案待裁，整理员荐 B2

| 范围 | 件数 | 内容 |
|---|---|---|
| `doc/history/01-功能测试执行记录/` 非 md | 169（=187 已跟踪 −18 md） | jsonl 抓取 30 / json 60 / mjs 探针脚本 64 / out·err·sh·png·txt·exit 15 |
| `doc/governance/08/verify-data/` 已跟踪 | 29 | t1-t4e2 真机验证的 .mjs 脚本 + .json 结果 + .txt 快照 |

- **B1（全删，git 可找回）**：符合「过程性冗余直接删除」字面；但与 doc/README 既有「证据冻结原样（不回改）」治理承诺冲突，且 verify.md 明文引用 `verify-data/t1-result.json`（删后成死链，需连改 verify.md）。
- **B2（保留 + npm 发布面收敛）——整理员推荐**：git 内证据冻结承诺不破；真正的缺陷（这 198 件 + archive 60 件随 `npm publish` 打进发布包，实测 npm pack 含 doc/history 187 件/doc 371 件）用 `.npmignore` 增列解决：
  ```
  doc/history/
  doc/archive/
  doc/governance/08-静默性全面审计/verify-data/
  ```
  预期发布包显著瘦身（doc/ 面约 6.5MB → ~1.5MB；先例 = v1.18.5 发布面清理）。README/doc-README 对这三处**零正文链接**（check-readme-sync E 项不受影响，已核）。
- 无论 B1/B2，A 组 75 件 .log 都先删（它们本就不在 git/npm 内）。

## 3. 对抗轮请重点复核的问题

1. A 组「不可逆删除」是否可接受（内容已被 md 蒸馏的前提是否成立——抽 3 份 .log 与对应 md 对照）；
2. Tier B 选 B1 还是 B2（若 B1：verify.md 的 `verify-data/t1-result.json` 引用与 doc/README「证据冻结」条款需同步改写）；
3. `.npmignore` 增列三行是否触碰任何消费方对 npm 包内 doc 路径的依赖（media-gen-mcp / cc-control 侧 grep）；
4. 提案-render-stop端口作用域化.md 保留原位 vs 归档（整理员荐保留，理由 §1.1）。

## 4. 本轮已执行（非删除类，纯增改）

- `doc/README.md` 索引刷新（governance/12 / bugs/03-08 / 根级渲染档组 / 新鲜度表 → 2026-09-16）。
- `ARCHITECTURE.md` v1.19→v1.26 对齐增补（头部横幅 / §2 渲染档行 / §11 INV 84→96 / 新 §16 五批新增面 / §14 版本要点 / §15 主线第 8-9 条）。
- `doc/architecture/02-实施排期.md` 增补 v1.19.0→v1.26.0 九行 + 文档结束增补注。
- `README.md` + `README.en.md` v1.26.0 changelog ①/(1) 补审查轮行为修复子句（3f73331 URL 规范化封口）；check-readme-sync 全绿。

## 5. 对抗轮复核签章（2026-09-16，双盲对抗复审员；§3 四问逐一裁定）

**① A 组不可逆删除（§3 问 1）**：抽 3 份蒸馏对照——ft1-s1-stderr.log 首行（machine_search_mcp_detected）↔ ft-round1-search.md T-SEARCH-03 内联引（逐字在档）；t1-server-stderr.log ↔ verify.md §2 六维表 + t1-result.json.residual（保留）；w2-browse-stderr.log ↔ 仓根/目录两份 cmp 实测 DIFFERENT（各属一次运行，均为尾流）。前提成立，**75 件 .log 删除准签**（不可逆性已如实标注，接受）。

**② Tier B 选 B1 还是 B2（§3 问 2）**：**裁定 B2**。实证：npm pack --dry-run 实测 doc/history 187 件 / doc/archive 60 件 / verify-data 29 件在发布清单；README.md/README.en.md/doc/README.md 对三路径正文链接 grep 零命中；消费方侧 grep（cc-control 全仓 + media-gen-mcp src/md/json）零依赖 npm 包内 doc 路径；.npmignore 增列三行不影响 check-readme-sync E 项。B1 会死链 verify.md 的 t1-result.json 引用并破「证据冻结原样」治理承诺——否决。

**③ .npmignore 消费方影响（§3 问 3）**：见 ②，零依赖，准签。

**④ 提案-render-stop 归档 vs 保留（§3 问 4）**：**维持保留原位**（整理员理由成立：v1.20.0 --stop 端口作用域化唯一裁决依据，1 处引用，移动收益 < 引用破坏风险）。

**对抗轮新发现并已处置**：
- **A4 事实错误（已勘误上文）**：devteam.db 从未被 track——处置从 `git rm --cached`+删除 改为 仅本地删除（不可逆）。
- **A3 引用漏记（已补注上文）**：verify.md :48/:66 引用 t1/t2-server-stderr.log，删除后指针死化（数据已蒸馏）。
- **B2 行为无测试钉（当场修）**：headless-stack-ledger 空账 unlink 行为（54d9975）在测试套件中零存在性断言（M2 变异「留 [] 文件」实测全绿逃逸——测试名「空账删文件」只断言读语义）；已补 `existsSync(sidecar)===false` 变异锚（红-绿闭环验证：clean 绿 → 变异红 1 failed → 还原绿）。

**签章结论**：A1/A2/A3/A4 + B2 全部准签执行；唯一否决项 = B1。

## 6. 对抗复审轮 2 勘误补注（2026-09-16，双盲对抗复审员第 2 轮；基线 HEAD 9ed9cfc）

**发现 1（P2-doc）：§5/A2/A3 死指针清单欠计——签章轮只记 6 名/5 件 md，实测 ≈22 名/11 件 md。**
全仓 grep 复核（本轮 §3「验证无引用」标准）实测 A1-A3 删除造成的新增死指针全集（引用文件 → 死化文件名）：
- ft-round1-search.md → ft1-s1✳ / ft1-s3✳ / **ft1-s4**
- wave1-gov-search.md → t-entry-04✳ / **u085b / u085d**
- wave2-gov.md → wave2-search✳ / **wave2-misc / wave2-rt06 / wave2-tools**
- ft-round1-perf.md → **ft-r11-server-stderr（及 ft-r11-* 族）**
- silence-acceptance.md → **ts05b / ts06**
- v1x-silent-verify.md → **v1-launch / v3-server / v3b-server / v6-headless**
- wave1-entry-cli.md → **server-stderr**
- wave2-desktop-cli.md:187 → **w2-browse-stderr（该行「留证」句就此失效；冻结档不回改，以此补注为准）**
- governance/08 verify.md:48/:66 → t1✳ / t2✳（✳ = 签章轮已注；粗体 = 本轮新记）

**裁定：P2，不改删除决议。** 依据：①引用形态与签章轮判定的 4+2 件完全同构（md「结论列」内联承载 stderr 关键行 + 证据列文件名作指针）——本轮抽 v1x-silent-verify.md V3a/V3b 行实证：`chrome_stop_result{...}` / `chrome_idle_reaped{idle_ms:2000, idle_for_ms:15680}` 等关键 stderr 内容逐字内联在档，蒸馏前提对欠计件同样成立，零数据丢失；②删除已不可逆执行，无补救动作空间，本补注即终态登记。

**发现 2（记录）：archive/parse* 与 governance/11 中 audit.log / lasso-v16-*.log / hide-enforcer.log 等死指针系 2026-08-27 前历史轮遗留，非本轮 A1-A4 造成（本轮盘点基线时该批 .log 已不存在）——登记不处置。**

**轮 2 独立复验记录（全部通过，零新 P0/P1）**：
1. gate 亲跑（后台化）：GATE GREEN——182 files / 2997 passed + 1 skipped / INV 96 全过 / readme-sync 绿（与主循环复验逐位一致）。
2. 变异验证 ×2（红-绿闭环，验后树净）：M2 重验（unlink→留 []：headless-stack-ledger.spec 恰 1 红=4b93a07 锚生效）；R1 新变异（normalizeForSameDocumentCompare 回退 raw 串比较：bug08-d-nav-semantics.spec 3 红，含 t7 行为级钉）。
3. 四轨漏检抽查各一面：①主 diff 三个 test-only commit（7b1c9f5/b02d338/0316d3f）零断言弱化（b02d338 反而收紧——按 args.function 过滤计数）；②架构漂移五可证伪探针全过（14 个 register*Tool 面基线↔HEAD 恒等 / src 二级目录恒等 / package.json 零新依赖 / INV 零删除仅增 96 / never_kill·zombie-gate·userTaken 红线符号与门约束零削弱——chrome-status.ts 唯一带减号行系 BUG-06 idleMs 字段增改非删除，descriptions.ts 的 never_kill_user_asset 行为 context 行实存）；③doc 引用链 md 面零悬空（src/test/README/ARCHITECTURE 全部 doc/*.md 引用逐一解析，3 个疑似死链均为 CJK 分隔符假阳性或历史注释）；④vitest.workspace.ts 覆盖面零收窄（两 project include/exclude 互斥并集=原覆盖，BUG-08 F 仅钉 pool=threads + 增 SLOW 成员，回退 forks 有孤儿治理锚钉红）。
4. 删除项落地复核：A1-A4 四处盘面零残留；B2 三目录出 npm pack 清单（dry-run 0 命中）。

**轮 2 终判：adversarial: clean（1 项 P2-doc 勘误即本节，无 P0/P1；删除与 B2 维持签章轮处置）。**
