# 设计决议：日常档 `--idle-ms 0` 幽灵常驻——硬顶天花板 + 双意图逃生口 + 超龄可观测 + 配方治理

> 事故日期：2026-09-10（用户实锤）· 实例：pid 97247 / port 9223 / 2026-09-09 23:36 由 CC 会话按固化配方拉起 · 严重级：**事故级**（违反产品宪法「静默服务前提 = 可发现可关闭」：无窗口 + 执守压窗 + 永不回收 = 12h 幽灵常驻，Dock/菜单栏不可见，用户「关不掉」；已当场 `chrome-stop` 收掉）
>
> 输入：白盒五任务闭环 + 外部调研（ZooKeeper maxSessionTimeout / browserless 全局 TIMEOUT 顶 / GKE Autopilot 平台级上限 / kubectl·systemctl·hdparm 双意图旗标先例）。设计合成员已对全部源码锚点逐条复核（launch-chrome.ts / config.ts / index.ts / chrome-ledger.ts / chrome-idle-reaper.ts / desired-hide-enforcer.ts / render-launcher.ts / render-guardian.ts / render-flags.ts / chrome-status.ts / doctor.ts / 既有测试面）。
>
> 🔴 **用户红线（沿用）**：禁项目外新组件；永不 kill 用户浏览器（userTakenAt/visible 豁免语义零变化）；**硬顶不得误杀合法长任务**（渲染档 `LASSO_RENDER_IDLE_MS` 语义独立——日常档显式 0 与渲染档无关，不得交叉污染）；消费方契约零破坏（nohup 配方行为变化属预期 breaking，须 changelog 显著位 + 通知）。纪律：改行为必配测试；`npm run gate` 读汇总行；单主题 commit 不 push 不发版。
>
> 🟠 **对抗复审 r1（2026-09-10 否定轮，设计修订员独立取证）**：两项 P1 否定发现经源码逐条复核**全部成立（零误报驳回）**：① 硬顶「在用永不杀」红线机械化存在**跨进程盲区**（browse 活动只写 server 进程 touchMap 不落盘，执守收割宿主不可见）→ 新增 **A-7 跨进程活动真源**；② §6.3 原案共享 reaper `hardCapMs` 缺省=24h 会打红 render-guardian 既有 null 钉子并使「渲染档零变化」承诺为假 → **缺省反转为 0（关），24h 下沉至两个日常档装配层**。修订明细见 §10。

## 1. 现象与溯源（白盒复核确认）

- 97247 = port 9223（媒体生成/Flow 惯例口），hidden 日常档，`--idle-ms 0` 显式启动。ownerPid 97245 已死是 **CLI 常态非异常**（launch-chrome spawn 后 `process.exit()`，所有权靠磁盘台账跨进程传递——chrome-ledger.ts 头注）。
- 配方源头链（C 治理对象）：`doc/bugs/02:45` 首创 `node dist/index.js launch-chrome --port 9223 --idle-ms 0`（v1.18.5 时代的正确缓解）→ 固化进 `doc/usage/02-TROUBLESHOOTING.md:397`（标注「推荐」）→ 传播至 `doc/usage/01-KEY-GUIDE.md:169`、`README.md:331/342`（+英文版）→ 消费方 media-gen-mcp `doc/flow-api-contract.md` 同步采用。v1.21.0（BUG-03 A1）已把 CLI **默认** idle 从 0 翻 30min，但显式 `--idle-ms 0` 仍字面无限——本决议根治该缺口。
- 事故时收割宿主其实活着：hide-enforcer 执守（97263）双职责收割在跑、阈值 60s（`enforcerCfg.launchIdleMs` = DEFAULT_LAUNCH_IDLE_MS）。**单点让 97247 不死的正是 record 级 0 分支**（chrome-idle-reaper.ts:240-241）。

## 2. 根因链（五点，全部设计合成员源码复核）

1. **CLI 解析放行 0**：launch-chrome.ts `--idle-ms` 分支守卫 `!Number.isNaN(n) && n >= 0`——0 合法入参。
2. **env 同样放行 0**：config.ts `parseLaunchIdleMs` 守卫 `Number.isNaN(n) || n < 0` 才回退默认——`LASSO_LAUNCH_IDLE_MS=0` 原样通过（0 =「禁用」文档化语义）。
3. **CLI 装配默认**：index.ts `launch-chrome` 路由——env 未设才用 `CLI_LAUNCH_IDLE_DEFAULT_MS`（30min）；env 设了（含 0）→ `cliCfg.launchIdleMs` 透传。
4. **台账保真落位**：chrome-ledger.ts `idleMs: typeof === "number" && Number.isFinite ? 原值 : undefined`——0 是 Finite 被保真记录（台账忠实记录请求，正确）。
5. **收割判定永久跳过**：chrome-idle-reaper.ts:240-241 `const idleMs = rec.idleMs ?? defaultIdleMs; if (idleMs <= 0) continue;`——record 级 0 = 永不进入收割判定。收割宿主在世也无济于事（本事故实证）。

附带发现（同链复核，D 面输入）：全局 env `LASSO_LAUNCH_IDLE_MS=0` 更强——`startChromeIdleReaper` 直接返 null（reaper 整体不启动），连 per-record `idleMs>0` 的记录也无人收割。

## 3. 定性

- **平台层缺天花板**：lasso 把「永不自收」做成了一个无界的单旗标语义。外部调研的四个成熟模式共同教训：**字段/请求无界时，平台层必须加顶**（ZooKeeper 把客户端 session timeout 夹进 [2×tick, 20×tick]；browserless 企业版全局 300s 顶在每会话 TIMEOUT 含 -1 之上；GKE Autopilot 对无上界 terminationGracePeriodSeconds 补 ~600s 平台上限——上游 API 无硬编码上限，discuss.kubernetes.io/t/32094）。同时**毁损级例外必须双重意图**（kubectl 强删须 `--grace-period=0 --force` 两旗并给；systemctl `--force --force` 字面重复；hdparm `--yes-i-know-what-i-am-doing`）。
- 与 BUG-03 的关系：A1 修的是**默认值**（0→30min），本决议修的是**上界形状**（0 的语义）。二者正交：A1 管「不写旗标的人」，本决议管「写了旗标/抄了配方的人」。
- 产品宪法判例：「静默服务前提 = 可发现可关闭」。visible/userTaken 档可发现（有窗口）可关闭（用户自关）；hidden 档可发现性靠 chrome-status/doctor、可关闭性靠 chrome-stop + idle 收割——`idleMs:0` 把唯一自动出口拆了，12h 幽灵 = 宪法违例。

## 4. 架构决议（A–E）

### 决议 A：硬顶天花板机制（本批核心）

**A-1 值与旋钮**

- 新单一真源常量 `LAUNCH_HARD_CAP_DEFAULT_MS = 86_400_000`（24h），落 **chrome-ledger.ts**（与 `CLI_LAUNCH_IDLE_DEFAULT_MS` 同位同理由：launch-chrome / index / enforcer / doctor 四方共用零环——chrome-ledger 只 import node:*，config.ts 反向 import 它无环）。
- 新 env **`LASSO_LAUNCH_HARD_CAP_MS`**（命名沿用 `LASSO_LAUNCH_IDLE_MS` 域前缀，刻意区别于渲染档——三套 idle 勿互抄的既训）：config.ts `parseLaunchHardCapMs`，未设 → 24h 默认；显式 `0` → 全局禁用硬顶（部署级策略开关）；**NaN/负数 → 回退默认**（防笔误静默拆掉安全网——失效方向必须偏「有顶」）。config.json 扁平层 key 同 env 名，自动继承既有合并机制。
- 默认值依据（外部惯例映射）：browserless 全局顶 = 其典型会话时长的 ~10×；GKE ~600s ≈ 典型 30s grace 的 20×。lasso 日常档合法长任务 = 小时级长会话抓取（30min CLI 默认的语义场景），24h = 默认值 48×、事故时长 12h 的 2×——**显著高于两个业界先例的倍率**，因红线把「不误杀合法长任务」权重置于「快收」之上（误杀代价 = 会话数据损失；不收代价 = 可观测 + 手动收）。

**A-2 语义（「不自收，但 N 硬顶」+ 双意图真无限）**

日常档记录的有效回收期限：

```
capApplies   = hardCapMs > 0 且 rec.launchMode !== "render" 且 rec.hardCapExempt !== true
effectiveMs  = !capApplies ? idleMs                    // 旧语义原样（0=无限）
             : (idleMs <= 0 ? hardCapMs                // 0 → 硬顶兜底
                           : min(idleMs, hardCapMs))   // ZooKeeper 式夹紧
```

- 计时基 = **lastUse 三源 max**（launchedAt / touchMap / touch 文件 mtime）——touch 活跃永不触发硬顶（合法长任务在用就一直活，红线机械化）。🔴 **r1 修正**：touchMap 是 reaper 实例私有的**进程内存态**，跨收割宿主不可见——「lasso browse 活跃」必须落盘 touch 文件才对执守宿主可见（**A-7**），否则本条红线机械化有跨进程盲区（否定轮 N1 实锤）。
- 真无限（旧 0 语义）需**双重意图**：CLI `--idle-ms 0 --no-hard-cap` 两旗并给（kubectl/systemd/hdparm 先例），或部署级 env `LASSO_LAUNCH_HARD_CAP_MS=0`。
- 收割出口不变：`stopFn({port})` → `stopLaunchedChromes` 验证杀路径（归属验证/树杀/删账 100% 复用；INV-78c 零新 kill 原语零违例）。日志 `chrome_idle_reaped` 增 `bound: "idle"|"hard_cap"` 与 `hard_cap_ms` 字段（审计可区分谁触发）。

**A-3 插入点裁决：收割判定处（chrome-idle-reaper.ts tick 循环），否决两个候选**

- ✅ **reaper tick（定案）**：单点判定、三宿主（server reaper / hide-enforcer 双职责 / 下述 cap-only 模式）一次全继承；visible/userTaken 豁免**结构性前置**（见 A-5）；读侧策略不改任何落盘数据。
- ❌ 启动时改写（recordLaunch 内夹紧 idleMs）：render-launcher 共用 recordLaunch——写入侧改值 = 渲染档交叉污染（红线）+ 台账失真（台账的天职是忠实记录请求，策略属执行层；ZooKeeper 也是服务端夹紧而非改写客户端请求）。
- ❌ 新独立守护/chrome-stop 扩权：违反「禁项目外新组件/禁新常驻守护」红线；且 chrome-stop 是显式出口不该自起。

**A-4 宿主存活（cap-only 模式）**：`startChromeIdleReaper` 返 null 条件从 `defaultIdleMs<=0 && !autoHide` 改为 `defaultIdleMs<=0 && !autoHide && hardCapMs<=0`——全局 env `LASSO_LAUNCH_IDLE_MS=0` 时 reaper 仍以「仅硬顶」模式运行（idleMs>0 记录照旧按 min(idleMs,cap) 收；idleMs<=0 记录按 cap 收）。**r1 修订：hardCapMs 一律由装配层显式传入（共享函数缺省 0，见 §6.3）**，装配点两处：① index.ts:496 server 装配传 `hardCapMs: config.launchHardCapMs`，启动条件 `config.launchIdleMs > 0 || config.autoHideAfterLogin` 追加 `|| config.launchHardCapMs > 0`（disabled 日志改 `chrome_idle_reaper_cap_only`）；② desired-hide-enforcer `startEnforcerIdleReaper` 包装层缺省 `opts.hardCapMs ?? LAUNCH_HARD_CAP_DEFAULT_MS`，且 index.ts hide-enforcer CLI 路由传 `enforcerCfg.launchHardCapMs` 经 `runHideEnforcerCli` 透传——**部署级 env `LASSO_LAUNCH_HARD_CAP_MS=0` 必须经此到达执守宿主**（只有包装层常量缺省会吞掉 env 覆盖；与既有 `defaultIdleMs: enforcerCfg.launchIdleMs` 同一传递通道）（其 readLedgerFn 已过滤日常档 hidden|headless|undefined→hidden，域正确）。**已知残余（诚实声明）**：若全局 idle=0 且 cap 开、且此刻无任何 lasso 进程在世（server 不在 + 执守已账空自退 + 无新 launch），则无宿主执行硬顶——退化为 doctor/chrome-status 可观测（决议 B）+ 下次任一 launch/server 启动补收。此残余与渲染档 r2 修的「guardian 死后不补拉」同形但代价低一个量级（不收 ≠ 误杀；失败方向安全），记入 §8 观察项，不为此加常驻组件（红线）。

**A-5 豁免继承（证明义务，机械化）**

- 结构证明：cap 分支文本位置在 tick 循环既有两道 `continue` 之后——`if (rec.launchMode === "visible") continue;`（P1 红线）与 `if (rec.userTakenAt !== undefined) continue;`（BUG-03 B1/A1 契约「唯一关闭出口 = 用户自己关或显式 chrome-stop」）。用户拥有记录**永不到达** cap 判定 → 豁免语义字节级不变。谓词锚 = chrome-ledger.ts `isUserOwnedRecord`（r2-F1 机械锚）。
- 渲染档隔离证明：cap 谓词含 `rec.launchMode !== "render"` 门；render guardian 的 readLedgerFn 本就过滤只剩 render 记录 → 该宿主 cap 恒 no-op；server reaper 无过滤但谓词门挡住 render。`LASSO_RENDER_IDLE_MS` 及 render-launcher/render-guardian/render-flags 三文件**零改动**（INV tripwire 钉死，见 §7-16）。

**A-6 记录字段（新增，加法式）**：`LaunchedChromeRecord.hardCapExempt?: true`——CLI `--no-hard-cap` 时由 runLaunchChromeCli 落盘；readLedgerSync typeof 守卫 `=== true` 才认（非法/缺省 → false → 有顶，失效方向偏「有界」）。旧版本 lasso 读新台账按既有前向兼容丢弃未知字段 → 降级为「有顶」，同样安全。

### 决议 A-7（r1 新增）：跨进程活动真源——onChromeUse 落盘 touch 文件

- **否定发现 N1（P1，复核实锤）**：硬顶红线「在用永不杀」的机械化存在跨进程盲区——server 的 browse 活动只写本进程 touchMap（index.ts onChromeUse 回调 → `chromeReaper.touch()` → `touchMap.set`），不落 touch 文件；而 touch 文件全仓写者仅 launch 时一次（launch-chrome.ts:691/722/767）与渲染档心跳（render-launcher.ts ×5）。执守宿主（本次事故实证在跑的收割宿主）装配的 reaper 无 touchPorts、touchMap 恒空，lastUse 只有 launchedAt + touch 文件两源——**看不见 server 侧任何 browse 活动**。实锤场景：旧配方（`--idle-ms 0`）+ 用户每天经 lasso browse_logged_in 使用但从不外部 touch——执守连续在世（台账非空不自退），24h 后照杀，尽管每天都在用。
- **修订**：index.ts LoggedInChannel onChromeUse 回调在既有 `chromeReaper?.touch(port)`（内存打点保留——进程内零延迟信号）之外追加 `void touchChromePort(port, log)` 落盘（fire-and-forget；touchChromePort 本身 best-effort 全内捕获永不抛，`.catch(() => {})` 仅防御性对称）。**不 gated on chromeReaper 非空**——server 侧 reaper 因 idle=0+cap=0 整体关闭时，browse 落盘仍对可能在世的执守宿主续命。
- **语义升级**：touch 文件从「外部第二消费者信号」升级为**唯一跨进程活动真源**（lasso browse / 外部消费者 / launch 自举 / 渲染档心跳四路同文件）。与渲染档 render-launcher 心跳 touch 完全同构（既有先例，非新形态）。
- **顺手封掉的既有缺陷（非本决议引入，同型同修，显式登记）**：idleMs=30min 记录 + 活跃 browse + 执守宿主在世 → 执守 30min 即杀（touchMap 跨进程不可见，server 宿主救不了先到者）——本修订前即为生产可复现盲区，此一行同修。
- **形态保持（R-INT-07）**：reaper 仍是纯读方（touchMap 读 + touch 文件 stat）；写方在 channel 层（LoggedInChannel 回调）——与 touchMap 既有「reaper 读 / channel 写」单写多读形态同构。备选（否决）：把落盘放进 `ChromeIdleReaper.touch()` 内——生产覆盖面等价（唯一生产调用方即本回调），但混淆 reaper 读方纯度、令执守宿主实例成为潜在写方；回调层写概念足迹更小。
- **消费方契约零破坏**：外部消费者照旧 shell `touch` 同一文件（文件名/目录契约不变）；写入频率 = 每次 browse 一次 mkdir-recursive + utimes（best-effort 系统调用级，与渲染档心跳频度同量级）。决议 E changelog 中「lasso browse 续命」一句**因本修订才为真**（r1 前该句对执守宿主形态不成立）。

### 决议 B：超龄可观测（不加 kill 面）

- **chrome-status**：`evidence.ledger_record` 增 `idleMs`；命中条件（台账在案 + 非用户拥有 + 非 render + 有效期限=cap 且 lastUse 龄 > 50% cap）时附 **`hard_cap_watch`** 咨询块（cap_ms / idle_for_ms / ratio）+ `user_paste_pack` 增一行（预期回收时刻、touch 续命法、用户本人 `chrome-stop --port N` 出口）。**classification 不变（仍 lasso_live）、AGENT_DIRECTIVES 不变（allowed_commands 恒空）**——B 面零新增 kill/指令面（INV-88 契约不破）。
- **doctor**：`nextStepTextForClassification` 的 `lasso_live` 分支在同一数据在场时追加同一行提示（chrome-status 单一真源，doctor 纯渲染——既有 A4 分工不变）。
- 12h 事故在 24h cap 下的新行为 = **12h 起持续 warn、24h 自动收**——「可发现可关闭」宪法闭环。

### 决议 C：配方治理（文档/会话配方修订 + 消费方通知）

| 面 | 现状 | 修订 |
|---|---|---|
| `doc/usage/02-TROUBLESHOOTING.md:397` | `--port 9223 --idle-ms 0` 标注「推荐」 | 改推 `--idle-ms 1800000`（bug02 §9.1 既有 session 配方）或 touch 契约；`--idle-ms 0` 改述为「不自收、但 24h 硬顶；真无限须 `--idle-ms 0 --no-hard-cap`」 |
| `doc/usage/01-KEY-GUIDE.md:169` | 同款教学 | 同步 |
| `README.md:331/342` + `README.en.md:311/322` | 「长会话显式 `--idle-ms 0` 永不自动收」 | 同步新语义 + 版本史 v1.24.0 显著位（决议 E） |
| `CLI_USAGE`（index.ts，launch-chrome --help） | "explicit --idle-ms 0 = never reap" | 改述 + 新增 `--no-hard-cap` 帮助行 |
| 历史文档（doc/bugs/02、03、渲染档设计决议:191、需求、对接说明） | 记载旧语义 | **不重写历史**——各加一行带日期的勘误后记指向本文件 |

**消费方通知要点**（cc-control + media-gen-mcp，用户=两侧 owner）：① flow 的 `--idle-ms 0` 配方**无需改代码**（分钟级会话远低于 24h，且下次 ensure 会自动重拉）；② 其文档 `doc/flow-api-contract.md` 的启动命令旁注一句新边界；③ 若未来出现真·无限常驻需求 → `--idle-ms 0 --no-hard-cap` 或 `LASSO_LAUNCH_HARD_CAP_MS=0`。

### 决议 D：同类面一揽子裁决（白盒清单）

| # | 面 | 今日 0 语义 | 裁决 | 理由 |
|---|---|---|---|---|
| D1 | record 级 `--idle-ms 0`（日常档 hidden/headless/undefined） | 永不收（本事故） | **硬顶** | 幽灵形态全要素：detached + 台账跨进程 + 无窗口 + 执守压窗 |
| D2 | 全局 env `LASSO_LAUNCH_IDLE_MS=0` | reaper 整体不启（per-record idleMs>0 也无人收） | **硬顶（cap-only 模式）** | 同域同风险；配方事故向量等价（env 也进 nohup 配方） |
| D3 | 渲染档 `LASSO_RENDER_IDLE_MS=0` | guardian 不拉 + record 永不收 | **本轮豁免** | 红线明令隔离；渲染档有 ensure 收尸重拉/heartbeat touch/profile 清理的独立生命周期；消费方 golden 依赖现状。对称缺口登记观察（§8），如需加顶另立渲染档专属决议（独立旋钮，勿共用） |
| D4 | `LASSO_HEADLESS_IDLE_MS=0`（MCP shim 僵尸 reaper） | zombie reaper 不启动（index.ts:421 `>0` 门） | **本轮豁免** | 不同数据域（server procs 树，非 detached 台账 Chrome）；无「不可见+执守压窗」组合；文档已明示「opt-out 即自负残留」（parse17）。仅登记 |
| D5 | 消费方 `MEDIA_GEN_BROWSER_IDLE_MS` | 已随 legacy 退役 | N/A | — |

**判据原则**：硬顶只落在「detached + 磁盘台账 + 跨进程所有权 + 不可见窗口」四要素齐备的面（= 幽灵形态可达）；进程内定时器域与渲染档不碰。

### 决议 E：changelog breaking 口径

v1.24.0 版本史**显著位**（🔴 前缀，与 v1.21.0 默认值翻面同级排版）：

> 🔴 **行为变更（BUG-06）**：`--idle-ms 0` / `LASSO_LAUNCH_IDLE_MS=0` 语义从「永不回收」改为「**无活动 24 小时硬顶回收**」（自最近活动起算——lasso browse（自动写 touch 文件，决议 A-7）或外部 `touch ~/.cache/lasso/chrome-touch-<端口>` 续命，在用永不触发）。真·无限常驻 = `--idle-ms 0 --no-hard-cap` 双旗，或部署级 `LASSO_LAUNCH_HARD_CAP_MS=0`。visible/用户认领（chrome-show）档豁免不变；渲染档零变化。**升级瞬间存量超龄 idle-0 记录将被首个收割 tick（≤15s）回收**——属预期（该类实例即本事故幽灵形态）；依赖 0=无限且不 touch 的 nohup 配方请迁移（见 doc/bugs/06 决议 C）。

## 5. 向后兼容分析

- `idleMs>0` 且 ≤cap 的记录：行为零变化（effectiveMs=idleMs）——存量绝大多数路径不受影响。
- `idleMs>cap`（如显式配 7 天）：改为 cap 夹紧回收——理论 breaking，但「>24h 的单次 idle 阈值」无任何仓内文档/配方/测试倡导，changelog 一并覆盖。
- 台账 schema：加法式可选字段 + typeof 守卫，旧读新丢弃（降级有顶，安全）、新读旧缺省（cap 生效，即修复本体）。
- CLI：加法式旗标 `--no-hard-cap`；未知旗标忽略的 forward-compat 既有语义不破。
- 渲染档/消费方 media-gen：零代码改动（D3 + C 通知）。
- reaper 既有注入面（readLedgerFn/nowFn/stopFn/touchStatFn）：`hardCapMs` 为新可选项，**缺省 = 0（关）——r1 反转原案（原缺省 24h 会令 render guardian 装配（不传 cap）从「idle opt-out 立即自退」变为「render 记录在册期间滞留的空转进程」，打红 render-guardian.spec:170-171 既有 null 钉子、令 INV-94② 使文件内补救（传 hardCapMs:0）亦违规、且使 changelog E「渲染档零变化」为假——三代价均高于反转缺省，判原案为设计缺陷非两可）**。共享函数档位无关（同 A-3「策略在装配、机制在共享」否决 recordLaunch 改写的既训），24h 缺省由两个日常档装配层各自拥有（config.parseLaunchHardCapMs 未设→24h + startEnforcerIdleReaper 包装缺省→常量）——「失效方向偏有顶」在装配层保住，INV-94 ⑥ 钉死两装配点必传。既有 null/禁用类单测（chrome-idle-reaper.spec test 5、render-guardian.spec null 块）**零改动**；cap 类新测试一律显式传 hardCapMs（缺省形态=旧语义）。
- touch 文件写入频率（r1/A-7）：每次 browse 一次 best-effort 落盘（mkdir-recursive + utimes 系统调用级）；外部消费者契约不变（照旧 shell `touch` 同一文件）。

## 6. 实施面（文件级）

1. `src/launcher/chrome-ledger.ts`：`LAUNCH_HARD_CAP_DEFAULT_MS` 常量 + record 类型 `hardCapExempt?: true` + readLedgerSync 守卫解析。（消费方 r1 修订：config.ts parse 缺省 / desired-hide-enforcer 包装缺省 / doctor·chrome-status watch 折算——chrome-idle-reaper **不再**消费该常量；放置理由仍成立：config.ts 反向 import 无环。）
2. `src/config/config.ts`：`parseLaunchHardCapMs`（NaN/负→默认；0→0）+ config 字段 `launchHardCapMs`。
3. `src/launcher/chrome-idle-reaper.ts`：opts 增 `hardCapMs?: number`（**缺省 = 0（关），r1 反转**——共享函数档位无关、不携带日常档策略常量，无需 import 常量）；tick 循环 cap 谓词（visible/userTaken 两道 continue 之后、render 门、hardCapExempt 门）；lastUse 计算上移到 cap 分支之前；`bound`/`hard_cap_ms` 日志字段；null 条件改三条件（`defaultIdleMs<=0 && !autoHide && hardCapMs<=0`——缺省形态与既有 null 行为字节级一致，render guardian 不传即旧行为）。
4. `src/launcher/launch-chrome.ts`：`parseLaunchChromeArgs` 增 `--no-hard-cap`（无参布尔旗）；`LaunchChromeOptions.hardCapExempt`；runLaunchChromeCli 记账透传。
5. `src/launcher/desired-hide-enforcer.ts`：`startEnforcerIdleReaper` opts 增 hardCapMs（**包装层缺省 `?? LAUNCH_HARD_CAP_DEFAULT_MS`，r1**）；`runHideEnforcerCli` opts 透传 hardCapMs；null 语义同步。
6. `src/index.ts`：server reaper 装配传 `hardCapMs: config.launchHardCapMs` + 启动条件追加 `|| config.launchHardCapMs > 0` + cap-only 日志分支；hide-enforcer 路由传 `hardCapMs: enforcerCfg.launchHardCapMs`（部署级 env 覆盖必达执守宿主）；**onChromeUse 回调追加 `void touchChromePort(config.cdpPort, ...)` 落盘 + import（A-7）**；CLI_USAGE 文本；launch-chrome 路由无改（hardCapExempt 走 opts）。
7. `src/doctor/chrome-status.ts`：evidence.ledger_record.idleMs + `hard_cap_watch` 咨询块 + paste 行（classification/指令面零改动）。
8. `src/doctor/doctor.ts`：`nextStepTextForClassification` lasso_live 分支追加行。
9. 文档面（决议 C 全表）+ CHANGELOG（README 版本史，决议 E）。
10. `src/invariants/check-invariants.mjs`：INV-94（见 §7-16）。

## 7. 测试面（改行为必配测试；`npm run gate` 读汇总行）

chrome-idle-reaper.spec.ts 增补（编号接续 16 起；**r1：共享层 hardCapMs 缺省=0（关），下列 cap 类用例一律显式传 hardCapMs，缺省形态=旧语义=既有测试零改**）：
1. idleMs=0 + lastUse 龄 > hardCapMs → stopFn 调用且日志 `bound:"hard_cap"`；
2. idleMs=0 + 龄 < cap → 不收；
3. idleMs=0 + 龄 > cap 但 touch 新鲜 → 不收（cap 基=lastUse 三源 max——touch 活跃永不杀的红线钉子）；
4. idleMs=7d > cap=24h → 按 cap 收（min 夹紧）+ bound=hard_cap；
5. idleMs=30min + cap=24h → 按 30min 收 + bound=idle（零回归锚）；
6. hardCapMs=0（opt-out）+ idleMs=0 → 永不收（旧语义钉死；=缺省形态，显式钉）；
7. hardCapExempt=true + idleMs=0 + 龄≫cap → 永不收（双意图钉死）；
8. visible + idleMs=0 + 龄≫cap → 永不收（豁免继承）；
9. userTakenAt 已落 + idleMs=0 + 龄≫cap → 永不收（豁免继承）；
10. launchMode="render" + idleMs=0 + 龄≫cap → 永不收（渲染档交叉污染零钉死；guardian 域与 server 域双跑）；
11. cap-only：defaultIdleMs=0 + hardCapMs>0 → reaper 非 null 且收 idle0 超龄记录；exitWhenLedgerEmptyTicks 自退仍工作；
12. 双关：defaultIdleMs=0 + hardCapMs=0 → null（旧 disabled 钉死）+ chrome_idle_reaper_disabled 日志。
13. **跨进程真源钉（r1/A-7，否定轮 N1）**：startEnforcerIdleReaper 形态（执守宿主——touchMap 恒空）+ idleMs=0 + 龄>cap + touchStatFn 返回新鲜 mtime → 不收（server 侧 browse 落盘后对执守宿主可见的形态钉；同时钉 wrapper 透传 touchStatFn 与 hardCapMs 缺省接线）；touchStatFn 陈旧 → 收（接线活性对照）。
- config-file.spec.ts：`LASSO_LAUNCH_HARD_CAP_MS` unset→24h / "0"→0 / "abc"、"-5"→24h（防笔误拆网）。
- launch-chrome.spec.ts：`--no-hard-cap` 解析 + 记账 roundtrip；chrome-ledger.spec.ts：hardCapExempt 写读往返 + 非法降级。
- bug06-chrome-status.spec.ts：idle0 超龄（>50% cap）→ hard_cap_watch 在场、classification 仍 lasso_live、allowed_commands 空（无新 kill 面）；未超龄/用户拥有/render → 无 watch。
- 16. INV-94 `launch-hard-cap-contract`（源码断言，89→90 组）：① chrome-idle-reaper tick 内 cap 谓词位于 visible/userTaken 两道 continue 之后且含 render 门；② render 三文件（render-launcher/render-guardian/render-flags）零 hard-cap 符号（交叉污染 tripwire）；③ CLI_USAGE 含 `--no-hard-cap`；④ isUserOwnedRecord 仍被 cap 路径前置短路；⑤ index.ts LoggedInChannel onChromeUse 回调含 `touchChromePort` 落盘符号（**r1/A-7 写侧机械钉**——browse 活动跨进程可见性的写侧锚，读侧由测试 3/13 钉）；⑥ 两个日常档装配点显式传 hardCapMs（index.ts server 装配 `config.launchHardCapMs` + startEnforcerIdleReaper 包装/hide-enforcer 路由——**r1 反转缺省后的失顶防护钉**：防未来装配点漏传静默回到无顶形态）。
- 真机验收（用后清理）：`LASSO_LAUNCH_IDLE_MS=0` + 降 cap env 起 hidden 实例 → 观察 cap-only 收割 + doctor warn 行；`--idle-ms 0 --no-hard-cap` → 存活；visible 档 → 存活。

## 8. 已知残余与观察项

1. **无宿主窗口**（A-4）：全局 idle=0 + 无任何 lasso 进程在世时硬顶无人执行——退化路径=可观测（B）+ 下次启动补收；不为此加常驻组件（红线）。观察：若实战再现「超龄幽灵且无宿主」，再议 launch 路径补拉 cap-only 执守（render r2 同款 ensure 补拉）。
2. **无 touch 的 0-依赖消费者**：升级后 24h 无信号即收——changelog 显著位 + 消费方通知（C）承担迁移；lasso 无廉价手段区分「CDP 连着但没 touch」（/json 不列客户端），诚实接受。（r1 收窄：lasso 自身 browse 经 A-7 已是一等续命信号——残余仅限**外部** CDP 消费者无 touch 场景。）
3. 渲染档对称缺口（D3）：`LASSO_RENDER_IDLE_MS=0` 仍真无限——红线令本轮不动；登记待渲染档独立决议。

## 9. 关联

- doc/bugs/02（idle reaper 第二消费者，`--idle-ms 0` 缓解首创——本决议将其「推荐」地位正式退役）
- doc/bugs/03 §4 A1（CLI 默认 0→30min——修默认值不修上界的补全）
- doc/bugs/04（chrome-status 归属鉴定——本决议 B 面复用其分类器与 paste 通道）
- doc/渲染档设计决议（三套 idle 勿互抄——`LASSO_LAUNCH_HARD_CAP_MS` 命名遵循）
- 外部先例：ZooKeeper maxSessionTimeout 夹紧 / browserless 全局 TIMEOUT 顶 / GKE Autopilot 平台上限（discuss.kubernetes.io/t/32094）/ kubectl `--grace-period=0 --force`、systemd `--force --force`、hdparm 双意图旗标

## 10. 对抗复审修订史（r1，2026-09-10）

否定轮两项 P1 发现，设计修订员独立取证逐条复核（锚点全部源码验证，无凭引文采信）：

| # | 否定发现 | 复核结论（取证锚点） | 修订落点 |
|---|---|---|---|
| N1 | 硬顶「touch 活跃永不杀」红线机械化跨进程盲区：browse 活动只写 server 进程 touchMap 不落盘，执守收割宿主不可见，24h 照杀；次级既有缺陷 idleMs>0 双宿主同型（执守 30min 即杀活跃记录） | **成立**：index.ts onChromeUse 回调仅 `chromeReaper?.touch()`（touchMap.set，进程内存）；touchChromePort 全仓写者 = launch-chrome.ts:691/722/767（launch 一次）+ render-launcher.ts ×5（心跳），browse 路径零写者；startEnforcerIdleReaper（desired-hide-enforcer.ts:294-311）不传 touchPorts 且执守进程无 LoggedInChannel → touchMap 恒空；chrome-idle-reaper.ts:244-248 lastUse 三源 max 为 per-host 视图；双宿主并存为常态（执守台账非空不自退 + server 承载 browse，server reaper 无让位——仅 watchdog 让位 PERF-2a） | 新增 **A-7**（onChromeUse 追加 `void touchChromePort` 落盘，touch 文件升级为唯一跨进程活动真源；顺手封既有双宿主盲区）；§6.6 实施项；**§7-13** 测试；INV-94 **⑤**；§5 频度说明；§8-2 残余收窄；决议 E「lasso browse 续命」句因之为真 |
| N2 | §6.3 原案共享 reaper hardCapMs 缺省=24h + A-4 三条件 null：render guardian 装配不传 cap → cap-only 非 null → 「idle opt-out 立即自退」变「render 记录在册期间滞留空转」+ render-guardian.spec:170-171 必红 + INV-94② 使文件内补救（传 hardCapMs:0）亦违规 + changelog E「渲染档零变化」为假 | **成立**：render-guardian.ts:253-267 装配调 startChromeIdleReaper 不传 hardCapMs；:318-321 `if (!g) exit(0)` 依赖 null 语义（注释「idle opt-out…立即退」）；render-guardian.spec.ts:169-172 既有钉子 `assembleRenderGuardian({defaultIdleMs:0/-1})` 断言 toBeNull()；chrome-idle-reaper.spec test 5 同款 null 断言；startChromeIdleReaper 生产调用方恰 3 处（server/wrapper/render——反转缺省不漏宿主） | §6.3 缺省**反转为 0（关）**，24h 下沉两日常档装配层（config.parseLaunchHardCapMs 未设→24h；startEnforcerIdleReaper 包装 `?? LAUNCH_HARD_CAP_DEFAULT_MS`；index.ts 两路由传 config 值——部署级 env 覆盖必达执守宿主）；A-2/A-4/§5/§7 同步；INV-94 **⑥** 失顶防护钉。效果：render 三文件零改动、既有 null 钉子全免改、「渲染档零变化」承诺为真 |

**误报驳回：无**（两项均实锤成立并采纳）。备选方案否决记录：N2 若坚持原案（共享缺省 24h）须同时放弃 A-5「render 三文件零改动」表述、重写 INV-94② 豁免条款、修订 changelog E——三代价均高于反转缺省，判原案为设计缺陷非两可。N1 备选（落盘进 `ChromeIdleReaper.touch()`）因混淆 reaper 读方纯度否决（A-7 形态保持条）。
