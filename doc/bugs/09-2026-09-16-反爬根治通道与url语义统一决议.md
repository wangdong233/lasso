# doc/bugs/09 — 反爬根治通道 + url 语义统一 + P2 批决议（2026-09-16）

> 输入：喵虎核验报告（2026-09-16）+ 白盒五项核查 + SOTA 调研（2025-2026 检测面×对抗矩阵）+ 简单架构清单 38 条全量。设计合成员对全部 findings 自行复核后裁决。
> 基线：v1.26.0，HEAD=d6e79f8，工作树净，182 files / 2997+1 skipped / 96 INV。
> 消费方核心诉求原话：「项目遇到强力的反爬机制就挂了完全没有办法了，这种情况不能根治吗」。

## r1 修订记录（2026-09-16，设计修订员对否定发现逐条复核后落地）

设计修订员对三条否定发现做了独立白盒复核（非转述——全部行号/机制重走了一遍）：

| # | 否定发现 | 复核裁决 | 关键复核证据 |
|---|---|---|---|
| N1 (P0) | HeadedChannel 生命周期防线建立在错误收割器域 | **成立，正文已重写（A.4⑤r1）** | ① SubprocessManager.cleanupZombies 只认 lastUsedAt（:266-269），lastUsedAt 仅 ensureRunning(:210)/touch(:306)/spawn 写——用户在窗口打字零刷新；② visible/userTakenAt 豁免是 chrome-idle-reaper.ts:281-291 的谓词，两域分工注释明写（:14-17「zombie reaper 管 procs / chrome reaper 管 ledger」）；③ userTakenAt 全库仅两条写径（chrome-ledger.ts:291-294 注释锚：B1 确认窗 + chrome-show），台账里没有 HeadedChannel 记录可落；④ 上游 1.7.0 `pipe:true`（browser.js:172）——CDP 走 pipe 无 TCP 端口，chrome_status 按 port 的归属鉴定结构性不可达；⑤ LASSO_HEADLESS_IDLE_MS 默认 **5min**（config.ts:74/120），原决议「对齐默认 30min」引错域错数（30min 是 ledger 域 LASSO_LAUNCH_IDLE_MS）；⑥ Chrome 窗口 title 由页面 `<title>` 控制，CLI 无法外部前缀——「title 前缀标记」不可行，已删 |
| N2 (P1) | L2 实效前提证据错配 + 唯一验证排全部实现之后 | **成立，已修（A.1r1 措辞降级 + 新增 WT0 前置 spike）** | D-δ 证据对象=用户日常 Chrome（真实 profile/cookie/历史）；HeadedChannel=fresh profile 零历史——NVC 服务端评分重 cookie/行为史，fresh 有头只证「消无头环境面交集信号」（D-β），不证「过服务端评分」。03 §0.4:50「比较性断言无外部锚=自我参照退化」+ §0.5.8「deferred-Spike=未验证须 ship 前补 L3」。原排序把实效验证放 WT4（合并序最末）而 INV-97 在 WT2 先固化——失败即全链返工。反查正面核实成立：上游 MCP 入口 headless default=false（chrome-devtools-mcp-cli-options.js:86）+ --isolated 独立可用 + --ignoreDefaultChromeArg 存在 + darwin detectDisplay（browser.js:162-163）——机制面 L2 可行，风险集中在实效 |
| N3 (P1) | 免解锁理由用了成本维度而非静默守则打扰维度；hint 无用户同意指令 | **可见部分成立；解锁门建议定向驳回（见 A.4⑥r1 论证）** | 静默守则 S1 判据=零打扰路径存在性（doc/governance/09 audit.md:18），A.4⑥ 原理由「本地资源、无付费、无 key」确是成本维度，不构成对 S2 介入面的审视豁免；A.2 hint「retry with browse_headed」无 consent 指令，与 A.6.1 拒绝 autoEscalate（最小惊讶）形成张力——agent 直hint 重试=把系统层拒绝掉的惊讶面挪到 agent 层重演。**驳回「加 INV-25 式解锁门」**：本机工具族（browse_logged_in 先例）全默认注册；INV-25 双重解锁语义是「cloud 付费资源不用时零存在感」（audit.md:146），browse_headed 的打扰形态是「调用即打扰」而非「存在即打扰」——每次调用的 consent 契约（description+hint 双钉）比部署级开关介入面更小且不弱化（S2：介入只在不可避免处）。若实施后发现 agent 无视 consent 语言再升解锁门（回写本文件） |

r1 变更面：A.1 路线②措辞、A.2 hint、A.4②⑤⑥、A.7 测试面（INV-97 断言更新+新增 INV-99）、决议 D（新增 WT0、WT2 文件域+config 弱冲突、WT4 验收拆分、合并序）、开放项 2 改 WT0 关闭、§6 gate 补 breaking 缓解清单。

---

## 0. 证据基线（设计合成员自行复核表，03 §0.3 L1 锚）

| # | 输入断言 | 复核 | 锚 |
|---|---|---|---|
| 1 | CURRENT_PAGE_ACTIONS 仅 {screenshot,wait} | ✅ | `src/channels/BrowseChannel.ts:2061` |
| 2 | descriptions 声称 evaluate「omit url to run on the current page」与代码拒绝矛盾 | ✅ | `src/tools/descriptions.ts` evaluate 段 |
| 3 | url 三分裂实锤：NAV_FIRST={network,screenshot,pdf} 无条件先导航 / FRESH_PAGE_NAV={snapshot,extract} 空白门控 / evaluate=ensure-nav | ✅ | BrowseChannel.ts 同文件三 Set + `wrapEvaluateEnsureNav`（:183 dispatch / :247 定义） |
| 4 | doWait 只认 expect.text vs ExpectPoll 认三键 | ✅ | BrowseChannel.ts:1758 / ExpectPoll.ts:121-123 |
| 5 | worked+error 并存写点 | ✅ | StepEngine.ts:262-266（verified→worked 覆写，error 未清） |
| 6 | SteelChannel 平级先例（extends BrowseChannel + 装配四处 + INV-74） | ✅ | SteelChannel.ts:162 + index.ts 装配 |
| 7 | stealth 12 evasions vendored | ✅ | `src/browse/stealth-evasions/` 12 文件 |
| 8 | 最新 INV-96，新增自 INV-97 起 | ✅ | check-invariants.mjs |

**设计合成员增量发现（输入未列，裁决依据）**：

- **D-α**：HeadlessChannel 已有完整 stealth 栈（StealthEngine injectProfile + UA/viewport launch 级一致 + `--chromeArg=--disable-blink-features=AutomationControlled` + freshProfile 换脸，INV-79d 断言）——**tm.aliyun.com 依然全灭**。⇒ 检测层在 stealth 栈覆盖面之外（环境/行为/服务端评分融合），这是路线①裁决的决定性反证：抹平更多 JS 指纹不必然通过 NVC 评分。
- **D-β**：Playwright MCP（CDP 泄漏面与 puppeteer 不同）同死法 ⇒ 检测信号在两者**交集**（无头模式本身的环境差异），而非各自特有的 CDP 泄漏。rebrowser/patchright 只抹 CDP 泄漏面，对本类站点实效**无公开可信证据**。
- **D-γ**：SOTA 决定性实证——curl 无 JS 环境 GET tm.aliyun.com → HTTP 200 完整页面；驱逐 = 页面 NVC SDK 采集 → 服务端评分 → 前端 2-3s 后执行重定向。⇒ ①服务端不拦 IP/UA 层（fetch_url 本就可用）②**驱逐窗口真实存在且 DOM 完整可读**（喵虎报告时间线 8：navigate→extract 独立调用抢读成功）。
- **D-δ**：用户有头 Chrome「四步法」在 tm 站实证成功（喵虎报告附录）⇒ 有头 vs 无头的检测鸿沟实证成立。
- **D-ε**：FallbackDecider.ts:339-341 **已有注释记载** fallback 污染问题（headless evaluate 超时被 logged_in 连接错误串染）——P2-B/C3 不是新问题，是已登记债务。
- **D-ζ**：`navSeenClients`/`lastNavigatedClient` 会话导航追踪基建已存在（BrowseChannel.ts:526-530，单写者）——驱逐哨兵有现成挂点。

---

## 1. 决议 A：反爬根治通道——**路线④ 分层防御梯（L0/L1/L2）+ 驱逐哨兵**

### A.0 目标 / 非目标

**目标**：对「JS 层重定向驱逐」类站点（tm.aliyun.com 形态），把消费方状态从「完全没办法」（context destroyed 深埋错误串、无差别重试）变为「三层可退 + 死因可观测」。
**非目标**：不承诺通过一切反爬（服务端 IP 拦截、验证码墙不在本案）；不自动化对抗（无行为模拟、无代理轮换——后者属新组件红线区）。

### A.1 四路线裁决矩阵

| 路线 | 对驱逐类站点实效 | 成本 | 简单架构（02） | 红线 | 裁决 |
|---|---|---|---|---|---|
| ① stealth 强化（rebrowser/patchright CDP 泄漏 patch） | **不确定**：D-α/D-β 双反证（stealth 栈已在仍灭；Playwright 异泄漏面同死 ⇒ 检测在无头环境面交集） | 高：patch 上游依赖树，每次 LOCKED 升级重打 | ❌ 版本锁变 patch 锁，R-CHG-01 变更放大 | 上游只读边缘（npm overrides 改 chrome-devtools-mcp 依赖树） | **REJECTED 本期**（条件重启见 A.6） |
| ② 有头通道 | **机制面成立，实效面假设有效待 L3（r1 措辞降级）**：D-β 交集论证⇒有头消除「无头环境面」检测信号；D-δ 证明有头可过，但其证据对象=用户日常 Chrome（真实 profile/cookie/历史），HeadedChannel=fresh profile——服务端评分维度（cookie/行为史）未被 D-δ 覆盖。**WT0 前置 spike 定生死（决议 D r1）** | 中：SteelChannel 同配方新 Channel | ✅ R-CI-07 有仓库先例归属 | 需延伸 visible 档红线（A.4⑤r1 两态生命周期） | **采纳为 L2（以 WT0 spike 通过为生效前提）** |
| ③ 混合 ①+② | ② 的实证 + ① 的不确定收益 | 高+高 | ❌ | ❌ | REJECTED |
| ④ 分层防御梯 + 驱逐哨兵 | L1 覆盖快照需求（**零新组件**）+ L2 覆盖驻留 JS 需求 + 死因信号化 | 中 | ✅ 每层独立可单独放弃（satisficing，03 §0.1.7） | ✅ | **采纳**（本案） |

**「业界实践即最佳则直接参考」条款的执行**：商业反爬服务（Steel/Browserless/Bright Data 类）对重度站点的事实标准答案 = 有头浏览器/真实环境——**L2 与业界一致，直接采纳并在此说明**。lasso 的差异化（业界未做的 1+1>2）：把「何时需要升级通道」从用户配置文档里的经验知识，变成**返回体内可机械判读的结构化信号**（驱逐哨兵 + hint），配合四通道并行架构实现显式降级链。

### A.2 防御梯设计

```
L0（已有，保持字节级不变）：browse_headless + stealth 栈 + freshProfile
    覆盖：指纹层检测（假 0 形态）。tm 类驱逐站：不覆盖（D-α）。
L1（本次新增，零新组件）：进程内原子抢读 = 决议 B 的直接副产品
    extract/evaluate/snapshot + url≠当前页 → 单次调用内「导航→load→零延迟快照」，
    无 MCP 往返间隔。把喵虎时间线 8 的用户 workaround（navigate→extract 抢读成功）
    机制化。覆盖：JS 层延迟驱逐形态的快照需求。
    诚实边界：驱逐若提前到 DOMContentLoaded 前或改服务端 302 则无效（D-γ 证实
    当前形态是 load 后 2-3s，窗口真实存在；服务端拦截形态 fetch_url 本就通过）。
L2（本次新增）：browse_headed 有头强力档（A.4）
    覆盖：无头检测站点的驻留 JS 需求（驱动搜索框+轮询结果——喵虎场景本体）。
```

**驱逐信号（升级提示）语义**：哨兵检出驱逐后，后续调用返回体附 `page_evicted` + `hint: "site evicts headless automation; snapshot may still work via L1 atomic read; for JS residency ASK THE USER FIRST, then retry with browse_headed (opens a real on-screen window)"`（r1：consent 指令入 hint——N3 复核确认原文案会把 A.6.1 拒掉的「未经同意弹真实窗口」挪到 agent 层重演）。**不自动升级**（与决议 C3 同哲学：通道选择是用户语义，系统只供信号——弹真实窗口的惊讶面必须经用户同意，无论触发方是系统还是 agent）。

### A.3（并入 A.2/A.4/A.5）

### A.4 HeadedChannel 规格（L2）

1. **落位**：`src/channels/HeadedChannel.ts`，`extends BrowseChannel` 平级兄弟（SteelChannel 先例配方：R-CI-02 守、装配四处 `channelToSpec` / 工具注册 / `initialCapabilities` / `longBreakers`、INV 新增）。
2. **spawn（r1 修订）**：`chrome-devtools-mcp@1.7.0`（同一 `LOCKED_CDP_MCP_VERSION`，**不带 `--headless`**）+ 显式 `--user-data-dir=<~/.cache/lasso/headed/<run_id>>`（**替代裸 `--isolated`**：上游 browser.js:138-144 不传 isolated 且不传 userDataDir 时会落到 `~/.cache/chrome-devtools-mcp/chrome-profile` 持久共享 profile——必须显式传 lasso-owned 路径；该路径同时是 ps 级归属锚 + reap hook post-kill rmSync 清理点，headless freshProfile 同款先例）+ `--no-usage-statistics`（INV-79b 四通道族扩展为五通道）。**零新二进制、零新依赖、同一上游不同 flag 组合 ⇒ 不触「新组件」红线。** CDP 走 pipe（上游 browser.js:172 `pipe:true`）——无 TCP 端口暴露（攻击面更小，但见⑤r1：台账/chrome_status 按 port 的机制对 headed 不可达）。
3. **stealth 策略**：**不接 StealthEngine 指纹注入**（真实有头环境即真实指纹；注入痕迹本身是信号——与 HeadlessChannel 相反的取舍，注释写明理由防后人「顺手对齐」）。仅保留 `--chromeArg=--disable-blink-features=AutomationControlled`（抹 navigator.webdriver）。实现期 audit 一遍 puppeteer 默认 flags（`--enable-automation` infobar 等）并在 spec 显式排除（上游 `--ignoreDefaultChromeArg` 已核实存在，WT0/WT2 期用）。
4. **窗口策略**：`--window-position` 屏幕边缘最小可见（macOS 完全屏幕外窗口会被系统判 occluded → `visibilityState:hidden` + rAF 节流，可能反成行为评分负信号）。**L3 真机验证项**（开放项 1/3）：边缘露出 vs 屏幕外全藏的 visibilityState 差异 + tm 站实效对照。
5. **生命周期与用户接管防线（r1 全文重写——N1 复核证实原案建立在错误收割器域上）**：
   - **域事实（先钉死，防后人再引错域）**：HeadedChannel 是 SubprocessManager procs 域成员，**不落 chrome-ledger**。visible/userTakenAt 豁免是 chrome-idle-reaper 的谓词（chrome-idle-reaper.ts:281-291），两域分工是结构性的（:14-17）；userTakenAt 全库仅两条写径（B1 确认窗/chrome-show，chrome-ledger.ts:291-294）——对 procs 域不可达。CDP 走 pipe 无 TCP 端口 ⇒ chrome_status 按 port 的归属鉴定同样不可达。**原案「SubprocessManager 统一注册（同 headless）+ 复用 chrome_status/user_taken_asset 语义」三处机制引用全部落空；「对齐默认 30min」引错域（headless 域实际默认 5min，config.ts:74——照原案实现，用户接管中的窗口 5 分钟无 lasso 调用即被 cleanupZombies 树杀，doc/bugs/03 F4 同型事故重演）。**
   - **两态生命周期（替换原「对齐」一句）**：
     - **态一·未接管**：idle 收割生效，独立阈值 `LASSO_HEADED_IDLE_MS`（config 新键，默认 **30min**）。依据：有头窗口弹出是屏幕上的显式可见事件，回收窗口须宽于 headless 域 5min 以覆盖「用户正走向电脑」竞态；数值对齐 ledger 域 `LASSO_LAUNCH_IDLE_MS` 默认而非 headless 域。
     - **态二·已接管（粘滞，永不回落）**：**接管探测器**——经 pipe 会话 30s 周期 evaluate `document.hasFocus()`（窗口获用户焦点 ⇒ 真实用户在场交互），命中即标记 session `userTaken` 粘滞 ⇒ **永不 idle 收割**；BUG-06 式硬顶兜底（`LASSO_HEADED_HARD_CAP_MS` 默认 24h，防孤儿舰队——media-gen-mcp P0 教训）。**用户关闭出口**：有头窗口用户可自行点 X（pipe 断 ⇒ proc exit ⇒ SubprocessManager 既有清理路径）——这是有头相对 hidden 的结构性优势，也是「接管后永不自动杀」能够安全成立的前提。lasso 侧关闭出口 = 显式 stop（admin/browser_recycle 族，须带归属校验）。
     - 实现挂点：SubprocessManager 增 per-spec `reapPolicy`（idleMs / stickyExempt / hardCapMs 三元组，缺省= 现行为字节级不变——存量 spec 不触新路径）；接管探测器与用户 browse 调用并发安全（JSON-RPC 请求可并发，实现期验证 + INV 钉「探测器不干扰在途调用」）。
   - **归属锚（r1 修正）**：~~窗口 title 前缀~~（title 由页面 `<title>` 控制，CLI 无法外部设置——不可行，已删）。改为：①profile 路径编码 lasso-owned（`--user-data-dir` 见②r1，ps 可见）；②`browse_headed` 返回体 `window_opened: true` 如实回显；③description 事先声明（见⑥r1）。外部观察者（用户/ps）锚 = profile 路径 + proc args。
6. **免解锁与 consent 契约（r1 修订——N3）**：独立工具 `browse_headed`（对齐 browse_headless/browse_logged_in 命名族，R-CI-01），默认注册。**免解锁依据重写**：不以「本地资源、无付费、无 key」（成本维度——静默守则的判据是打扰维度，成本不构成豁免）为由，而以**本机工具族先例**（browse_logged_in 同默认注册，操作本机 Chrome 资产）+ **打扰面用 consent 契约治理**为由：browse_headed 是 S2 介入型工具（调用即弹真实窗口），非 S1 零打扰通道——**description 首行钉死 `opens a real on-screen window — call only after explicit user consent`**，hint 同步带 ask-the-user 指令（A.2r1）。INV-25 式部署级解锁门**定向驳回**（r1 表 N3 行论证：介入面反而更大且弱化每次调用的 consent 检查）；保留升级出口：实施后若实测 agent 无视 consent 语言，回写本文件升解锁门。SSRF guard 与三层安全经 BrowseChannel 继承，**零削弱**。

### A.5 驱逐哨兵规格

- **挂点**：wrapNavigate 完成后注册短窗观察（framenavigated 监听 + 5s 定时窗），写会话级状态。单写者 = wrapNavigate（D-ζ 基建，R-INT-07 合规：仅一处写、clearTimeout/listener 随 respawn 清理）。
- **判定**：非 lasso 发起的主框架**跨域**导航（目标域 ≠ 本 lasso 导航目标域）⇒ `eviction{from,to,at_ms}`。不枚举站点黑名单（脆弱），用「异域跳转」泛化信号。
- **回显**：①后续任意 browse 调用返回体附 `page_evicted`（自上次调用以来，一次性消费）；②evaluate 的 `Execution context was destroyed` 错误分类增强——错误串匹配 + 会话 URL 偏离目标域 ⇒ typed error `page_redirect_eviction_suspected`（消费方首次能机械区分「我的调用错了」vs「站点杀了无头」）。
- **资源回收**：5s 窗定时器 + listener 在窗口结束/respawn/shutdown 三点清理（记入触碰面登记，03 §0.5.2）。

### A.6 rejected-by-design（R-CI-06）

1. **autoEscalateOnEviction 自动升级**：自动在用户屏幕弹真实窗口 = 最小惊讶违反 + visible 红线面。信号+hint 已满足「有办法」诉求。
2. **rebrowser/patchright CDP patch（路线①）**：D-α/D-β 实效反证 + patch 锁变更放大 + 上游只读边缘。**条件重启**：若未来出现「CDP patch 后通过 tm 类站点」的可复现 L3 实证（非厂商自述），重开裁决。
3. **新浏览器二进制（Camoufox/Firefox 定制构建/Xvfb 类）**：新组件红线，需强论证——本案有头路线已实证覆盖，论证不成立。
4. **行为模拟（鼠标轨迹伪造等）**：军备竞赛无底洞 + 简单架构否决；L2 真实环境天然带真实行为基线。

### A.7 兼容 / 安全 / 测试面

- **兼容**：L0 字节级不变；L1 是决议 B 的行为变化面（见 B.3）；L2 纯新增工具，存量调用方零影响。
- **安全**：SSRF/三层安全经继承零削弱；有头窗口按 A.4⑤r1 两态生命周期纳入 never-kill 防线（接管粘滞 ⇒ 永不 idle 收割；硬顶是唯一兜底出口，24h 级不构成「用户面前关窗」面）；SSRF guard 对 browse_headed 同样生效（spawn 前校验不变）。
- **测试（r1 更新）**：①INV-97 HeadedChannel 平级断言（extends BrowseChannel / 装配四处 / spec 无 --headless、**有 lasso-owned --user-data-dir（非裸 --isolated、非上游默认共享 profile 路径）** + --no-usage-statistics + AutomationControlled / **无 StealthEngine 注入** / reapPolicy 三元组挂载）；②**INV-99（r1 新增）两态生命周期契约**：未接管 idle 收割走 LASSO_HEADED_IDLE_MS（默认 30min，非 headless 域 5min）/ hasFocus 探测命中 ⇒ userTaken 粘滞且 idle 收割豁免 / 硬顶 24h 兜底存在 / 探测器与在途 browse 调用并发安全 / **存量 spec 无 reapPolicy ⇒ 行为字节级不变**（回归锚）；③INV-98 哨兵契约（单写者、5s 窗清理三路径、page_evicted 一次性消费语义）；④单测：驱逐判定（同域 hash 跳转不误报、异域跳转必报）；⑤L3 真机：tm.aliyun.com browse_headed 全流程（真机实验 Chrome 用后清理）——**以 WT0 spike 通过为前提**。

---

## 2. 决议 B：url 语义统一——单一 ensure-navigation 模型（P1-A/B/C 一体）

### B.1 统一语义（全部 url 感知 action，四族收敛为一）

1. **url 缺省** → current-page 模式（会话守卫：无会话 ⇒ didnt + `no_active_session`，不静默空白页）。**白名单 += evaluate**（CURRENT_PAGE_ACTIONS 三项：screenshot/wait/evaluate）。
2. **url = 当前页** → **零导航直执行**（不 reload——同 URL 导航会毁 SPA 状态；evaluate 现语义泛化到全族）。
3. **url ≠ 当前页** → 先导航（保留 hash same-document 检测/no_reload 语义）再执行，回显 `did_navigate: true`。
4. 所有 url 感知 action 返回体统一带 `did_navigate: true/false`（如实回显，消灭「url 是装饰」）。

### B.2 四族收敛明细

| action 族 | 现状 | 统一后 | 行为变化 |
|---|---|---|---|
| evaluate | ensure-nav（已是目标语义） | 不变 + 白名单补缺省形态 | 缺省 url 从拒绝 → current-page（**修复 descriptions 谎言**） |
| snapshot/extract | FRESH_PAGE_NAV 空白门控 | ensure-nav | url≠当前页：从「读残留页（假数据）」→ 导航后读（**修复 P1-B 本体**）；空白会话+url：行为等价；已导航+缺省 url：行为等价（观察流 `navigate→click→extract` 无 url 照旧） |
| network/screenshot/pdf | NAV_FIRST 无条件导航 | ensure-nav | url=当前页：从 reload → 跳过（**改善**：保 SPA 状态；记 CHANGELOG 行为标注） |
| wait/click/fill | current-page 动作 | 不变（wait 已在白名单） | 无 |

三个 Set（NAV_FIRST/FRESH_PAGE_NAV/ensure-nav 分裂）收敛为单一 ensure-navigation 实现 + CURRENT_PAGE_ACTIONS 白名单——R-CI-02「同一类操作一种做法」落地，BrowseChannel 语义面**净减**一个概念。

### B.3 兼容 / 安全 / 测试面

- **有意 breaking**（修复目标本身）：依赖「extract+url 读当前页」的 workaround 流（喵虎时间线 7 证存在）统一后行为改变。CHANGELOG + descriptions 显著标注 + 返回体 did_navigate 使新旧行为可判读。
- **安全**：ensure-nav 语义下每次导航仍过 SSRF guard（wrapNavigate 路径不变）；current-page 模式零新增导航面。
- **测试**：dispatch 矩阵单测（action × {缺省, 相同, 相异} × {有会话, 无会话, 空白会话} → 导航行为 + did_navigate 回显值）；INV-92 族（schema↔description 同 commit）同步更新；FRESH_PAGE_NAV golden 测试重写为统一语义版。

---

## 3. 决议 C：P2 批（C1-C5）

| # | 项 | 裁决 | 修点 | 测试 |
|---|---|---|---|---|
| C1 | steps 内 wait 校验与顶层不一致 | 删 BrowseChannel.ts:1758 的 text-only 检查，链内与顶层统一走 `ExpectPoll.validateCondition`（三键）；同步审 :987 的链内 expect 剥离注释是否仍成立 | BrowseChannel.ts（与决议 B 同文件域，并入 WT1） | steps 传 selector/url_contains 各一用例（修复前红） |
| C2 | worked+error 并存 | StepEngine.ts:262-266 verified→worked 覆写时，`result.error` 迁入新字段 `warnings: string[]`；消费方契约固化为 **outcome=worked ⇒ error 必空** | StepEngine.ts | 断言 worked 结果无 error 字段 + warnings 保留瞬态信息 |
| C3 | headless→logged_in 跨通道 fallback | index.ts 装配层 browse_headless 的 `FallbackPlan.fallbacks` **默认移除 browse_logged_in**；escape hatch：config `LASSO_FALLBACK_CROSS_CHANNEL=1` 可恢复（默认关）。理由：语义越权（登录态通道须用户显式选择，INV-23 精神扩展）+ 无 9222 环境必死加时 + D-ε 污染旧账 | index.ts（与 HeadedChannel 装配同文件域，并入 WT2） | INV 断言默认链无跨通道边 + config 开启路径单测 |
| C4 | final_url 尾部句点 | **取证工作项**（不盲修）：L2 证据定位句点进入点——消费方输入侧 / 站点 Location 重定向 / lasso 响应拼接 三选一。若 lasso 拼接混入 → 修；若上游如实携带 → **不剥**（诚实回显原则），文档标注 | 取证后定（可能 BrowseChannel.ts） | 取证结论落本文件附录后补 |
| C5 | logged_in 硬编码 9222（上次遗留） | 三层：config > `LASSO_LOGGED_IN_CDP_PORT` env > 默认 9222；doctor 检查项同步报当前生效端口 | LoggedInChannel.ts + config + doctor | 环境变量覆盖单测 + 默认值回归 |

---

## 4. 决议 D：worktree 并行拆分（r1 修订：新增 WT0 前置 spike；WT2 文件域扩 SubprocessManager/config）

**文件域冲突分析（r1 更新）**：BrowseChannel.ts 是 B/C1/C4/哨兵 四方热点 ⇒ 必须串行；index.ts 是 A-L2/C3 双方热点 ⇒ 归并同 WT；**r1 新增**：SubprocessManager.ts（A.4⑤r1 reapPolicy）与 WT1/WT3/WT4 零交集 ⇒ 归 WT2；config.ts 现为 WT2（LASSO_HEADED_IDLE_MS/HARD_CAP_MS）与 WT3（C5 LASSO_LOGGED_IN_CDP_PORT）双方弱冲突——不同字段区块，冲突时 WT3 让行（沿用 doctor 让行规约）；check-invariants.mjs / descriptions.ts 为弱冲突（WT1 改既有段、WT2 加新段，行域不重叠）。

### 第零波（前置 gate，非代码树——手动实验，~10min）

**WT0【L2 实效 spike——N2 修复：止损前置于固化】**
- 操作：`npx -y chrome-devtools-mcp@1.7.0 --isolated`（**不带 --headless**，MCP 入口 headless 默认 false 已核实）stdio 起 server → 经 JSON-RPC 发 navigate tm.aliyun.com → **驻留 ≥10s**（覆盖驱逐窗口 2-3s 数倍）→ evaluate 检查会话存活 + 页面 URL 仍为目标域。**重复访问两轮**（同 profile 二访，观察驱逐窗口是否收缩——直接影响 L1 抢读余量评估，回写开放项）。
- **形态迁移逻辑（03 §0.5.5，须显式写死防误杀/误放）**：spike 裸形态 vs HeadedChannel 成品形态的 flag 面差异=①profile：temp dir vs lasso-owned 路径（两者均零历史首启，站点不可见路径本身——等效）；②automation flag：spike 裸形态**不排除** `--enable-automation` 等，成品**排除**。⇒ 结论迁移是**保守方向**：spike（更脏的 automation 面）通过 ⇒ 成品（更干净）大概率通过，放行有效；spike 被驱逐 ⇒ **不得直接砍单**——先加 `--chromeArg=--disable-blink-features=AutomationControlled` + `--ignoreDefaultChromeArg=--enable-automation` 二分复测一轮（5min）：仍驱逐 ⇒ fresh-profile 前提崩，砍单坐实；驱逐消失 ⇒ 前提成立且成品 flag 配方反成关键路径（回写 A.4③）。
- 判定：驻留存活 ⇒ 路线②实效前提成立，WT2 放行；被驱逐 ⇒ 按上节二分复测一轮——**仍驱逐** ⇒ **WT2 整树止损**（HeadedChannel/INV-97/99/相关 doctor 项全部不建），A.1 路线②改判 REJECTED-待新证据，本案降级为 L1+哨兵交付（哨兵/决议 B/C 批不受影响）；**驱逐消失** ⇒ 前提成立 + 成品 flag 配方升级为关键路径（A.4③ 回写）。
- 纪律：真机实验 Chrome 用后清理（树杀 + profile 目录删除，验尸留证）；spike 结论（两轮驱逐行为 + 二访窗口变化）回写本文件开放项 2 后才算关闭；形态与 HeadedChannel 同 spawn 形态（同版本同入口同 flag 族——pipe CDP、automation flag 面一致），结论才可迁移（03 §0.5.5 证据迁移前提）。

### 第一波（3 并行，WT0 通过后放行 WT2；WT1/WT3 与 WT0 无依赖可先行）

**WT1【url-语义统一】**＝决议 B 全部 + C1
- 文件域：`src/channels/BrowseChannel.ts`、`src/tools/descriptions.ts`、`src/tools/browse.ts`(schema)、既有 browse 测试、`src/invariants/check-invariants.mjs`(INV-92 族改)
- commit 序：①fix(browse): unify url semantics to single ensure-navigation model（含 evaluate 白名单 + did_navigate 回显 + 四族收敛）②fix(steps): wait expect validation unified to three-key（C1）
- 验收：npm run gate（**后台化 nohup + 60s 轮询**，180s stall 判死纪律）

**WT2【headed+fallback+生命周期】**＝A.4（r1 版）+ A.6 装配 + C3
- 文件域（r1 扩）：`src/channels/HeadedChannel.ts`(新)、`src/tools/headed.ts`(新)、**`src/subprocess/SubprocessManager.ts`(reapPolicy 三元组 + 接管探测器挂点)**、`src/config/config.ts`(LASSO_HEADED_IDLE_MS/HARD_CAP_MS，弱冲突让行 WT3)、`src/index.ts`(装配四处 + fallback 边移除)、`src/invariants/check-invariants.mjs`(尾部新增 INV-97/98/**99**)、`src/doctor/`、新 headed 测试
- commit 序：①feat(subproc): per-spec reapPolicy + headed two-state lifecycle hooks（存量 spec 字节级不变回归锚）②feat(channels): HeadedChannel headed tier L2（含接管探测器 hasFocus 粘滞标记 + consent description）③fix(fallback): remove cross-channel headless→logged_in default edge（C3）④test(invariants): INV-97/98/99
- 验收：gate 后台化 + 真机冒烟（本地 spawn 有头实例：**验证 hasFocus 接管探测→粘滞→idle 豁免链路** + 显式 stop 清理 + 硬顶路径单测；真机 Chrome 用后清理——红线）

**WT3【step-engine+logged-in】**＝C2 + C5
- 文件域：`src/browse/StepEngine.ts`、`src/channels/LoggedInChannel.ts`、config、`src/doctor/`（doctor 与 WT2 弱冲突：不同检查项区块，可自动合并；冲突时 WT3 让行；**config.ts 同规约**）
- commit 序：①fix(steps): worked outcome moves error into warnings（C2）②feat(logged-in): configurable CDP port（C5）
- 验收：gate 后台化

### 第二波（依赖 WT1 合并；WT4 不依赖 WT2 成败——r1 拆清）

**WT4【驱逐哨兵+取证】**＝A.5 哨兵 + C4 取证
- 文件域：`src/channels/BrowseChannel.ts`(哨兵挂 wrapNavigate——基于 WT1 统一后的语义面)、`src/types.ts`(page_evicted)、哨兵测试、C4 取证产物落本文档附录
- commit 序：①feat(browse): eviction sentinel + page_evicted signal + typed eviction error（hint 文案用 A.2r1 consent 版）②docs: final_url trailing-dot forensics结论
- 验收（r1 拆分——哨兵独立于 L2 成败）：gate 后台化 + tm.aliyun.com L3 实测哨兵信号复现（**headless 通道上即可测**——驱逐本就发生在 headless，无需 headed）；browse_headed 全流程 L3 仅在 WT2 已合并时执行；真机 Chrome 用后清理

### 合并序（r1）

```
WT0（手动 spike，~10min）──失败──▶ WT2 砍单，仅 WT1/WT3/WT4（哨兵+取证）交付
   │通过
WT1 → main（先：BrowseChannel 语义面是 WT4 的基座，且 WT2 弱冲突方需 rebase 它）
WT3 → main（随时，与 WT1 零交集；与 WT2 在 config/doctor 弱冲突，先到先合、后者 rebase）
WT2 → main（WT0 通过 + WT1 之后 rebase，check-invariants/descriptions/config 弱冲突手工核对）
WT4 → main（严格最后，基于统一后 BrowseChannel；不依赖 WT2 成败）
```
全程单主题 commit、**不 push 不发版**；上游只读（本案零上游变更——路线① REJECTED 的直接收益）。

---

## 5. 开放项（L3 真机验证依赖；r1：项 2 改由 WT0 前置关闭）

1. macOS occlusion：屏幕边缘露出 vs 屏幕外全藏窗口的 `visibilityState`/rAF 差异（决定 A.4④ 最终窗口参数）。
2. ~~tm.aliyun.com `browse_headed` 实测：驱逐是否消失（L2 核心实效验证；若失败回写本文件并重审 A.1 路线②前提）~~ **r1 升级为 WT0 前置 spike（决议 D 第零波）**：同形态 spawn 手动实验（驻留 ≥10s + 两轮重复访问），**先于 WT2 开工**；失败即 WT2 整树止损——实效验证不再排在全部实现之后（N2/03 §0.5.8 deferred-Spike 规则）。**已关闭（WT0 通过 + 验收员 tm 真机四轮：驻留根治 6/6 存活）。**
3. 边缘露出窗口的行为评分对照（NVC 是否因部分可见扣分——对照全可见基线）。
4. ~~C4 final_url 尾点取证（L2 真实往返捕获）~~ **已关闭（对抗复审 r1 I-1，94b5929）**：结论=lasso 侧混入（上游 navigate_page 散文句末句读被 `\S+` 吞入；真机复现 final_url 带尾点而 location.href 无点）→ 剥尾部 `[.,;:!?]`，括号/引号不剥。**取证定案即本附录（原计划 WT4 docs commit 的落档义务在此结清）。**

### 5r2. 对抗否定复审第 2 轮登记（2026-09-16；WT0-WT3 合并 + 94b5929 之后的终态复核）

| # | 发现 | 级 | 处置 |
|---|---|---|---|
| R2-1 | **steps 链路径保留 P1-B 假数据形态**：executeStep 直派裸 handler（BrowseChannel.ts:1022，不经 dispatchAction ensure-nav 门），StepEngine 成功终止恒回 `final_url: url`（StepEngine.ts:330，请求串非真值）；`action:"extract"+steps+url` 在会话残留页上跑链 → worked + final_url 仍声称目标页；`action:"navigate"+steps` 先导导航结果（final_url/sd 双标注）也被丢弃、链结果无 did_navigate。决议 B 表未列 steps（非回归，存量行为），但 descriptions 新增的「one rule for EVERY url-aware action」全称断言对 steps 形态不真 | P1 | **当场修描述**（三处 description 块补「single-action calls」限定 + steps 链语义句）；行为修复（链尾 final_url 真值化 / 链 did_navigate 回显）登记为开放项 5，须独立小决议（改行为+测试，不并入本轮）。**回炉修复员 2 复核补漏（af765ef 后）**：限定实漏 2 处残留——headless Returns 段「Every url-aware action echoes data.did_navigate」全称（第 4 处；链结果 `action:"chain"` 无 did_navigate）+ logged_in Args 段交叉引用未显式限定（第 5 处）；均已补限定，全套限定与 R2-2 症状化由 `test/unit/bug09-r2-description-truth.spec.ts` 钉死（摘除任一即红） |
| R2-2 | **page_evicted 前向引用**：BROWSE_HEADED_DESCRIPTION「when browse_headless reports page_evicted」——全库无发射点/无类型/无测试（WT4 哨兵未合并）；r1 曾以「WT4 合并自愈」延期，但验收以 WT4 未合并收卷 → 延期失效，描述承诺结构性不可达信号 | P1 | **当场修**：改为症状判读（evaluate 死于 'Execution context was destroyed' 秒级 = 驱逐形态）——指导力等价、当下为真；WT4 若合并可再切回信号形态 |
| R2-3 | **LASSO_HEADLESS_IDLE_MS=0 交叉域副作用**：startIdleWatchdog（index.ts）是 cleanupZombies 唯一周期驱动且 gated 于 headlessIdleMs>0 → 该 opt-out 同时停掉 headed 档 idle 收割**与 24h 硬顶**（INV-99「硬顶=唯一兜底」在该配置下为假）。缓解：窗口可见用户可关 / exit 钩子 / 陈年扫描兜底 | P2 | KEY-GUIDE LASSO_HEADED_IDLE_MS 行已补耦合披露；代码修法（watchdog 在 headed 需要时以 legacy-never-fire 阈值启动）登记为开放项 6 |
| R2-4 | **决议回写缺口**：C5 实施偏差（不另立 `LASSO_LOGGED_IN_CDP_PORT`，复用既有 `LASSO_CDP_PORT` 单旋钮——R-CI-02，理由在 commit 980da1c）未回写 §3 C5 行；C4 取证结论原计划落本文附录未落（已由 5r2 项 4 结清） | P2 | 本节即为回写；C5 偏差在此追认 |

### 5r2. 开放项（r2 新增）

5. steps 链 final_url/did_navigate 真值化（R2-1 行为面）：链尾读真实 href 回显（或无导航链省略 final_url）+ navigate+steps 先导导航结果透传 + 链级 did_navigate——独立小决议后实施，配 dispatch 矩阵测试扩展（steps × {navigate 先导, 残留页} × final_url 真值断言）。
6. headed 生命周期与 headless watchdog 解耦（R2-3 代码面）：`headlessIdleMs=0 ∧ (headedIdleMs>0 ∨ headedHardCapMs≠0)` 时仍启动 reaper、legacy 阈值传 never-fire 哨兵（Infinity）——防 `LASSO_HEADLESS_IDLE_MS=0` 静默拆掉 24h 硬顶；INV-99 补「watchdog 驱动面」断言。

### 5r3. 对抗否定复审第 3 轮登记（2026-09-16；376631e 之后的七面攻击）

七面：①变异（7 组，6 kill + 1 存活归类）②真机实弹（新通道普通站/资源回收）③url 语义×hash 边界（含真机）④安全三层 ⑤BUG-04~08 回归（gate 全量）⑥文档零漂移 ⑦worktree 残留。

| # | 发现 | 级 | 处置 |
|---|---|---|---|
| R3-1 | **browse_headed SSRF 接线零测试钉（变异存活实锤）**：headed-channel.spec.ts 工具段整体 `vi.mock` ssrf-guard（ALWAYS_OK_SSRF）——M-I 变异（整段删除 headed.ts SSRF 门）**全库 3087 测仍绿**。打破工具族既有不变量（browse_headless/logged_in 有 file-guard.spec §5 真 guard 端到端；network/fetch_url/screenshot/pdf/fetch_feed 各有 `served_by:"lasso.ssr_guard"` 断言）——新工具的安全接线无任何测试可守护漂移 | P2 | **当场修**：新增 `test/unit/headed-tool-ssrf-entry.spec.ts`（真守卫不 mock，3 测）：私网 IP 字面量 → didnt + `ssrf_blocked:private_ip:*` + channel.browse 零调用；file:// 默认拒 + opt-in hint；allowlist 放行 127.0.0.1/32 → 到达 channel + window_opened 回显。复跑 M-I 变异 → 2 红 kill（还原 md5 核对） |
| R3-2 | **headed ssrfBlocked 缺 fileGuardHint parity**：browse.ts 入口 file: 族拒绝带 opt-in 指引（BUG-05 B3），headed.ts 复刻时丢了 hint 参数——功能等价（仍拒）但消费方拿不到 LASSO_ALLOW_FILE_FROM 指引 | P3 | **当场修**（与 R3-1 同 commit）：ssrfBlocked 补 hint 参数 + fileGuardHint 接线（additive 字段，browse.ts 单一范式对齐）；上表 file:// 用例断言 hint 含 LASSO_ALLOW_FILE_FROM |
| R3-3 | M-H 变异存活（registerBrowseTools `crossChannelFallback=false` 默认值改 true 全绿）：工具层默认参数为死码——生产装配 index.ts:946 恒显式传 config.crossChannelFallback，默认 off 真源在 config 层（`loadConfig(env:{}）→ false` 已钉 fallback-cross-channel.spec:146） | P3 | 归类为可达性等价变异，**不修**（安全方向正确的防御性默认）；若未来移除默认值改必填可一并消歧 |
| R3-4 | bug09-w1/w2/w3 分支 ref 残留：worktree 目录全清（`git worktree list` 仅 main）、三分支零未合并提交（`main..wN` 全空），但分支引用未删 | P3 | 登记；删除属仓库卫生操作，留用户处置（`git branch -d bug09-w1 w2 w3` 均安全） |

**真机实弹（r3，本地 dist 重建后）**：六探针全过——①example.com 首调 worked + window_opened:true + did_navigate:true（retrieval chrome_devtools_mcp_headed）；②同 URL 二调 did_navigate:false（规则 2 真机）；③hash-only 目标 did_navigate:true + same_document_navigated:true + **same_document_reloaded:true**（③面边界组合语义真机钉——ensure-nav 全量判等含 hash + D-1 默认 reload 两层都在新通道继承链上成立）；④current-page evaluate 读到 `https://example.com/#r3probe`（会话连续性）；⑤私网 192.168.1.1 → lasso.ssr_guard 拒（第 1 层真机）；⑥teardown exit 钩子树杀（SIGKILL×8）+ `~/.cache/lasso/headed/` 清零 + chrome-devtools-mcp 进程数基线 12→12（零孤儿，未触碰任何非本次实验进程）。

**变异记分（r3）**：M-C（hash 判等剥 hash——marathon 假数据形态复活）4 红 kill；M-A（ENSURE_NAV_ACTIONS 去掉 extract——P1-B 复活）4 红 kill；M-E（接管探测器永不触发）1 红 kill；M-G（描述限定摘除）1 红 kill；M-F（C1 三键校验判死）1 红 kill；M-I（SSRF 门整删）修复前全库存活 → 修复后 2 红 kill；M-H 存活（R3-3 归类）。全部还原（md5/git status 核对）。

**文档零漂移（⑥）**：§5r2 R2-1 回炉记录（第 4/5 处）、KEY-GUIDE LASSO_HEADED_IDLE_MS 耦合披露、C4 取证结清 + C5 追认（§3 表保留原决议文本 + §5r2 追认行，KEY-GUIDE 用户面为实施真值 LASSO_CDP_PORT）均在；page_evicted 全库仅存于 truth spec 负向断言；README 无 browse_headed 提及属未发版常态（check-readme-sync 绿）。

## 6. 验收 gate（03 §0.4/§0.5；r1 补 breaking 缓解清单）

- 每工作树：npm run gate（nohup 后台 + 60s 轮询）全绿；vitest 基线 2997+1 → 只增不减。
- 触碰面登记（03 §0.5.2；r1 增补）：wrapNavigate（B+哨兵）、FallbackPlan 装配（C3）、StepEngine expect 段（C2）、spawn spec（L2）、**SubprocessManager 收割路径（A.4⑤r1 reapPolicy——存量 spec 字节级不变是硬回归锚）**、**接管探测器（新机器——INV-99 钉并发安全与粘滞语义）**——每面有对应测试轨或显式 not-assigned-with-reason。
- 审查按 03_审查测试清单：§0.4 裁决纪律（本案路线对比已锚 D-α~ζ 实证 + 业界对照条款；**r1 自我参照修正两处**：路线②「实证有效」降「假设有效待 WT0/L3」、生命周期「对齐 30min」改两态设计——均因外部锚错配）、§3.4 zero-issues-pass、§3.7 耦合对枚举先于开轨。
- **用户可见行为变化（r1 盘点为四处，全部进 CHANGELOG 消费方通知**，对齐 doc/bugs/06 消费方通知要点惯例）：
  1. extract+url 导航（读残留页 workaround 失效——决议 B 本体，返回体 did_navigate 可判读新旧行为）；
  2. screenshot/pdf/network+同 URL 从 reload → 跳过（SPA 状态保留，行为标注）；
  3. fallback 边移除（headless→logged_in 默认不再自动回退；escape hatch `LASSO_FALLBACK_CROSS_CHANNEL=1`）；
  4. **驱逐 hint（r1 新增面）**：返回体新字段 `page_evicted`/`hint` 带 ask-the-user 指令——增量字段非 breaking，但 agent 行为面变化（弹窗前须问用户）需在 CHANGELOG 向消费方明示。
  breaking 缓解总原则：三处行为变化均有「可机械判读的回显字段」（did_navigate）或「一键恢复开关」（LASSO_FALLBACK_CROSS_CHANNEL），无静默语义翻转。
