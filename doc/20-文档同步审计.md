# 20 · 文档同步审计（v1.8 → v1.13）

> 审计基线：`lasso-mcp v1.13.0`（commit `a9eb106`，2026-08-17）。真源 = `git log v1.10.0..HEAD`（**实际 2 个 commit**：`0b07536` v1.11.0 + `a9eb106` v1.13.0；v1.8/v1.8.1/v1.9/v1.10 的行为经 tag 间 diff 与既有文档交叉核对）+ `git diff v1.10.0..HEAD --stat -- src/`（44 文件，+2187/−409）+ `rust-helper/`（5 文件，+1573/−35）。
> 门禁：`npm run check-invariants` 79 条全绿（审计后复跑无回归；本次仅新增本文档，未改任何既有文档）。
> 文档风格铁律：README 用户向（用法/能力/配置/费用，零技术内部）；KEY-GUIDE 教开通配置；TROUBLESHOOTING 教排障；技术细节归 ARCHITECTURE.md。

---

## 1. 行为变化全集（用户可感知；源码锚点）

### A. search

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| A1 | **`freshness` 参数全引擎**：`day/week/month/year` 透传智谱（`search_recency_filter`）/ Brave（`pd/pw/pm/py`）/ Bing（`Day/Week/Month`；year 档 Bing 无粒度不传，诚实降级） | v1.11（T6） | 查新闻/版本动向不再手写日期；不传 = byte-identical 基线 | `src/tools/search.ts`（searchSchema.freshness）；`src/channels/BraveChannel.ts` BRAVE_FRESHNESS_MAP；`src/channels/BingChannel.ts` L237-241；`src/types.ts` SearchFreshness |
| A2 | freshness 全链补齐：machine_mcp 首位引擎 + `browse_headless` 兜底路径不再静默丢参（DDG `df=d/w/m/y`） | v1.12（T2-5） | 显式时效参数在**任何**引擎/兜底路径都生效 | `src/tools/search.ts` runFallbackChainEngine / fanout 分支；`src/serp/extract.ts` DDG_FRESHNESS_DF |
| A3 | freshness 入 cache key：同 query 不同时效不互相污染缓存 | v1.11（T6） | day 档结果不会被 year 档缓存误命中 | `src/search/SearchCache.ts` `_key()` L175-180 |
| A4 | **DDG 非 CJK 兜底**：SERP 兜底引擎按 query 语言分流——CJK→百度、非 CJK→`html.duckduckgo.com`（纯 HTML 端点，零 Key）；DDG 跳转壳 `uddg=` 自动解包；`retrieval_method` 区分 `serp_scrape_ddg/baidu` | v1.11（T9） | 英文 query 的零 Key 兜底从百度换 DDG，命中率/相关性提升 | `src/serp/extract.ts` serpEngineForQuery / serpUrlFor / unwrapDdgRedirect / SELF_HOST_RE |

### B. 浏览器引擎（chrome-devtools-mcp 0.3.0 → 1.7.0）

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| B1 | **驱动升级 chrome-devtools-mcp 0.3.0→1.7.0**（版本锁常量切换；`--chromeArg` 透传机制解锁） | v1.11（T1） | 11 个月/57 个上游版本的红利；`--disable-blink-features=AutomationControlled` 从「哑 flag」变真到 Chromium | `src/subprocess/SubprocessManager.ts` LOCKED_CDP_MCP_VERSION="1.7.0" |
| B2 | **上游遥测默认关闭**：全部 spec 显式 `--no-usage-statistics`（1.7.0 默认采集使用统计） | v1.11（T1） | 隐私不倒退——Lasso 不向上游回传使用统计 | 同上 + `HeadlessChannel.ts` / `SteelChannel.ts` registerSpec；INV-79(b) |
| B3 | **启动级 stealth**：launch 即下发 `--user-agent=<profile UA>` + `--viewport=WxH` + `--accept-lang=<profile language>`——网络层 HTTP 头（UA / Accept-Language）与 JS 层 `navigator.*` 同源同值 | v1.11（T2）+ v1.13（T3-1） | 消除「HTTP 头发宿主真值（zh-CN）↔ 页面伪装 en-US」的自矛盾指纹；无 TUN 网络下更难被识破 | `src/channels/HeadlessChannel.ts` registerSpec；`src/browse/StealthEngine.ts` buildUserAgentOverrideScript |
| B4 | **macOS 宿主指纹对齐**：新增 `mac_chrome` profile；darwin 默认 profile 从 `windows_chrome_120` 改为宿主对齐（`defaultHeadlessProfileForHost()`） | v1.12（T2-1） | mac 上不再出现「UA 说 Windows、低熵 client hints 招供 macOS」的 OS 级矛盾 | `src/channels/HeadlessChannel.ts` defaultHeadlessProfileForHost；`src/browse/stealth-profiles.ts` STEALTH_PROFILES.mac_chrome |
| B5 | stealth 值域刷新到 2026-07 stable 时代：Chrome 130→151、Safari 17.5→27（`Version/27.0 Safari/605.1.15` 冻结 token）、Firefox 130→153；profile key 名不动 | v1.11（T4） | UA 版本年龄本身就是检测信号，刷新后更安全 | `src/browse/stealth-profiles.ts` STEALTH_PROFILES 各条 |
| B6 | **`LASSO_PROXY` 出口代理**（新 env，默认空=直连）：`browse_headless` 经 `--proxy-server=`、Steel 经 session `proxyUrl`；**`browse_logged_in` 永不读取**（用户真实 Chrome 出口原样，铁律）；doctor `proxy_config` 回显 | v1.11（T10） | 反封锁/代理网络环境可给无头/云浏览器配出口；登录态 Chrome 不受影响 | `src/config/config.ts` proxy + CONFIG_TEMPLATE；`src/index.ts` L427-432/L637；`src/channels/SteelChannel.ts` proxyUrl；`src/doctor/doctor.ts` checkProxyConfig |
| B7 | **network 原生化**：从 JS 层 PerformanceObserver 注入改为 1.7.0 原生 `list_network_requests`（CDP Network 域）——fake-ip/TUN 代理「抓不全」限制（Go/No-Go F2）关闭；`timeout_ms` 字段保留但无行为（原生工具即时返回）；per-request `method/status/reqid` | v1.11（T5） | 资源列表在代理网络下也完整；结果含每个请求的方法/状态码 | `src/browse/cdp-actions.ts` doNetwork + FILTER_TO_RESOURCE_TYPES；`src/tools/network.ts`；`src/tools/descriptions.ts` NETWORK_DESCRIPTION |
| B8 | console 工具从占位变实装：`list_console_messages`（1.7.0 原生） | v1.11（T5） | browse 流程可读页面 console 日志 | `src/browse/cdp-actions.ts` CDP_UPSTREAM_TOOL_NAMES.console_log |
| B9 | Browserbase 通道改 `--wsEndpoint`（与 `--browser-url` 互斥，wss 语义保障） | v1.11（T1） | 托管型云浏览器连接语义更严谨 | `src/channels/BrowserbaseChannel.ts`；INV-79(c) |
| B10 | `LASSO_CDP_PORT` 解析加 NaN/越界守卫（非法值回退 9222 + warn） | v1.11（T12） | 配错端口报错更早更明确，不再静默下渗 | `src/config/config.ts` parseCdpPort |

### C. markdown 抽取（fetch_url / browse）

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| C1 | **defuddle 站点抽取器双激活**：URL 透传 defuddle → ~24 个高频站点（github/reddit/HN/wikipedia/substack/medium/discourse/mastodon…）走专用 extractor + 相对链接绝对化（`/item?id=1` → 完整 URL，可直接 fetch） | v1.12（T2-3） | 这些站点的正文质量显著提升；markdown 里链接可点 | `src/browse/markdown-extractor.ts` MarkdownExtractOptions.url；`src/tools/fetch-url.ts` `url: rawUrl` |
| C2 | **表格/数学结构保真**：`separateMarkdown:true` 让 defuddle 内置转换器接管（GFM 表格 / MathML→LaTeX / 脚注 / callout / 代码语言检测），turndown 降级保底——修复裸 turndown 丢表格结构 | v1.12（T2-4） | 表格不再是碎文本；输出可能含 Obsidian 方言（`==高亮==`、`[^N]` 脚注）——对 LLM 阅读无害 | `src/browse/markdown-extractor.ts` T2-4 分支 |
| C3 | 工具描述加「文档站直出 markdown」TIP：先试 URL 加 `.md` 或站点根 `/llms.txt`（Mintlify/GitBook/Fern 惯例） | v1.12 | 文档站抓取又快又省 | `src/tools/descriptions.ts` FETCH_URL_DESCRIPTION |

### D. desktop（从「能看」到「能点」）

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| D1 | **`ax_act` 真实现**（v0.3.5 起的 Rust 占位废除）：click/type/press/scroll/hotkey 经 AXAPI 语义执行 + 写后读回验证（secure 字段豁免）；逐项结果 `actions_and_results`；全败且含 stale_ref → `didnt`（UI 已变，引导重新 observe） | v1.11（T3） | `desktop` 的 act 真的会点了；结果诚实（部分成功也可见） | `src/desktop/AxProvider.ts` act；`src/desktop/AxBackend.ts` act(app,maxDepth,where,actions)；`rust-helper/src/ax.rs` |
| D2 | **坐标鼠标动作**（tier-3 cgEvent，canvas/Electron 兜底）：`click{x,y,button}` / `drag{from→to}` / `move{x,y}` / `scroll{dx,dy[,x,y]}`；坐标 = snapshot rect 中心 | v1.11（T7） | 画布/自绘 UI 也能点；@wN 透传链同样支持坐标形态 | `src/tools/desktop.ts` schema；`src/desktop/CGEventProvider.ts` ALLOWED_CGEVENT_KINDS；`src/tools/descriptions.ts` INTERACT_ACT_DESCRIPTION |
| D3 | **拖拽物理质量**：按下 200ms + 12 点线性插值轨迹（每点 16ms）+ 100ms 沉淀 + clickState | v1.12（T2-8） | 滑条/拖拽排序从大概率失败变可用 | `rust-helper/src/cgevent.rs` L330-448 drag_interpolation_points |
| D4 | **type 整值替换语义 + Electron 兜底**：AXSetValue 路径=替换（非追加）；AX 不可用（Electron 吞 AXSetValue，Slack/VSCode 典型）时档内兜底「聚焦 + cmd+a 全选 + 逐字符键盘合成」（ASCII） | v1.12（T2-7） | Electron 输入框能输入了；输入语义与用户预期一致（说「输入 X」= 变成 X） | `rust-helper/src/ax.rs` L518-531 / L587-660 type_via_keyboard |
| D5 | `skeleton:true` 树剪枝：被 max_depth 剪掉的子树折叠为边界节点 + childrenCount | v1.11（T8） | 密集 app（Slack/Electron/IDE）token 数量级下降且保留「下面还有」信号 | `src/tools/desktop.ts` skeleton 字段；`src/desktop/AxBackend.ts` snapshot(app,maxDepth,skeleton) |
| D6 | `truncated:true` 顶层诚实信号：仅 max_depth 真剪掉子树时出现 | v1.12（T2-11） | 知道树被截断了，该升 max_depth / 换 skeleton / 换更近 root | `src/desktop/AxProvider.ts` snapshot L161-164 |
| D7 | `find` 命中节点附 `actions:[...]`（AX 动作名，如 press/show-menu/value-set） | v1.12（T2-9） | 知道找到的节点能做什么 | `src/desktop/AxProvider.ts` find 注释；`src/tools/descriptions.ts` DESKTOP_DESCRIPTION |
| D8 | **`find` 拒绝纯 ref 查询**：`where` 只认 text/role；ref 是 act/expect 域——纯 ref/空 where 报 `invalid_params`（此前静默退化成全节点命中 + token 爆炸还装成功；TS zod 删 ref 字段 + Rust 端双保险） | v1.13（T3-3） | 错误用法得到明确报错而非错误结果 | `src/tools/desktop.ts` where schema；`rust-helper/src/ax.rs` invalid_params 兜底 |
| D9 | **expect 后置条件真接线**（schema 早承诺、此前零消费）：act worked 后轮询验后置；失败 → `didnt` + `expect_failed:` 前缀；**连续 2 次命中才算**（稳定性采样，加载闪现元素不再假成功；wait 同款） | v1.11（T15）+ v1.12（T2-10） | 「做完了」的确认可信度提升 | `src/channels/DesktopChannel.ts` verifyExpect + wait REQUIRED_CONSECUTIVE=2 |
| D10 | **VLM 档真执行闭环**：截图推断 → 容错解析为坐标动作 → `cgevent_dispatch` 真执行；不可解析/执行失败 → 诚实 `unknown`（`vlm_inference_only:`，推断原文附 data 不浪费）——不再「调用成功即 worked」的假成功 | v1.12（T2-6） | canvas/Metal 场景链尾结果可信 | `src/desktop/ScreenshotVlmProvider.ts` T2-6 分支 |
| D11 | **VLM region 偏移修复**：带 `screenshot_region` 时 VLM 返回区域相对坐标，dispatch 前平移回全局坐标（此前系统性偏移一个 region 原点还逐项报 ok） | v1.13（T3-2） | 指定区域截图点按落点正确 | `src/desktop/ScreenshotVlmProvider.ts` offsetVlmActionsByRegion |
| D12 | **macOS 15+ 输入合成权限**：`tcc_event_synthesis_denied` → `didnt`（明确报「System Settings → Privacy & Security → Event Synthesizing 授权 helper（或 Accessibility）」）；doctor 新增 #21 `tcc_event_synthesizing` 检查；CGEvent 与 VLM 双消费者同判 | v1.11（T11）+ v1.13（T3-6） | 权限缺失得到人话报错与指引，而非含糊失败 | `src/desktop/CGEventProvider.ts` L324-338；`src/desktop/ScreenshotVlmProvider.ts`；`src/desktop/desktop-doctor-checks.ts` |

### E. 生命周期与稳定性

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| E1 | **stdin-EOF 收尾**：CC 异常退出（崩溃/关窗）→ 父进程死 → stdin EOF → 复用幂等 shutdown；受管子进程不再孤儿到 zombie reaper 1h 阈值 | v1.12（T2-12） | CC 崩溃后 Lasso 进程即时收尾，不留孤儿进程 | `src/index.ts` L1256-1266 |
| E2 | **Steel release 3s 双层上界**：停机链 race 3s + fetch `AbortSignal.timeout(3s)`（session 激活同款）——自托管 Steel 停摆/endpoint 悬挂（实测可挂 ~301s）不再把退出卡到分钟级 | v1.13（T3-4） | 「Steel 挂了」不再拖死 CC 退出 | `src/index.ts` L1208-1222；`src/channels/SteelChannel.ts` releaseSession/defaultSteelSessionProvider |
| E3 | admin/动态注册工具的时延/错误入 metrics 观测窗（`toolManager.setMetrics` 接线） | v1.12（T2-13） | 内部可观测，无直接用户面 | `src/index.ts` L1002-1007；`src/runtime/ToolManager.ts` |
| E4 | inv-selftest 10→14（门禁自测扩） | v1.13 | 内部 | `scripts/inv-selftest.mjs` |

### F. doctor

| # | 变化 | 版本 | 用户感知 | 源码锚点 |
|---|---|---|---|---|
| F1 | `proxy_config` 回显（LASSO_PROXY 配没配、生效面说明；只回显不探活） | v1.11（T10） | 排障第一步：代理配错一眼可见 | `src/doctor/doctor.ts` checkProxyConfig |
| F2 | stealth self-check 增强：宿主默认 profile 回显 + **与已装 Chrome 的版本 skew 提示**（\|skew\|≥2 建议刷新）+ UA 年龄提示（>12 月告警） | v1.11（T4）+ v1.12（rider） | doctor 直接告诉你指纹是否过旧/偏差 | `src/doctor/doctor.ts` chromeVersionSkewHint / estimateUaAgeMonths |
| F3 | serp_selectors 检查改 BAIDU+DDG（google 死配置删除） | v1.11（T9） | 内部一致性 | `src/doctor/doctor.ts` checkSerpSelectors |

### G. v1.8–v1.10 既有项（本轮核对：中文 README 与 KEY-GUIDE 已覆盖，列此存照）

- v1.8：wave1 24 缺陷修复（上游 0.3.0 契约适配、截图真实落盘、launch-chrome 探活、read_text 续页工具、`lasso --version/--help` 常规惯例）。
- v1.9：无头浏览器 5 分钟空闲回收（`LASSO_HEADLESS_IDLE_MS`）、`lasso chrome-stop`（台账 + 归属验证）、`admin tab_restore`。
- v1.10：`launch-chrome` 默认 hidden 档（零窗口/不抢焦点/恒静音；`LASSO_LAUNCH_MODE=visible` 回退）、台账 Chrome 用完即关 ~60s（`LASSO_LAUNCH_IDLE_MS`）、反节流三件套恒带。
- 佐证：README.md L289-298 ✓、KEY-GUIDE.md §B L162-168 ✓、TROUBLESHOOTING.md §8 ✓。

---

## 2. 差距矩阵

图例：✅ 已反映 ｜ 🟡 部分反映/表述过时 ｜ ❌ 缺失 ｜ 🔴 过时且**错误**（事实矛盾）｜ ➖ 不适用（用户向文档不载技术内部）

### 2.1 README.md（中文主档；更新日志段已有 v1.8-v1.13，正文段落局部滞后）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| 版本号/badge/「当前版本」行 | ✅ | badge 动态（npm shields）；L195「当前版本 v1.13.0」+ 完整 v1.8-v1.13 变更段 |
| 费用一览 | ✅ | 无新增付费/免费项（LASSO_PROXY 免费、freshness 免费）——无需改 |
| 60 秒上手 | ✅ | 零配置档已列桌面控制；「机器 MCP 复用」提示 ✓ |
| 能力全家桶（desktop 能点了） | ✅ | L48「点一下…/输入 XX」（v1.11）行 ✓；L47 truncated ✓；L44 markdown 专用抽取器 ✓ |
| **配置详解·高级调优：`LASSO_PROXY`** | ❌ | 高级调优清单（L362-370）**缺 LASSO_PROXY 一行**——它只在 L195 更新日志一句话里出现；用户按「配置详解」查不到这个新 env |
| 「搜一下」段补 freshness | 🟡 | 表格 L43 有（v1.11）✓，但「能帮你做什么·搜一下」（L121-125）未提时效过滤一句话——能力段与表格不对齐 |
| 「抓公开页」段补 v1.12 双激活 | 🟡 | L131 未提 20+ 站专用抽取器/表格保真（仅 L44 表格 + 更新日志有）；段内 stealth 注（L133）只写到 v1.5/v1.8，未提 v1.11 启动级 UA/Accept-Language 与 v1.12 宿主对齐 |
| 网络审计段（L156-159） | 🟡 | 未提 v1.11 原生化（TUN 网络下也完整）——非错误，是能力提升未宣传 |
| 更新日志段形态 | 🟡 | L195 单段 900+ 字墙——用户向可读性差（建议拆 version 列表；低优先） |
| INV-65「更多文档」段 | ✅ | L445-449 引 doc/08 + doc/09 ✓ |

### 2.2 README.en.md（更新到 v1.11.0，缺 v1.12/v1.13）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| 「Current version」行 | 🟡 | L193 停在 **v1.11.0**——缺 v1.12（markdown 双激活/宿主对齐/VLM 诚实化/Electron 修复/拖拽/truncated/find actions/稳定性采样/stdin-EOF）与 v1.13（Accept-Language 一致/VLM region 修复/find 拒 ref/Steel 3s）两段 |
| freshness / DDG / LASSO_PROXY / desktop act / 1.7.0 | ✅ | v1.11 内容与 zh 对齐（L43/L193/L275） |
| **高级调优 env 清单** | ❌ | 缺 `LASSO_PROXY`、`LASSO_HEADLESS_IDLE_MS`、`LASSO_LAUNCH_MODE`、`LASSO_LAUNCH_IDLE_MS` 四个 bullet（zh 版有后三个、缺第一个；en 全缺） |
| v1.10 内容（hidden 档/chrome-stop/idle） | ✅ | L275 已含 |
| INV-65 段 | ✅ | L426-427 引 doc/08+09 ✓ |

### 2.3 README.de / es / fr / ja / pt / ru（6 份；全部冻结在 commit `86a7cc0`，2026-07-23 ≈ v1.2/v1.3 era）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| v1.5 反检测（16 路 stealth） | ❌ | 无任何 stealth/反检测描述（grep「反検出/Steel」零命中） |
| v1.6 Steel 自托管（免费云浏览器） | ❌ | 费用一览表**缺 Steel 行**——免费项缺位会误导「要过反爬只能付费」 |
| v1.4 机器 MCP 复用（零配置搜索） | ❌ | 搜索节无「已配 web-search-prime 自动复用」提示（grep machine/web-search-prime 零命中） |
| v1.8-v1.13 全部（见 §1 全表） | ❌ | freshness/DDG/LASSO_PROXY/desktop act/1.7.0/生命周期/隐默启动…全部缺失 |
| 结论 | 🔴→🟡 | 内容跨 11 个 minor 版本未同步；非「错误」但能力面严重失真。处置：以 README.en 补齐 v1.12/v1.13 后作模板整体重译，或在文首加「内容以中/英版为准」过渡注记 |

### 2.4 doc/KEY-GUIDE.md（整体最新；两处小差距）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| env 全收录（LASSO_PROXY/LASSO_LAUNCH_MODE/LASSO_LAUNCH_IDLE_MS/LASSO_HEADLESS_IDLE_MS/LASSO_CDP_PORT） | ✅ | E 节表格 L305-314 全收录，LASSO_PROXY 条目完整（生效面/铁律/示例/doctor 回显） |
| v1.8/v1.9/v1.10 行为注记 | ✅ | §B L162-168 三段 ✓ |
| stagehand 状态（v1.12 复核） | ✅ | L276 复核补记 ✓ |
| **C 节桌面控制：macOS 15+ Event Synthesizing 权限** | ❌ | 只列 Accessibility + Screen Recording——v1.11 起坐标鼠标（cgEvent）在 macOS 15+ 需「Event Synthesizing」授权（或 Accessibility 兜底），doctor #21 专查；缺一句会导致「点不动但已勾了两项权限」的排障死角 |
| F 节「doctor 现包含 38 项检查」 | 🟡 | doctor.ts 现 39 处 `checks.push`（v1.11 加 proxy_config + #21 tcc_event_synthesizing 后计数漂移）；建议改为「以 `lasso doctor` 实跑输出为准」或精确复核 |
| freshness（搜索节示例） | ➖ | 能力非 key 配置，可不加（README 职责） |

### 2.5 doc/TROUBLESHOOTING.md（**滞后最多**；最后实质更新 v1.10 前）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| §1「doctor 跑 32 项 readiness check」 | 🔴 | v1.0 时代数字；现已 +machine_mcp(#36)/steel(#37)/creepjs(#38)/stagehand(#39)/proxy_config/tcc_event_synthesizing(#21)。示例 JSON 的 `"lasso_version": "1.0.0"` 同旧 |
| §2.7 `upstream_unsupported:network` 根因 | 🔴 | 文案仍说「不支持 `Network.*` CDP 域或 `Runtime.evaluate`」——v1.11 起走原生 `list_network_requests`，该错误面已收窄为「上游工具缺失」；修复建议里的 PerformanceObserver 语境全部失效 |
| **新排障项：macOS 15+ Event Synthesizing**（`tcc_event_synthesis_denied`） | ❌ | 任务点名的三新项之一；error_kind 释义表缺条目（症状/根因/修复三段式都没有） |
| **新排障项：Steel 停摆/退出卡住** | ❌ | v1.13 已修（3s 上界）；缺「Steel Docker 挂了会怎样/怎么确认」条目（现象：`browse_cloud_steel` 返 unknown 自动降级 + 退出不再卡） |
| **新排障项：Chrome 版本偏差提示** | ❌ | doctor stealth self-check 的 skew hint（\|skew\|≥2）/UA 年龄 >12 月——用户看到该 detail 不知道何意，缺解释条目 |
| 新 error_kind：`vlm_inference_only:*` | ❌ | VLM 链尾诚实 unknown 的新错误族（no_coordinate_action / execution_failed）缺释义 |
| 新 error：`invalid_params`（find where.ref） | ❌ | v1.13 行为变化：老用法（纯 ref 查询）从「装成功」变「报错」——**Breaking 行为**，升级用户最需要一条 FAQ |
| LASSO_PROXY 排障 | 🟡 | 可加「代理不生效？→ doctor proxy_config 回显 + logged_in 永不走代理是设计」；优先级中 |
| Q2 `npm install -g chrome-devtools-mcp@<pinned>` | 🟡 | 实际机制是 npx `-y chrome-devtools-mcp@1.7.0` 锁版自动拉取，用户无需也不应手动全局装；建议改为「版本由 Lasso 锁定自动拉取，报错时检查网络/npx」 |
| Q5 引 parse11-acceptance.md | 🟡 | 用户向文档链内部 parse 验收文件（风格铁律灰区）；可改指 README 诚实边界段 |
| §8 v1.10 静默/反节流 | ✅ | 已有且准确 |
| DDG 兜底说明（英文搜索走 DDG） | 🟡 | 零 Key 用户感知变化，可并入搜索引擎 FAQ；优先级低 |

### 2.6 ARCHITECTURE.md（**滞后最重**；v1.0 快照，2026-07-22 后未更新）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| §6 边界「**不做坐标 grounding**：desktop 走语义 AX tree，不 click (x, y)」 | 🔴 | **与 v1.11 直接矛盾**：坐标鼠标已是 tier-3 cgEvent 正式能力（canvas/Electron 兜底 + VLM 链尾真执行）。这是全矩阵唯一的「架构级错误陈述」，最高优先修正（边界应改写为「语义优先、坐标兜底、ref/坐标两形态分层」） |
| 「当前版本 1.0.0（2026-07-22）」/「v1.0 架构概览」 | 🔴 | 应为 1.13.0；标题与落款同步 |
| 「65 条 INV」×3 处（§5 原则8/§7 表/§8 表） | 🔴 | 现 79 条；INV-66..79 族（v1.8 wave 修复/浏览器生命周期/静默启动/1.7.0 迁移守护）未入分类表 |
| §7 测试策略：65 INV / ≈1400 TS / ≈180 Rust | 🔴 | 现 79 INV / 1961 TS / 207 Rust |
| §10 索引：doctor「32 项 check」/ index.ts ~1000 行 | 🟡 | doctor.ts 现 ~1900+ 行、检查项增长；行数级示意全旧 |
| chrome-devtools-mcp 版本锁 | ❌ | §7「契约锁」未载明 **1.7.0**（迁移要点：wait_for.text 变数组/--chromeArg/--wsEndpoint/--no-usage-statistics/network+console 原生）——这正是本仓最大的一次上游迁移，架构档零记载 |
| network 原生化 / console 实装 | ❌ | 数据流与 Subprocess 层描述无 |
| search：machine_mcp 复用（v1.4）+ DDG 兜底（v1.11） | ❌ | §2 图与 §9.1 链仍是「智谱→Brave→Bing→Wayback」旧链 |
| Steel 自托管通道（v1.6） | ❌ | §2 图 chrome-devtools-mcp ×2；§6「不引云浏览器」未区分 Steel（自托管/免费/Apache-2.0）与托管型 |
| desktop 四档升级（act 真实现/坐标/VLM 真执行/expect 稳定性采样/skeleton/truncated） | ❌ | §3.3/§9.3 仍是 v1.0 语义（ax invoke + expect 视觉验证的旧叙述） |
| LASSO_PROXY / freshness / stdin-EOF / Steel 3s | ❌ | 全缺 |
| stealth 体系（16 路/4 profile/宿主对齐/launch 级） | ❌ | 全缺（v1.5-v1.12 演进未载） |

### 2.7 ../doc/09-media-interact-实施排期.md（排期真源；止于 v1.7）

| 变化 | 状态 | 现状与差距 |
|---|---|---|
| §1 阶段总览表 **v1.8 / v1.8.1 / v1.9 / v1.10 / v1.11 / v1.13 六行** | ❌ | 表止于 v1.7 ✅ 行——六个已发布版本无行（每行格式：版本/主题/验收/完成记录 + INV/测试数快照） |
| §3 能力跃升曲线 | ❌ | 止于 v1.0「生产可用」；缺 v1.1-v1.13 曲线（LLM 友好抽取→剪枝→config→机器复用→stealth→Steel→门禁→实测修复→生命周期→静默→能点→调优） |
| §4 关键里程碑 | ❌ | 止于 v1.0 npm 发布 |
| 文末落款「2026-07-21 重写」 | 🟡 | 需补「2026-08-XX 增补 v1.8-v1.13」变更注 |
| README「更多文档」引此档为「版本路线」 | ✅ | 引用本身成立（INV-65），但内容滞后使引用价值缩水——**这正是补六行的必要性** |

### 2.8 门禁风险核对（本次审计的写入面）

- 本次仅新增 `doc/20-文档同步审计.md`，不改 README/KEY-GUIDE/TROUBLESHOOTING/ARCHITECTURE——**INV-65（README 引 doc/08/09）不受触碰**。
- 后续按 §3 计划实施时的高危点：①README 任何改动保住「更多文档」段与版本三处一致（INV-63：package.json / index.ts / doctor.ts——文档不涉）；②ARCHITECTURE 改「65 条」为「79 条」时同步 §5/§7/§8 三处，防 grep 半改；③doc/09 补行时沿用既有列格式（版本｜主题｜验收｜完成记录）。

---

## 3. 更新计划（按优先级）

### P0（错误陈述/用户会被误导——先做）

| 文档 | 段落 | 怎么改 |
|---|---|---|
| ARCHITECTURE.md | §6 边界第 2 条 | 「不做坐标 grounding」改写：语义 AX 优先（tier-1）+ 坐标形态 {x,y} 为 tier-3 cgEvent 兜底（canvas/Electron/VLM 链尾），坐标取 snapshot rect 中心；引 v1.11 T7 |
| ARCHITECTURE.md | 标题/§11/落款/§5.8/§7/§8 | 「v1.0 架构概览」→ v1.13；65→79（三处）；测试数 1961 TS/207 Rust；§8 补 INV-66..79 六行分类（v1.8 wave/生命周期/静默/1.7.0 迁移） |
| TROUBLESHOOTING.md | §1 + §2.7 + Q2 | 「32 项」改为以实跑为准（或精确复核）；§2.7 根因/修复文案重写为 1.7.0 原生语境；Q2 改「锁版自动经 npx 拉取，勿手动全局安装」 |
| TROUBLESHOOTING.md | 新增 §2.10-2.12 + FAQ | 三新排障项：`tcc_event_synthesis_denied`（macOS 15+ Event Synthesizing 授权路径）、Steel 停摆（unknown 自动降级 + 退出 3s 上界）、Chrome 版本 skew hint 释义；另加 `vlm_inference_only` 释义与「find 纯 ref 升级后报 invalid_params」FAQ |
| doc/09 排期 | §1 总览表 | 补 v1.8/v1.8.1/v1.9/v1.10/v1.11/v1.13 六行（沿用现列格式，各含一句主题 + INV/测试数快照 + commit/tag 佐证；素材直接取本文档 §1 与 README L195 更新日志段） |
| KEY-GUIDE.md | §C macOS | 补一句：macOS 15+ 用坐标鼠标（拖拽/坐标点击）需「Event Synthesizing」授权（Accessibility 可兜底），doctor #21 专查 |

### P1（能力/配置可见性——次做）

| 文档 | 段落 | 怎么改 |
|---|---|---|
| README.md | 「高级调优」清单 | 加一行 `LASSO_PROXY`（浏览器出口代理；只影响无头/Steel，登录态 Chrome 不动；详见 KEY-GUIDE E）——与 L368-369 既有 idle 两行并列 |
| README.md | 「搜一下」段 | 加一句 freshness（说「最近一周」自动带时效过滤，v1.11）；「看一个页面加载了什么」段加一句 v1.11 原生化（代理/TUN 网络下也完整） |
| README.md | 「抓公开页」段 | stealth 注补 v1.11/v1.12 两句：启动级 UA/Accept-Language 一致 + macOS 默认指纹与宿主对齐；正文提 20+ 站专用抽取器/表格保真（正文-表格对齐） |
| README.en.md | L193 版本段 | 补 v1.12/v1.13 两段（对照 zh L195 直译）；「Current version v1.13.0」 |
| README.en.md | Advanced tuning | 补 LASSO_PROXY + 三个 idle/mode bullet（对齐 zh） |

### P2（翻译债——批处理）

| 文档 | 怎么改 |
|---|---|
| README.de/es/fr/ja/pt/ru | 以补齐后的 README.en 为模板整体重译（六份共用同一 diff）；若短期无人力，先在每份文首加注「本文档滞后，以中/英版为准（v1.13）」止损——**费用表缺 Steel 行优先补**（免费项缺位 = 误导付费） |

### P3（形态优化——可延后）

| 文档 | 怎么改 |
|---|---|
| README.md L195 | 900+ 字单段更新日志拆为按版本的列表（v1.8/v1.9/v1.10/v1.11/v1.12/v1.13 各 2-3 行 + 折叠详情），提升用户向可读性 |
| TROUBLESHOOTING §6 反馈节 | 附 doctor JSON 的建议保留；Q5 内部 parse 链接改指 README 诚实边界段 |
| KEY-GUIDE F 节 | doctor 检查项数改为动态表述 |

### 验收门（实施后）

1. `npm run check-invariants` 79 全绿（README 改动后必复核 INV-65）。
2. `grep -c "65 条\|65 invariants\|32 项" ARCHITECTURE.md doc/TROUBLESHOOTING.md` → 0（旧数字清零）。
3. `grep "LASSO_PROXY" README.md` 高级调优段命中 ≥1。
4. doc/09 §1 表 `grep -c "^| \*\*v1\." ` ≥ 13（v1.0..v1.13 含 v1.8.1）。
5. 六语版（若走止损注记路线）：每份文首含「v1.13 / see zh+en」字样。

---

## 4. 矩阵要点（返回摘要）

- **两处「过时且错误」（🔴 最高优先）**：ARCHITECTURE §6「不做坐标 grounding」与 v1.11 坐标鼠标直接矛盾；TROUBLESHOOTING §1「32 项」+ §2.7 network 根因停留在 PerformanceObserver 时代。
- **六行排期缺口**：doc/09 §1 阶段总览止于 v1.7，v1.8/v1.8.1/v1.9/v1.10/v1.11/v1.13 六行全缺。
- **一个新 env 未入中文 README 配置面**：LASSO_PROXY 只活在更新日志与 KEY-GUIDE，README「高级调优」清单缺行（en 版则连同三个 idle/mode env 共四个全缺）。
- **三新排障项全缺**：macOS 15+ Event Synthesizing 输入权限、Steel 停摆行为、Chrome 版本偏差（skew hint）释义——另加 `vlm_inference_only` 与 find-纯-ref-breaking 两条次新项。
- **六语 README 冻结在 v1.2/v1.3 era**（2026-07-23，commit 86a7cc0）：无 stealth/Steel/机器复用/freshness/桌面 act——费用表缺「Steel 免费」行属误导级。
- **README.md 正文三处轻滞后**：搜一下段缺 freshness 一句、抓公开页段缺 v1.12 双激活与 stealth 升级两句、网络审计段缺原生化一句。
- **KEY-GUIDE 仅两小缺口**：C 节缺 Event Synthesizing 权限说明；F 节「38 项检查」计数漂移。
- **零变化项（无需动）**：费用一览（无新增付费）、版本 badge（动态）、INV-65 引用段、KEY-GUIDE env 全表。

---

## 5. 审查段（round-docs-review，2026-08-17）

> 审查员独立于写作 agent；方法 = 逐条声称 grep src/ / rust-helper/src/ 对源码实证（参数名 / env 名 / 默认值 / 字面量 / 行为锚点），数字类声称实测（cargo --list / defuddle 注册表清点）。

### 5.1 事实核对（30 项，全部对照源码）

**v1.11（11 项，全中）**：freshness `z.enum(["day","week","month","year"])` 且入 cache key（`search.ts` L104/L219）✓；machine_mcp / DDG 兜底透传（`MachineMcpSearchChannel.ts` L141-143 / `search.ts` L435-436）✓；英文零 Key 兜底 Baidu→DDG（`serp/extract.ts` L59-60）✓；`LOCKED_CDP_MCP_VERSION="1.7.0"` 锁版 + `npx -y chrome-devtools-mcp@<ver>` 自动拉取 ✓；四通道 spec 显式 `--no-usage-statistics` ✓；启动级 `--chromeArg=--user-agent=/--viewport=`（`HeadlessChannel.ts` L88-97）✓；network 原生 `list_network_requests` + method/status 全量 + TUN 完整（`cdp-actions.ts` L159-166）✓；desktop act AXAPI 真执行 + 写后读回（`rust-helper/src/ax.rs` L518-580）✓；坐标鼠标 + skeleton ✓；LASSO_PROXY 三通道语义（headless `--proxy-server` / Steel `proxyUrl` / logged_in 永不读，`index.ts` L427/L637 + `config.ts` L280）+ doctor `proxy_config` 回显（只回显不探活）✓；doctor `#21 tcc_event_synthesizing` 编号与 `#36/#37/#38` 全部对上 ✓；stealth 值域锚点 Chrome 151 shipped 2026-07-28（「2026-07 时代」表述准确）✓。

**v1.12（9 项，8 中 1 修正）**：表格/数学保真 + Obsidian 方言注释（`markdown-extractor.ts` L114/L150）✓；相对链接绝对化（同文件 L37/L112）✓；mac_chrome 宿主对齐 + skew≥2 提示（`doctor.ts` L1787-1827）✓；`truncated:true` 仅真剪枝时出现（`AxProvider.ts` L165）✓；find 附 actions 清单 ✓；Electron 降级「聚焦+cmd+a 全选+逐字符 ASCII+整值替换+值读不回跳过读回」（`ax.rs` L521/L587/L619-621/L641/L705）✓；拖拽 200ms+12 点插值（每点 16ms）+100ms 沉淀（`cgevent.rs` L339-448）✓；VLM 真执行 + 诚实 unknown（`vlm_inference_only:no_coordinate_action/execution_failed` 字面量在 `ScreenshotVlmProvider.ts` L221/L264）✓；stdin-EOF 收尾（`index.ts` L1259 T2-12；「此前悬挂 1 小时」对应旧 zombie 阈值 `3_600_000`）✓；**「28 站」失实→修正**（见 5.2①）。

**v1.13（6 项，全中）**：Accept-Language 经 `--chromeArg=--accept-lang=` 成活数据 + `navigator.languages` 档案感知（`stealth-profiles.ts` L63-64 + `StealthEngine.ts` T3-1）✓；VLM region 坐标平移回全局（`offsetVlmActionsByRegion`，`ScreenshotVlmProvider.ts` L380-389）✓；find 拒纯 ref（zod 删 ref 字段 + Rust `invalid_params` 双保险，`tools/desktop.ts` L107-115）✓；Steel 3s 双层上界（`AbortSignal.timeout(3_000)` ×2，`SteelChannel.ts` L105/L360；「~5 分钟」对应实测 ~301s）✓；VLM 档 macOS 输入合成缺失明确报「需要授权」（`tcc_event_synthesis_denied` → didnt，L244-252）✓。

**一致性/门禁（4 项）**：版本单一真源三处 `1.13.0`（package.json / index.ts L207 / doctor.ts L154）✓；9 个 README「当前版本 v1.13.0」一致 ✓；ARCHITECTURE 测试数 207 Rust 经 `cargo test -- --list` 实测确证（1961 TS 与 doc/19 round4/round5 两次门禁记录一致）✓；inv-selftest 14 样本 ✓。

### 5.2 失实修正（3 类，52 处编辑面）

① **「28 个高频站点」数字失实**（README.md + 8 个翻译版各 3 处 + 本文档 4 处）：defuddle 0.19.2 extractor 注册表实测 **24 个站点专属 extractor**（+1 个 bbcode 格式助手，非站点）——28 无任何来源支撑（src 注释的「~28」同样是未验证近似）。已全部改为「20 多个 / 20+ / más de 20 / plus de 20 / 20 余り / более 20」（向下取整防版本漂移）；本文档 §1 C1 改记「~24」精确值。
② **「全部引擎通用」对 year 档绝对化失实**（9 处：8 README 正文 + KEY-GUIDE）：Bing v7 只映射 Day/Week/Month，year 档静默跳过（`BingChannel.ts` L240-244；工具描述自身如实披露）。已补「仅 Bing 无『年』档粒度会自动跳过」诚实边界（各语言对应句）。
③ **README 内部术语泄漏**（17 处）：更新日志段「wave1」（8 个 README）与「caller-tier」（7 个 README）属内部过程术语，违反「README 零技术内部」铁律。已改为「全量实测」「调用方配额」（各语言对应词）。改后全 9 README `grep "wave1|caller-tier"` 零命中。

### 5.3 风格与门禁终态

- README.md 全文 `INV/parse/断言/wave/caller-tier/tier-N/T 编号` 零残留 ✓；用户向语言（能力→怎么做→得到什么）保持 ✓。
- `npm run check-invariants` → **All 79 invariants passed**（修正后复跑）✓；`git status` 全部改动为 11 个 .md 修改 + 1 个 .md 新增（doc/20），**零 src/test/rust-helper/scripts 触碰**（`git diff --stat -- src/ test/ rust-helper/src/ scripts/` = 0 行）✓。
- 验收 grep 复跑：旧数字「65 条/32 项」清零 ✓；README 高级调优段 `LASSO_PROXY` 命中 ✓；`LASSO_PROXY` 在全部 9 个 README 各 2 处、KEY-GUIDE 5 处拼写一致 ✓；INV-65「更多文档」段（doc/08 + doc/09 引用）健在 ✓；doc/09 §1 v1.x 行数 14 ≥ 13 ✓。

### 5.4 预存观察（非本轮引入，不属修正面）

- README「更多文档」段的 `doc/08-…` / `doc/09-…` 相对链接在 lasso 仓内无对应文件（两文档实际位于上层工作区 `../doc/`）——外部 GitHub/npm 用户视角为死链；ARCHITECTURE 用 `./../doc/` 本地可达但越出仓根。INV-65 只查文本引用不查文件存在，故门禁绿。建议后续发布前决定：纳入仓内 or 改绝对链接。
- README 更新日志单段 900+ 字形态（doc/20 §P3 已列）本轮未拆分——非失实，延后。

### 5.5 终判

**PASS（带 3 类修正）**。核对 30 项 v1.11-v1.13 能力声称：27 项与源码逐字相符，3 类失实/泄漏已全部修正（站点数 28→20+、freshness 全引擎绝对化→补 Bing year 边界、内部术语 2 个清除）；门禁 79 全绿、零代码改动、9 语版本一致。文档面与 v1.13.0 源码事实对齐，可随发布。
