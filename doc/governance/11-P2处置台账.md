# 11 · P2 处置台账（wf_c02ef3df 全量裁决）

> **时间**：2026-08-31 ｜ **基线**：v1.18.7 @ 8b60087（CI 双绿，146 files / 2455+1 skipped / 84 INV / readme-sync）
> **来源**：wf_c02ef3df-77a 五路全面重审（archA / archB / contract / redundancy / perf）+ 三轮回炉定向复审。签发员终裁只合并了 5 条 P2，明示以五路完整报告为准——本台账即全量清偿。
> **口径**：原始 P2 = 56 条（首轮五路 34 + 复审轮 22），去重合并 = **46 项**。三档裁决：**fixed**（本轮修，行为改动均配自动化测试）/ **deferred**（留档，给理由与建议时机）/ **rejected**（证据表明非问题或已被上轮修复覆盖）。
> **纪律**：门禁三件套全绿后单 commit（不 push，主循环统一）；最小面修复；审查标准 03_审查测试清单 §0.3 证据阶梯。

## 0. 总览

| 档 | 数量 | 说明 |
|---|---|---|
| **fixed** | 20 | 本轮落地（见 §1）；行为/工具面改动 8 处全部配自动化测试真跑 |
| **deferred** | 25 | 留档（见 §2），每条给一句话理由与建议时机 |
| **rejected** | 1 | r1re-archA#5——r3 轮（37f5c58）已改真，证据在案 |

原始分布：archA 5 + perf 7 + redundancy 9 + archB 9 + contract 4 = **34**（与上轮 p2count=34 逐条对上）；复审轮 r1re-archA 5 + r1re-contract 2 + r3-arch 4 + r2re-contract 3 + r3re-archA 6 + r3re-verify 2 = **22**。去重合并规则：url-safety（red#4=archB#5）、doctor.ts 体量（archA#8=r1re-archA#2）、types 死字段（archB#6=r3-arch#5=r2re-contract#4）、执守日志（contract#7=r3re-verify#1）、SubprocessManager 双族（archB#4=r3-arch#3=r3re-archA#5）、INV-85 两提案（r1re-archA#4+r3re-archA#4）。

## 1. fixed（20 项）

| # | 来源 | 发现（一句话） | 修复（本 commit 内） | 测试证据 |
|---|---|---|---|---|
| F01 | archA#4 | CDP 端口 9222 三处独立实现，doctor 裸 parseInt 绕过 parseCdpPort 守卫（垃圾值→NaN 下渗 checkCdp9222） | doctor.ts 改用 `parseCdpPort(process.env.LASSO_CDP_PORT)`（NaN/越界→回退 9222+warn）；launch-chrome 侧**不改**——INV-64 禁 launcher import config（../config 在禁引清单），字面量 9222 是 Chrome 生态外部约定（R-INT-08），在排期行留注 | doctor.spec 新增白盒锚点测（parseCdpPort 形态 + 禁 parseInt 复辟）；parseCdpPort 守卫行为由既有 config-file.spec 覆盖 |
| F02 | archA#5 | ARCHITECTURE.md §2 分层图把 search-local 画进 Channel Layer，与 §1/§3.1「工具直连」矛盾 | 图内注明 search-local 不在此层（指向 §1/§3.1） | check-readme-sync E 项链接存在性（doc 改动零链接新增） |
| F03 | archA#6 | fetch_url 用户面描述泄漏内部实现锚（SubprocessManager.acquireHttpClient→r1 迁池后残留 util/http-pool 路径） | 描述改「shared undici keep-alive connection pool」（去内部路径，信息量不减） | **新增** test/unit/tool-descriptions-no-leak.spec.ts：4 类泄漏 pattern 反向断言 + keep-alive 语义保留断言 |
| F04 | archA#7 | network include_bodies 三方口径不一致（转发/头注释「不实装推v0.6」/描述「NOT wired, detail via upstream tool」） | 以 cdp-actions.ts doNetwork 白盒结论（上游原生工具即时返回、值被忽略、字段保留 zod 契约稳定）为单一真源：头注释+描述两处对齐，删过时「推 v0.6」承诺 | tool-descriptions-no-leak.spec：include_bodies 口径=「value is ignored」+ 禁「detail via upstream tool / 推 v0.6」回潮；network.spec 既有 8 测继续守透传行为 |
| F05 | perf#3（部分） | CI 矩阵 [20,22] 但 HAS_NODE_SQLITE 需 ≥23 → search-local 40 测两节点全 skip（v1.17 特性 CI 零覆盖）；node 24 缺席；spec 头两段注释互相矛盾 | 矩阵改 **[20,22,24]**（20=engines 下限锚点/24=真覆盖）；订正 spec 头注释为整文件门控的真语义 | CI yml 改动（无工具行为）；注释订正随 search-local.spec 40 测在 node 24 真跑；describe 拆分部分 deferred（D16） |
| F06 | perf#3 附带（签发员线索四） | checkout@v4 + setup-node@v4 = node20 runtime，GitHub 已弃用（2026-06 起强制 node24） | 升当前最新大版本：**actions/checkout@v7**（v7.0.1=Latest）/ **actions/setup-node@v6**（v6.4.0；破坏面=自动缓存限 npm，本仓本就显式 cache:npm）——2026-08-31 releases 页核真 | 无工具行为（CI 面）；远程结论待主循环 push 后回查（yml 注释已留此话） |
| F07 | red#2 | doc/README.md 索引停在 v1.18.5；bugs/02 仍标「根治未做待裁决」而 fb4f790 已根治（§9 闭环在档） | 头部更新至 v1.18.7/P2 轮；bugs/02 两处改「已根治（v1.18.6 §9）」；usage/02、testing/01、architecture/02、ARCHITECTURE 行同步；governance/11 档入表 | npm test 内 check-readme-sync 全绿 |
| F08 | red#3 | TROUBLESHOOTING §2.13 死链 ./KEY-GUIDE.md（实为 01-KEY-GUIDE.md，含锚点整链死） | 改 ./01-KEY-GUIDE.md#steel_endpoint--...（锚点在档核真） | check-readme-sync E 项链接存在性 |
| F09 | red#4 + archB#5 | src/util/url-safety.ts 全仓零消费，且与 ssrf-guard.ts 同语义重复（v0.1 遗留死层） | 删除文件（git 历史留档） | tsc 编译过 + 全量测试绿（零引用编译即证） |
| F10 | red#5 | forest-types.ts isRootRef/isRootKind/isRootInfo 零消费者，头注释「用于单测+doctor 自检」为假 | 删三函数+假注释，留墓碑说明（类型保留，wire-shape 守护由 interact_* 契约测承担） | tsc 编译过 + 全量测试绿 |
| F11 | red#6 | fuseDelayMs 死参数：v1.10 起实现零读取，注释仍称「默认 1.5s」，5 处测试传 1 是 placebo | 删接口参数+假注释；删 5 处测试实参+标题提法；:438 注释改真 | launch-chrome.spec / p1-visible-chrome-lifecycle.spec 全绿（62+ 测，含保险丝行为测） |
| F12 | red#7 | 运行态残留入库入包：profiles/profiles.json 与 .devteam/devteam.db git tracked；profiles 进 npm 包 | `git rm --cached` 两文件；.gitignore + .npmignore 加 profiles/（.devteam 两文件已有/半有） | `npm pack --dry-run` 复核发布面（见 §3 验证记录） |
| F13 | red#8 | 排期表止于 v1.18.4，缺 v1.18.5/6/7 决策行 | 补三行（31cd0cc/fb4f790/8b60087+P2 轮素材），footer 增补注同步 | check-readme-sync |
| F14 | red#9 | ARCHITECTURE.md 停在 v1.17.2（2253 测试/135 文件/81 INV；governance/07 §8 自挂遗留 P0） | 头部 v1.18.7；§9 计数改真：**84 INV / 样本覆盖 20/84（26 pin 红翻转）/ 2467 passed+1 skipped / 147 文件**（本机实跑数）；分层图见 F02——遗留 P0 销账 | npm run inv-selftest 输出对表（20/84、26 pins）；全量 npm test 输出对表 |
| F15 | red#10 | INV-64(c) 顶级 auto-execute 守卫只枚举 runLaunchChromeCli/runReplayBaselineCli 两旧入口，新 CLI 入口潜伏缺口 | 枚举改通用形态 `/^(void\s+)?run[A-Z]\w*\s*\(/`（一次覆盖现在与未来全部 run* 入口；声明/赋值形态不误伤） | check-invariants 84/84 真跑 + 全量测试绿（gate 行为改动，绿即证） |
| F16 | archB#6 + r3-arch#5 + r2re-contract#4（签发员线索一） | types.ts 内部死可选字段：ScreenshotSpec.element、BrowseOptions.wait_until/timeout_ms、ChromeStopOptions.all（r2 清了 zod wire 面，内部 TS 类型按裁决留档） | 四字段全删 + BrowseChannel.ts:662 的死写 timeout_ms 一并删（写而无读）+ parseChromeStopArgs 死赋值删 + chrome-ledger.spec 断言改真 + 3 处白盒锚点同步 | tsc 编译过；chrome-ledger / lifecycle-teardown / p1-visible-chrome-lifecycle / browse 全链测试绿 |
| F17 | archB#9（签发员线索二） | doctor.spec「checks.length>=10」全量套件并行负载下超默认 5000ms（隔离复跑绿） | describe 加 `{ timeout: 15_000 }`（照 doctor-v17-integration.spec describe timeout 45s 先例；全量真跑通过 3.7s） | 全量 npm test 该文件 22/22 绿 |
| F18 | contract#6 + r2re-contract#3 | 执守日志吞没×3：ensureHideEnforcerRunning 的 writeEnforcerPidfile(pid) 不传 logFn；runHideEnforcerCli 自写不传；chrome-hideshow-cli 默认拉起不传（同库 launch-chrome 传了——两调用点不一致） | enforcer.ts 两处透传 logFn（CLI 主体统一 cliLogFn）；chrome-hideshow-cli 默认分支传 stderr 结构化 logFn | **新增行为测 6c**：pidfile 写失败（ENOTDIR 注入）→ hide_enforcer_pidfile_error 流经 logFn 且 spawn 仍成功；白盒锚点 2 处更新（含 hideshow-cli logFn 形态） |
| F19 | r1re-contract#0 | 停机路径 closeAllHttpAgents() 裸 await，缺同段兄弟步统一的 3s race（Agent.close 等 in-flight 完成，原注释「仅销毁空闲 socket」不精确） | 包 Promise.race 3s + 注释改真 | lifecycle-teardown.spec 既有 6 测（3s 上界断言族）全绿 |
| F20 | r1re-contract#1 + r3re-archA#2 | 两处 L0 注释谎言：index.ts:466 指已迁走的 defaultHeadlessProfileForHost 旧居；SearchCache.count() docstring 称「doctor 用」而 doctor 从不调用 | 两处注释改真（指向现居/如实标注测试用公开面） | 白盒锚点（p1-visible G1 族）不受影响；全量绿 |

## 2. deferred（25 项）

| # | 来源 | 发现 | 留档理由（一句话）与建议时机 |
|---|---|---|---|
| D01 | archA#8 + r1re-archA#2 | doctor.ts 3060 行聚合 8 域 35+ check；LASSO_VERSION 住诊断模块 | 每个 check 是小纯函数无内缠绕，拆分是纯机械但大面积搬移；下次功能性扩张 doctor 时照 desktop-doctor-checks 先例分域拆（版本常量可先迁 package-info） |
| D02 | perf#1 | watchdog 1.5s tick 默认 spawnSync ps（1s/条）+ reaper stopFn 递归 spawnSync pgrep | 异步化触及 4 文件签名放宽（string|Promise 兼容手法）+ 注入面，超最小面；与 D03 同批做（下次动 launcher 生命周期时） |
| D03 | perf#2 | 双执守重叠：server 恒启 in-process watchdog + enforcer 自带同款，enforcer 不探测 server 在世 | 需心跳/让位设计（成本恰在 P27 病根上但 reassert 幂等、正确性无碍）；建议与 D02 合并为「执守生命周期」专项 |
| D04 | perf#4 | doctor 路径 execFileSync Chrome --version（3s 上限，常态 100-400ms） | 改 execFile+缓存是行为改动需配测（CI 无 Chrome）；下次动 doctor 时与 D01 一并 |
| D05 | perf#5 | readOutputPage 每页全量 readFileSync spill（≤16MiB）+全量解码切 16-64KiB | 精确读需 utf8 边界处理重构 output-envelope 核心；分页读大 spill 是低频路径；下次动 output-envelope 时做 Buffer 存读 |
| D06 | perf#6 | mutation 抽样 3/4 存活（chrome-history 窗口左移/watchdog 复位删除/chrome-stop tab 分支） | 三处断言补齐各需理解测试语义（合计>20min×3）；建议立「断言补强」小专项与 Stryker 基线一起做（03 §2.1.4 起点阈值 ≥80%） |
| D07 | perf#7 | ~/.cache/lasso/<run_id>/ 328 目录零清理（注释承诺「退出便于清理」但清理代码不存在） | 磁盘增长缓慢非紧急；建议在 doctor 或 shutdown 钩子加 mtime>N 天清扫（模式同 chrome-touch） |
| D08 | archB#1 | pid 探活+ps cmdline 原语 launcher 内 5 处内联 | 纯机械收敛但触及 4 文件+5 测试注入点，收益<回归风险；下次动 launcher 时抽 launcher/pid-probe.ts |
| D09 | archB#2 | 磁盘 JSON 台账骨架双实现（chrome-ledger vs desired-hide-state + pidfile 变体） | 两实现各自带契约测、行为稳定；再动其一时抽 json-ledger.ts（shape guard 回调注入） |
| D10 | archB#3 | 60s 限频两套窗口算法（RpmLimiter 滑动窗 vs CallerTierTracker 翻转窗）+ 自辩引用错误范式锚 | DEFAULT_CALLER_CAP=Infinity 下无行为差；下次触及任一侧时按 reviewer 处方收敛（RpmLimiter 按 callerId 键控） |
| D11 | archB#4 + r3-arch#3 + r3re-archA#5 | SubprocessManager 双进程族平行实现（spawn-backoff/mergedEnv/isAlive/kill 各两份，公有面 17 方法） | 两族 kill/alive 有真实类型差异（McpClient 线协议 vs 裸 ChildProcess）；下次触及抽 mergeSpecEnv 私有 helper + 退避常量单源 |
| D12 | archB#7 | UiChannel 零成员抽象层（v0.1 占位，承诺 v0.3 未兑现） | 删除需动 INV-2 措辞+3 处 extends，成本>收益；作为「占位抽象未兑现」案例记录，防新增同类 |
| D13 | archB#8 | registerSearchTool 单函数 ~483 行、12 个位置可选参数 | 本质复杂度（engine 三路+缓存+富化）；下次动 search.ts 时收拢 SearchToolDeps 对象+分支抽函数 |
| D14 | contract#4 + r2re-contract#2 | search 降级链终局可返回 outcome=worked+count=0（「已验证交付」语义被 0 结果打破） | 语义设计题：真无结果 vs 抓取失败需「无结果页」探测信号区分，盲改会把真无结果也变 unknown 触发多余降级；下次动 serp 引擎时与 producer 契约一起设计 |
| D15 | contract#5 | desired-hidden/launched 粘滞账多写者 lost-update（tick 全量重写 vs CLI 追加写） | 触发需 tick 内剔除与新写并发（窗口窄、后果是执守自退非误伤）；改定向删除需动 watchdog 主循环；建议与 D03 专项合并 |
| D16 | perf#3（describe 拆分部分） | search-local.spec 不依赖 sqlite 的 4 组（纯函数/mdfind/分发/装配）被整文件门控连坐 | node 24 入矩阵后 CI 覆盖缺口已闭（F05），拆组只增 node20/22 跑量；下次动 search-local 时拆 |
| D17 | contract#7 + r3re-verify#1 | 独立执守 stdio:"ignore" 丢弃全部结构化日志（P27 观测点在 enforcer 形态不可观测） | 需 spawn stderr 落盘设计（~/.cache/lasso/hide-enforcer.log + doctor 读它）；F18 已把**进程内**日志吞没修掉，spawn 面留档与 D03 专项合并 |
| D18 | r1re-archA#1 | LoggedInChannel 同文件两份「后台 tab id-diff 归因」实现（~45 行同构） | 触及 logged_in 静默性核心路径，reviewer 自荐留档下轮与测试一起动 |
| D19 | r1re-archA#3 | serp/http-serp.ts 是 5 个 markdown 消费点中唯一静态 import（INV-66 守卫面外） | 修法①需把导出的同步函数 serpHtmlToSnapshotText 转 async（涟漪>收益）；修法②动 INV-66 白名单；下次动 serp 引擎时一并 |
| D20 | r1re-archA#4 + r3re-archA#4 | INV 守卫面缺口：search/serp→browse 跨域 import 无白名单 INV；全局 value-import 无环无 INV 断言 | 新增 INV 涉及 84→85 计数面与文档面联动（多处「84 INV」引用）；当前零违规纯防回潮，建议下轮 INV 扩容时一次性加（白名单+无环合并一条） |
| D21 | r3-arch#2 | browse→channels 类型反向边（StepEngine 持具体类 BrowseChannel 作回调类型）无 INV 覆盖 | type-only 环无运行时危害；方向：复刻同文件 HighRiskGateLike 手法定 ChannelLike 接口，与 D20 同批 |
| D22 | r3-arch#4 | browse 族 6 schema 手工「同构」维护（一个概念改动触及 11 文件） | reviewer 自判维持现状（强行统一基础 schema 是 01 §3.2 错误抽象风险）；若出第三轮同类清扫，抽公共 expect 子对象这一最小共享面 |
| D23 | r3re-archA#1 | 顶层 options.expect 的 selector/url_contains/gone 三个死 wire 子字段（doWait 只消费 text/timeout_ms） | zod 收紧=工具 schema 面变化，需按 r2 三件套模板（schema+描述+守护测）独立一轮；r3 注释已诚实标注消费面（部分偿付） |
| D24 | r3re-archA#3 | machine_mcp 路径 region 参数无线上效果（producer schema 有 location 未映射） | 接 wire 需先 L2 实证 producer location 值域，避免再造 count 式死参数（reviewer 默认留档）；下次核 machine_mcp 契约时一并 |
| D25 | r3re-verify#0 | considerAutoHide 是唯一对台账 pid 施 osascript 前不做 verifyOwnership 的路径（护栏③单源） | 默认 off + hide 可逆 + 触发前提刁钻（pid 复用×port 占用组合），reviewer 自判留档不阻断；处方在档（3 行同 watchdog :134 同款） |

## 3. rejected（1 项）

| # | 来源 | 发现 | 证据 |
|---|---|---|---|
| X01 | r1re-archA#5 | types.ts:168-169 与 browse.ts:97 标注 steps/expect「v0.1 忽略」陈旧说谎 | **已被 r3 轮（37f5c58）修复**：当前 types.ts:171-172 注释为真实语义（「steps 非空→StepEngine.runChain 分流」「action=wait 消费（doWait 读 expect.text，必需）」），browse.ts:97-99 同步改真——本轮白盒核证，无需再动 |

## 4. 签发员线索处置

| 线索 | 处置 | 证据 |
|---|---|---|
| 一：types.ts 孤儿字段清理 | **fixed**（F16：四字段+死写+断言+锚点全链） | tsc + 4 spec 文件绿 |
| 二：doctor.spec 5000ms flake 抬 15s | **fixed**（F17，照 doctor-v17 describe-timeout 先例） | 全量跑 doctor.spec 22/22（3.7s） |
| 三：scripts/tmp-e2e-review-driver.mjs 未跟踪件 | **无需处置**——文件已不存在（scripts/ 仅 4 个 tracked 文件；git status 净；r1-redundancy 报告时它已注明 untracked+npmignored 零发布面影响，后被上一轮清掉） | `ls scripts/` + `git status --short` 本轮核证 |
| 四：CI actions v4 Node20 弃用 → 升最新大版本 | **fixed**（F06：checkout@v7 + setup-node@v6，releases 页 2026-08-31 核真：checkout v7.0.1=Latest / setup-node v6.4.0=Latest） | yml diff；远程 CI 待 push 回查（注释留话） |

## 5. 门禁与验证记录（本 commit）

- `npm run build`：exit 0（tsc + ad-hoc 签名链正常）。
- `npm test`：**147 files / 2467 passed + 1 skipped**（1 skip = linux-only ResourceMonitor macOS 对称跳过）；新增 test/unit/tool-descriptions-no-leak.spec.ts（3 测）+ enforcer 6c 行为测 + doctor 白盒锚点测。
- `npm run check-invariants`：**84/84 PASS**（INV-64(c) 通用化后无假红）。
- `npm run inv-selftest`：样本覆盖 20/84，26 pin 红翻转，工作树零污染。
- `npm pack --dry-run`：profiles/ 与 .devteam/ 均不在发布清单（F12 复核）。
- 行为改动↔测试对照表见 §1 末列（8 处行为/工具面改动全配自动化测试）。

## 6. 遗留提醒（给下轮）

1. **CI 远程结论**：checkout@v7 / setup-node@v6 / Node 24 矩阵的远端绿灯待主循环 push 后回查（yml 注释已留话）；若 runner 侧不兼容（预期不会——ubuntu-latest 自动更新），降级路径 checkout@v5/setup-node@v5。
2. **D20/D21 建议合并立 INV-85**（search/serp→browse 白名单 + 全局 value-import 无环 + StepEngine ChannelLike），一次过 84→85 计数联动。
3. **D03+D15+D17 建议合并为「执守生命周期」专项**（双执守让位/账本定向删/enforcer 日志落盘三点同根）。

| P2-追加-12 | CI Node24 矩阵：linux node:sqlite 运行时漂移（同 22 系签名：require 成功+API 行为漂移→search-local did`nt 假失败；mac node24 全绿）| deferred | 2026-08-31 试加即回退为 [20,22]；search-local 40 测由本地 node24 门禁承担；待 Linux 调试环境（或 node:sqlite 稳定化）再攻 |
