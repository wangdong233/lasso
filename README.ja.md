<h1 align="center">Lasso</h1>

> Claude Code の「外の世界をすべて掴む取っ手」——検索、ウェブ収集、ログイン済みページの収集、デスクトップ操作まで、すべてが一言で完結。
> カウボーイの投げ縄——どんなインターフェースでも絡め取る。

<p align="center">
  <img src="https://img.shields.io/npm/v/lasso-mcp">
  <img src="https://img.shields.io/badge/license-MIT-green">
  <img src="https://img.shields.io/badge/MCP-compatible-purple">
</p>

**Claude Code に Lasso を一度インストールすれば、以降は検索も、ウェブ収集も、ログイン済みページの収集も、デスクトップ操作もすべて一言で済みます。** 毎週検索したり、ページを取得したり、デスクトップアプリを操作したりする人で、用途ごとに別ツールを揃えたくないなら、これ一つ入れて Claude に任せきりにできます。

[media-gen-mcp](https://github.com/wangdong233/media-gen-mcp)（画像の取っ手）の双子星：「すべての画像操作を一つの MCP に」↔「外の世界とのあらゆる相互作用を一つの MCP に」。

<div align="center">

[简体中文](README.md) | [English](README.en.md) | [Deutsch](README.de.md) | [Español](README.es.md) | [Français](README.fr.md) | **日本語** | [Português](README.pt.md) | [Русский](README.ru.md)

</div>

## 目次

- [言ってみる・結果が返る](#言ってみる結果が返る)
- [💰 費用の総覧](#-費用の総覧)
- [60秒ではじめる](#60秒ではじめる)
- [何ができるか](#何ができるか)
- [インストール](#インストール)
- [設定](#設定)
- [プライバシーとセキュリティ](#プライバシーとセキュリティ)
- [トラブルシューティング](#トラブルシューティング)
- [こんな人に向け・むいていない人](#こんな人に向けむいていない人)
- [作者を応援する](#作者を応援する)
- [ライセンス](#ライセンス)

---

## 言ってみる・結果が返る

| 言うこと …… | 得られる結果 |
|---|---|
| 「rust の async エコシステムの最新動向を検索して」 | 構造化された検索結果（一つのエンジンが落ちても自動で次に切り替わります——ユーザーは何も感じない） |
| 「直近 1 週間の Claude Code の更新を検索して」（v1.11） | `freshness=week` で絞り込まれた時効性のある結果——クエリに日付を手書きしなくて OK |
| 「github.com のトップページの本文を取得して」 | 整った記事テキスト（ナビバーや広告・雑多な要素を除去——トークンを 30〜70% 節約；頻出 20 余りのサイトは専用抽出器で、表も構造を失いません——v1.12） |
| 「ログイン済みの自分の Jira を開いて ToDo を見せて」 | ログイン済みページのスナップショット（ローカルの Chrome を再利用——2FA は自分で通します） |
| 「このリンク切れてるから、アーカイブを探して」 | Internet Archive の最新スナップショット |
| 「今開いてる Finder ウィンドウのファイル一覧をちょうだい」 | デスクトップのウィンドウとコントロール一覧（スクリーンショットではなく意味ツリー；ツリーが切り詰められたら `truncated:true` とはっきり申告します——v1.12） |
| 「新規フォルダボタンをクリックして」「検索ボックスに XX と入力して」（v1.11） | デスクトップ操作が本当に実行される（AXAPI の意味的クリック／入力 + 結果検証；canvas／Electron は座標クリックへ自動フォールバック） |
| 「このページの全体スクリーンショットを撮って」／「PDF で保存して」 | ディスク上のファイルパス（チャットに巨大な画像データを垂れ流しません） |
| 「このページが読み込んだサードパーティのトラッカーを教えて」 | トラッカードメインごとの集計付きリソース一覧 |
| 「今操作できるものをすべてリストアップして」 | 統合された一つのリスト（ウェブページもデスクトップウィンドウも全部入り） |
| 「ダークモードをオフにして」 | 自動クリック・入力・ホットキー（結果検証付き——本当に実行されたことを確認します） |
| 「この JSON エンドポイントをただ取得して」 | 生バイトデータ（最速・最安） |
| 「このサイト、ちょっとアンチボットがあるみたい、試してみて」 | `browse_headless` にアンチ検出が内蔵（基本のボット検出は通過）——設定なしで直接スクレイプできるサイトが多数 |
| 「このサイト Cloudflare がいてスクレイプできない」 | クラウド Chrome によるアンチボット回避——**セルフホスト Steel（無料）** または browserbase／stagehand（有料・デフォルトはオフ） |
| 「Lasso の設定、合ってる？」 | ヘルスチェックレポート（足りないものを指摘してくれます） |

> 機能名を暗記する必要はありません。やりたいことを口で言えば、Claude が最適な手段を選びます。

---

## 💰 費用の総覧

Lasso 本体は **完全無料・MIT オープンソース** です。各機能の実際のコストは以下の通り：

| 機能 | 費用 | 備考 |
|---|---|---|
| Lasso 本体（MCP サーバー + すべてのコア機能） | ✅ 無料 | MIT オープンソース、永久無料 |
| 検索（Zhipu + Brave） | ✅ Zhipu なら無料で始められる | Zhipu はトークン課金（新規ユーザーには無料クレジットあり。マシンに Zhipu の MCP が設定済みならゼロ設定で利用可）。Brave は現在は有料プランで \$5/月 のクレジット込み（2026-02 から無料枠は廃止）。Lasso 自身も無料の実検索フォールバックを内蔵しているため、どこも設定しなくても検索は使えます |
| 公開ページのスクレイプ / スクリーンショット / PDF / ネットワーク監査 / 生バイト取得 | ✅ 無料 | ローカルで動作、キー不要、支払い不要 |
| ログイン済みページのスクレイプ（ローカル Chrome 再利用） | ✅ 無料 | ローカルで動作、キー不要、支払い不要 |
| デスクトップ操作（macOS / Windows / Linux） | ✅ 無料 | ローカルでビルド・実行、OS の認可だけが必要。署名付きの永続認可には Apple Developer アカウント \$99/年が **任意**（未署名でも動作——その都度再認可するだけ） |
| クラウドブラウザ · セルフホスト Steel（v1.6 新） | ✅ 無料 | ローカル Docker で Steel（Apache-2.0 オープンソース）を運用——**セッション単位の費用ゼロ + cookie はローカルから出ない**；`LASSO_ALLOW_CLOUD_BROWSER=true` + `STEEL_ENDPOINT=http://localhost:3000` が必要 |
| クラウドブラウザ · ホスト型（browserbase / stagehand） | ⚠️ 有料・デフォルトはオフ | browserbase はトライアル後は従量課金；stagehand はプログラマティックな実験チャンネル（MCP ツール入口なし）；**設定しなければ一切課金されません** |
| `browse_headless` のアンチ検出（v1.5 新） | ✅ 無料 | デフォルトで 16 層のアンチ検出を注入（UA / webdriver / webgl など）——基本的なボット検出の多くをそのまま通過 |

> 一言で言えば：**ホスト型クラウドブラウザ（browserbase / stagehand）を有効にしない限り、Lasso のコストはゼロ** です——検索には日常使いに十分な無料枠があり、セルフホスト Steel も無料です。

---

## 60秒ではじめる

### 30秒 · 一行インストール（設定ゼロ）

```bash
claude mcp add lasso -- npx -y lasso-mcp
```

Claude Code を再起動 → `/mcp` → `lasso ✓ Connected` と表示されれば完了。**インストールコマンドにキーは一切不要**——設定は別ステップ（次の層）で行います。

### 30秒 · 設定なしでもう既にできること

インストール直後、キーがなくても以下はすべて使えます（**ティア 1：設定ゼロ**）：

- **公開ウェブページ**のテキストを取得し、整った markdown に変換
- **ページ全体のスクリーンショット** と **PDF 保存**（ファイルパスを返す）
- ページが読み込む **サードパーティのトラッカー** を確認
- JSON API やファイルから生バイトを直接取得
- macOS のネイティブアプリを操作（Finder / Mail / システム設定など——システム設定で一度だけチェックが必要）

> 💡 **検索も設定ゼロで使える可能性があります**：マシンの `~/.claude.json` に Zhipu の `web-search-prime` MCP が既に設定されていれば、Lasso は起動時に自動検出して再利用します——`ZHIPU_API_KEY` を別途設定する必要すらなく、検索がそのまま使えます。`lasso doctor` で `#36 machine_search_mcp` が `pass` かどうか確認してください。

最初の一歩——Claude にこう言うだけ：

> 「example.com のテキストを取得して markdown にして」

### もっと欲しい？ 設定ファイルで追加（ティア 2）

- **検索** → `lasso config init` を実行して `~/.lasso/config.json` を作成し、Zhipu のキーを記入（[設定](#設定) を参照）
- **ログイン済みページのスクレイプ**（Jira / プライベート GitHub / 社内ネット） → `lasso launch-chrome` を一度だけ実行
- **macOS デスクトップの操作** → `lasso doctor` を一度だけ実行して認可を案内してもらう

各キーの取得方法や無料枠の詳細は [**キー設定ガイド**](./doc/KEY-GUIDE.md) を参照してください。

---

## 何ができるか

**ツール名**ではなく、**やりたいこと**ごとに整理しています。どれも一言で入力し、一言で結果が返ります。

### 検索

> あなた：「X を検索して」→ 構造化された検索結果

デフォルトは Zhipu（中国語に強い）。さらに Brave を追加してマルチソース化も可能（Bing の上流はすでに停止——設定キーは残るが自動スキップされます）。**いずれかのソースがレート制限や障害で使えなくなっても、自動で次に切り替わります——ユーザーは何も感じません。** 一つのプロバイダーの無料枠を使い切っても、全体が止まることはありません。

**ニュースやバージョン動向**のような時間感度の高い内容を調べたいときは、「直近 1 週間 / 直近 1 ヶ月の X を検索して」とそのまま言えば OK——時効フィルタ（day / week / month / year、v1.11）が自動で付き、クエリに日付を手書きする必要はありません。

### 公開ページのスクレイプ（ログイン不要）

> あなた：「example.com のテキストを取得して」→ 整った記事テキスト（3 段階の粒度を選択可能）

ナビバーや広告、サイドバーなどの雑多な要素を自動で除去——**トークンを 30〜70% 節約**（費用も抑える）。GitHub / Reddit / Hacker News / Wikipedia / Substack / Medium など**頻出 20 余りのサイトは専用抽出器**で処理され、表や数式の構造も失われません（v1.12）——本文中のリンクはすべてクリック可能な絶対アドレスになります。引用マーカーが必要（研究や RAG への投入に最適）なら、一言でモードを切り替えられます。

> **v1.5 以降、`browse_headless` はアンチ検出（ステルス）がデフォルトで有効**（UA 偽装 / `navigator.webdriver` 抹消 / webgl・plugins・codecs 偽装など十数層）。**設定不要で自動的に効きます**——「headless を検出する」サイトの多くがそのままスクレイプ可能に（v1.8 で注入が黙って無効になる欠陥を修正、現在は本当に効いており、注入失敗はログに正直に報告されます）。v1.11 からはアンチ検出が**ブラウザ起動レイヤー**で効きます：UA・ビューポート・言語がプロファイルで統一されて適用され、ネットワーク層の HTTP ヘッダーとページ JS が同じ値を見るため、もう矛盾しません。v1.12 からは macOS でデフォルトのフィンガープリントが**あなたのシステムと整合**（「UA は Windows、マシン特性は macOS だと白状」という矛盾を解消）。Cloudflare 級の強力なアンチボットにだけクラウドブラウザが必要です（下記「アンチボット回避」参照）。アンチ検出の効果を確認したい？ `lasso doctor --stealth-check` で creepjs の検出比較を見られます。

### ログイン済みページのスクレイプ（2FA があっても）

> あなた：「自分の Jira の ToDo を見せて」→ ログイン済みページのスナップショット

**ローカルでログイン済みの Chrome** を再利用——2FA は一度だけ自分で通し、あとは Lasso が引き受けます。プライベート GitHub リポジトリ、社内ネット、有料購読コンテンツなどでも機能します。

> 🔴 **レッドライン**：Lasso は **2FA / SMS 認証 / CAPTCHA / マジックリンクを代わりに解除することは絶対にありません**。これらは必ずローカルの Chrome で一度だけ自分で通してください。

### 生バイトの取得（最速・最安）

> あなた：「この JSON エンドポイントを GET して」→ 生バイトデータ

ページ全体をレンダリングする必要がない場合、直接 HTTP で取得する方がブラウザ経由より **約 4 倍速く、約 4 倍安く** すみます。コンテンツタイプ（JSON / テキスト / バイナリ）は自動検出されます。

### スクリーンショット / アーカイブ

> あなた：「ページ全体のスクリーンショットを撮って」／「PDF で保存して」→ ディスク上のファイルパス

画像も PDF も **ディスクに保存され、パスが返されます**——チャットに巨大なデータが流れ込んでコンテキストを無駄遣いすることはありません。超大量のテキスト出力（fetch_url / network など）が 48 KiB を超える場合も自動でディスクに落とされ、プレビュー + `@oN` のページ継続ハンドルが返ります——`read_text` ツールでページごとに続きを読めます（v1.8 から MCP 経由で直接呼び出し可能）。

### ページが読み込むものを可視化

> あなた：「このページが読み込んだサードパーティのトラッカーを教えて」→ トラッカードメインごとの集計付きリソース一覧

ページが読み込むすべてのリソースを自動で識別し、サードパーティドメインごとにグループ化——プライバシーリスクやパフォーマンスのボトルネック発見に重宝します。v1.11 からはリソース収集がブラウザエンジンのネイティブなネットワーク層を直接利用——**プロキシ / TUN ネットワーク環境でも完全**で、各リソースにリクエストメソッドとステータスコードも付きます。

### ネイティブデスクトップアプリの操作

> あなた：「ダークモードをオフにして」／「Mail の受信トレイの一番上のメールを読んで」→ 自動操作（検証付き）

macOS では Finder / Mail / Safari / メモ / システム設定など、すべてのネイティブアプリを操作できます。**Windows と Linux でも動作します**（下記の正直な境界線を参照）。すべての操作は検証され——「本当に実行された」ことを確認し、成功を偽ることはありません。

> **正直な境界線**：macOS は実機で検証済み、Windows / Linux はコンパイル時と契約レベルのセルフチェックを通過していますが、実機での網羅的手動テストはまだ進行中です。**「Win/Linux で完全検証済み」とは偽りません。**

### ウェブとデスクトップをまたぐ統合スケジューリング

> あなた：「今操作できるものをすべてリストアップして」→ 統合された一つのリスト

ウェブページとデスクトップウィンドウが一つのリストを共有——「これはブラウザの中」「これはデスクトップ」と区別する必要はありません。Claude が何を操作すべきかを選び、そこからすべてが流れていきます。

### リンク切れの復元

> あなた：「このリンク 404 だ」→ Internet Archive の最新スナップショット

Internet Archive（Wayback Machine）に飛んで、その URL の最後にアーカイブされたコピーを探します。**生きているリンクを切れたとみなすことは絶対にありません**——「これは消えた」とあなたが言った時だけ探しに行きます。

### アンチボット回避（デフォルトはオフ）

> あなた：「このサイト Cloudflare がいてスクレイプできない」→ クラウド Chrome のアンチボット

**デフォルトでは完全にオフ。** あなたが明示的に有効化し、かつクラウドブラウザ（セルフホスト Steel またはホスト型 browserbase / stagehand）を設定した場合にだけ動作します。軽度のアンチボットは `browse_headless` 内蔵のアンチ検出で通ります——**Cloudflare 級の強固な保護にだけクラウドブラウザが必要**です。

- **Steel セルフホスト（推奨 · 無料）**：ローカル Docker でオープンソースのクラウドブラウザを運用——セッション単位の費用ゼロ、cookie はローカルから出ません。一行のコマンドで開通、[キー設定ガイド · Steel](./doc/KEY-GUIDE.md#steel_endpoint--自托管云浏览器v16-新推荐免费) を参照。
- **browserbase（ホスト型 · 有料）**：トライアル後は従量課金。自分で Docker を運用したくない場合の選択肢。
- **stagehand（ホスト型 · 有料）**：⚠️ プログラマティックな実験チャンネル——キーを設定しても内部チャンネルを組み立てるだけで、**MCP ツールの入口はありません**（REST 契約は未検証；`lasso doctor` #39 `stagehand_rest_contract_probe` がこれを専用チェック）。

---

## インストール

**現在のバージョン v1.13.0**（更新履歴はこの節末尾の折りたたみブロックにあります）。

前提条件：Node.js ≥ 20 + Claude Code（または MCP 対応の任意のクライアント）。

```bash
claude mcp add lasso -- npx -y lasso-mcp
```

Claude Code を再起動 → `/mcp` → `lasso ✓ Connected`。**これ一行だけ、キーは一切不要**——インストール直後にスクレイプ / スクリーンショット / PDF / デスクトップ操作が使えます。キーを任意で設定できるのは検索だけです（[設定](#設定) を参照）。

**macOS でデスクトップ操作を使いたい場合**：`lasso doctor` を一度実行し、案内に従って `lasso-rust-helper` に「アクセシビリティ」と「画面収録」権限をチェックするだけ。`doctor` が一歩ずつ案内します。

<details>
<summary>📋 更新履歴（v1.8 → v1.13、クリックで各バージョンの変更を見る）</summary>

- **v1.13**：ヘッドレスブラウザの言語フィンガープリント一貫性（HTTP `Accept-Language` がプロファイルに従って送信され、「ヘッダー zh-CN ↔ ページ en-US」の矛盾を解消）；領域付き VLM スクリーンショットの座標ずれ修正；`desktop find` が純 ref クエリを拒否；Steel セッション解放に 3 秒の上限を追加（Steel 停止時でも終了が 5 分固まらなくなった）。
- **v1.12**：markdown 抽出のデュアル活性化（defuddle の 20 余りサイト専用抽出器 + 表・数式の構造保真）；macOS のデフォルトフィンガープリントをホストシステムと整合；デスクトップチェーン末尾の正直化（VLM が成功を偽らない / expect は連続 2 回ヒットが必要 / `truncated:true` シグナル）；Electron 入力欄の type 自動降格；ドラッグの補間軌道が実用に；CC 異常終了時の即時後始末。
- **v1.11**：デスクトップが「見える」から「押せる」に（click / type / scroll の真実装 + 座標マウス + `skeleton` ツリー刈り込み）；ドライバ層を chrome-devtools-mcp 1.7.0 へ更新（起動レベルのアンチ検出、テレメトリはデフォルトでオフ）；検索に `freshness` 時効フィルタを追加；英文クエリのキーなしフォールバックを DuckDuckGo へ変更；`LASSO_PROXY` アウトバウンドプロキシを新規追加。
- **v1.10**：ブラウザはデフォルトでサイレント + 使い終わったら閉じる（`launch-chrome` はウィンドウゼロ起動、約 60 秒で自動クローズ、`--mode visible` で戻せる）。
- **v1.9**：ブラウザライフサイクルの後始末（ヘッドレスはアイドル 5 分で自動回収、`lasso chrome-stop`、`tab_restore` が元のタブ一覧を復元）。
- **v1.8**：全量実測で露見した 24 件の欠陥を修正（上流契約適応、スクリーンショットの実ディスク保存、`read_text` ページ継続など）——完全なリストは [doc/17-功能测试清单.md](doc/17-功能测试清单.md) の「v1.8 修复记录」を参照。

</details>

---

## 設定

**キーが必要なのは検索だけ。それ以外はすべてインストール直後に使えます。** 用途に応じて表で確認：

| やりたいこと | 設定するもの |
|---|---|
| 公開ページのスクレイプ / スクリーンショット / PDF / サードパーティリソース確認 / 生バイト取得 / デスクトップ操作 | **何も設定不要** |
| 検索 | Zhipu のキー一つ（無料で取得可；マシンに Zhipu MCP が設定済みならそれすら不要） |
| 検索をほぼ落ちないように | Brave のキーを追加（有料プランで毎月 \$5 のクレジット付き。設定しなくても無料の実検索フォールバックあり） |
| ログイン済みページのスクレイプ | `lasso launch-chrome` を一度だけ実行 |
| macOS デスクトップの操作 | `lasso doctor` を一度だけ実行して認可 |
| Cloudflare があるサイトのスクレイプ | 総スイッチ + Steel（無料セルフホスト）/ browserbase（有料） |

以下の 4 モジュールは、それぞれ「最短で動かす」設定を示します。詳細は折りたたみを展開。

### 1. 検索（✅ 無料 · キーが必要な唯一のモジュール）

**まず設定が必要か確認**：マシンに Zhipu の `web-search-prime` MCP が既に設定されていれば、**Lasso が自動検出してそのキーを再利用**します——何も入力不要。`lasso doctor` で `#36 machine_search_mcp` が `pass` ならその状態です。

**設定するなら 3 ステップ**：

```bash
lasso config init        # ~/.lasso/config.json を作成
```

```json
{ "ZHIPU_API_KEY": "your_zhipu_key" }
```

保存すれば即効きます。**より堅牢に**するには Brave を追加（有料プランで \$5/月 のクレジット ≈1000 回、クレジットカード必須——2026-02 から無料枠は廃止。設定済みのどれかが落ちたら自動で次へ切り替わり。複数キーはカンマ区切りでそれぞれ別枠）：

```json
{
  "ZHIPU_API_KEY": "your_zhipu_key",
  "BRAVE_API_KEYS": "bravekey1,bravekey2",
  "BING_API_KEYS": "bingkey1"
}
```

> フォールバック順：マシン MCP 再利用 → Zhipu → Brave →（Bing は停止済みのため自動スキップ）→ ヘッドレスブラウザ実検索フォールバック。前が落ちたら自動で次へ。

キーの取得方法や無料枠の詳細 → [キー設定ガイド · 検索](./doc/KEY-GUIDE.md#a-搜索)。よく使うコマンド：`lasso --version` / `lasso --help`（v1.8 から未知のコマンドは使い方を表示して非ゼロ終了し、黙って固まらなくなりました）。

### 2. ログイン済みページのスクレイプ（✅ 無料 · コマンド一行、キー不要）

```bash
lasso launch-chrome
```

最初にこのウィンドウでアカウントへログイン（2FA は自分で通す）と、**ログイン状態は以後ずっと再利用**されます。あとは Claude に「ログイン済みの Jira を開いて」と言うだけです。

- デフォルトは**ウィンドウゼロのサイレント**稼働。フォーカスを奪わず、常にミュート。動作を見たい場合は `--mode visible` を追加
- 使い終わったら**約 60 秒で自動クローズ**——片付けを覚えておく必要なし。手動で閉じるならいつでも `lasso chrome-stop`
- あなたの Chrome に開いたタブは、タスク後に `admin {action:"tab_restore", reason:"完了"}` で元の一覧を復元（サーバー終了時にも自動実行）

> 🔴 **レッドライン**：2FA / 認証コード / CAPTCHA——Lasso は代行解除しません。ウィンドウで一度だけ自分で通してください。

<details>
<summary>詳細：profile 再利用 / ポート占拠 / パラメータ調整 / サイレントの境界</summary>

- v1.8 からは Lasso 独立 profile をデフォルトで使用（Chrome 136+ はデフォルト profile でのデバッグポートを禁止しており、旧方式は即終了していました）。既存 profile の再利用は `lasso launch-chrome --profile <ディレクトリ>`。
- 起動後はデバッグポートを自動プローブ。Chrome が起動しない / ポートが塞がっている場合は明確にエラーを出し、成功を偽りません。
- 自動クローズのしきい値：`LASSO_LAUNCH_IDLE_MS`（デフォルト 60000。`300000` で 5 分に戻す、`0` で無効）。単発の長時間タスクは `--idle-ms 3600000`。
- ヘッドレスブラウザはアイドル 5 分で自動回収（`LASSO_HEADLESS_IDLE_MS` で調整・無効化可）。
- 正直な境界線：`lasso launch-chrome` を単体で（サーバー経由なしで）実行した場合、idle 自動クローズはなく、出口は `chrome-stop` です。`browse_logged_in` が**自分で開いた可視 Chrome** に繋ぐ場合は「低ストレス、ただしゼロではない」（macOS プラットフォームレベルの制限で、一部の操作が一度フォーカスを奪うことがあります）——完全にサイレントにしたいなら hidden 案か `browse_headless` を。`desktop` は実人のキーボード／マウスをエミュレートするため、設計上、物理キーボードとマウスを占有します——サイレントな形態はありません。
- chrome-stop は Lasso 自身が起動した所有権を検証済みのものだけを閉じます。手動で開いたブラウザを誤って閉じません。

</details>

**詳細はこちら** → [キー設定ガイド · ログイン済みブラウジング](./doc/KEY-GUIDE.md#b-登录态浏览命令行配置无-key)。

### 3. デスクトップの操作（✅ 無料 · 認可は一度だけ、キー不要）

- **macOS**：`lasso doctor` を実行し、案内に従って `lasso-rust-helper` に「アクセシビリティ」+「画面収録」をチェック
- **Windows**：最初のデスクトップ操作時にシステムが認可ウィンドウを表示するので「許可」をクリック
- **Linux**：アクセシビリティインターフェースをインストール（GNOME / MATE は標準で搭載。なければ `sudo apt install at-spi2-core`）

> 正直な境界線：macOS は実機で検証済み。Windows / Linux はコンパイル時と契約レベルのセルフチェックを通過していますが、実機での網羅的手動テストは進行中です。「完全検証済み」とは偽りません。

**詳細はこちら** → [キー設定ガイド · デスクトップ操作](./doc/KEY-GUIDE.md#c-桌面控制系统授权无-key)。

### 4. クラウドブラウザ（デフォルトはオフ · 強力なアンチボットにだけ必要）

軽度のアンチボットは `browse_headless` 内蔵のアンチ検出で通ります——**必要なければ設定しないでください**。Cloudflare 級を通す場合のみ、総スイッチ + 一つのチャンネルを同時に満たす必要があります：

```json
{
  "LASSO_ALLOW_CLOUD_BROWSER": true,
  "STEEL_ENDPOINT": "http://localhost:3000"
}
```

- **Steel セルフホスト（推奨 · 無料）**：Docker 一行で起動 `docker run -p 3000:3000 -p 9223:9223 ghcr.io/steel-dev/steel-browser`。セッション単位費用ゼロ、cookie はローカルから出ない
- **browserbase ホスト型（有料）**：`"BROWSERBASE_API_KEY": "your_key"` に差し替え。Docker を運用したくない場合の選択肢
- ⚠️ stagehand：プログラマティックな実験チャンネルで、MCP ツール入口はありません。ページのスクレイプには当てにしないでください

**キーの申請方法、Steel の完全な開通手順** → [キー設定ガイド · クラウドブラウザ](./doc/KEY-GUIDE.md#d-云浏览器反爬默认关双重解锁)。

<details>
<summary><b>高度なチューニング（任意——通常のユーザーは読み飛ばして OK）</b></summary>

日常利用では以下は **完全に無視して構いません**。これらは特殊シナリオ向けで、大半は `lasso config init` で `~/.lasso/config.json` に設定できるか、環境変数で上書きできます（環境変数は設定ファイルより優先されるので、一時的な差し替えに便利です）：

- ログイン済み Chrome のデバッグポートを変更（デフォルトの `9222` が取られている場合）
- キャッシュや状態ファイルの保存場所を変更
- 無料の検索ソースのみに制限
- 社内ネットや特定のプロキシレンジを許可
- ログイン Cookie を暗号化する独自パスフレーズを設定（未設定時は macOS Keychain を使用）
- 検索結果のスナップショットをディスクに保存（回帰テスト用）
- ヘッドレスブラウザの idle 自動回収時間を調整（`LASSO_HEADLESS_IDLE_MS`、デフォルト 5 分。`0` で無効）
- launch-chrome の Chrome の「使い終わったら閉じる」時間を調整（`LASSO_LAUNCH_IDLE_MS`、デフォルト 60 秒。`300000` で 5 分、`0` で無効）、または可視起動へ戻す（`LASSO_LAUNCH_MODE=visible`）
- ブラウザにアウトバウンドプロキシを設定（`LASSO_PROXY`、例：`http://127.0.0.1:7890`。**ヘッドレスブラウザと Steel クラウドブラウザにのみ影響——ログイン済み Chrome の出口は常に元のまま**——v1.11）
- Steel セルフホストクラウドブラウザのエンドポイントを設定（`STEEL_ENDPOINT`、例：`http://localhost:3000`。有効化には `LASSO_ALLOW_CLOUD_BROWSER=true` も必要）

変数の完全なリストとデフォルト値：[キー設定ガイド · 高度なチューニング](./doc/KEY-GUIDE.md#e-高级调优可选全不配)。**Surge / Clash などの TUN プロキシネットワーク（fake-ip、`198.18.0.0/15`）と `127.0.0.1`（ローカル Chrome の CDP デバッグポート用）は初期状態で許可済み**——追加設定は不要で、設定し忘れではなく設計意図です。

> **後方互換性**：以前 `claude mcp add -e KEY=VAL` でインストールしていた場合、それらの環境変数は **そのまま動作し**、設定ファイルを **上書き** します。設定ファイルは追加の、よりフレンドリーな経路にすぎません——環境変数を置き換えるものではありません。

</details>

---

## プライバシーとセキュリティ

あなたのデータはあなたのものです。

- **ログイン Cookie は絶対に外部に送出されません**——あなたが明示的に有効化し、ディスクに暗号化する場合を除いて。Lasso があなたのログイン状態をこっそりどこかへ送ることはありません。
- **デスクトップ操作のログはローカルに留まります**——リモートへの報告はゼロ。Lasso はあなたの行動を外に報告しません。
- **クラウドブラウザはデフォルトでオフ**——有効化には **明示的な二重確認**（マスタースイッチ + キー）が必要です。それがない限り、この機能は実質的に存在しません。
- **2FA / CAPTCHA / 認証コードの代行解除はしません**（レッドライン）。これらは必ずローカルのブラウザで、あなた自身が一度だけ通す必要があります。
- **外部者が社内サービスを勝手に叩くことはできません**——社内ネットへのアクセスはデフォルトで拒否。Surge / Clash の TUN プロキシネットワークは初期状態で許可済みです。
- **検索結果はデフォルトでディスクに書き込まれません**——記録モード（回帰テスト用）を明示的に有効化した場合のみ。

---

## トラブルシューティング

**どんな問題でも、最初の一歩は必ず `lasso doctor` です。** セルフチェックを行い、設定の不備を指摘してくれます。

| 症状 | 対処 |
|---|---|
| macOS のデスクトップ操作が動かない | 「システム設定 → プライバシーとセキュリティ → アクセシビリティ / 画面収録」で `lasso-rust-helper` にチェックを入れる（`lasso doctor` が案内します） |
| ログイン済みページのスクレイプが失敗する | ローカルの Chrome で一度手動でログイン（2FA も自分で通す）してから、「ログイン済みの X を開いて」と言う |
| PDF 保存が失敗する | 代わりに「このページの全体スクリーンショットを撮って」と言う |
| 検索がいつも空で返ってくる | キーの有効期限や無料枠の使い切りを確認。複数プロバイダー（Zhipu + Brave）を追加すると失敗率が劇的に下がります |
| リンクが開かない | 「このリンク切れてるからアーカイブを探して」と言って Internet Archive を確認 |
| 社内ネットへのアクセスがブロックされたと表示される | URL を再確認。TUN プロキシネットワークはデフォルトで許可されていますが、その他の社内ネットには明示的な許可が必要です |
| アンチ検出の効果を確認したい | `lasso doctor --stealth-check` を実行——creepjs 検出ページを駆動してベースラインと比較します（任意、日常利用には影響なし） |

完全な FAQ とデバッグのヒントは [`doc/TROUBLESHOOTING.md`](./doc/TROUBLESHOOTING.md) にあります。

---

## こんな人に向け・むいていない人

**むいている人**

- **Claude Code をヘビーに使う人**——毎週検索・スクレイプ・デスクトップ操作をし、用途ごとに別の MCP を入れたくない人
- **研究者 / レポート作成者 / データ系の人**——検索から整ったテキストの取得、リンク切れの復元までを一気通貫で
- **RAG / ナレッジベースを構築する人**——ウェブページを整った markdown + 引用マーカー付きに変換し、トークンも費用も節約
- **自動化 / DevOps 系の人**——macOS のネイティブアプリを操作し、ログイン済みの社内ダッシュボードをスクレイプ
- **ログイン済みページをよくスクレイプする人**——ローカルの Chrome セッションを再利用、設定に認証情報を再保存する必要なし

**むいていない人**

- **Claude Code やその他 MCP クライアントを使っていない人**——Lasso は MCP サービスであり、動作には MCP クライアントが必要です
- **単一の機能だけが必要で、すでに専用ソリューションを持っている人**——オールインワンは冗長になる可能性があります
- **2FA / CAPTCHA の回避を探している人**——レッドラインです。やらないし、これからもやりません

---

## 作者を応援する

Lasso がお役に立てば、作者にコーヒー一杯をおごってください ☕

<div align="center">

WeChat | Alipay
:-: | :-:
<img src="doc/support-wechat.jpg" height="200" alt="WeChat sponsor QR"> | <img src="doc/support-alipay.jpg" height="200" alt="Alipay sponsor QR">

</div>

または ⭐ [このリポジトリに Star を付ける](../../stargazers)、[Issue を開く](../../issues)、[PR を送る](../../pulls)——どれも作者の励みになります。

---

## その他のドキュメント

- 深掘りのアーキテクチャは？ [機能アーキテクチャ](doc/08-media-interact-功能架构.md) を参照
- バージョンのロードマップは？ [実施スケジュール](doc/09-media-interact-实施排期.md) を参照
- キーの取得方法は？ [キー設定ガイド](doc/KEY-GUIDE.md) を参照

## ライセンス

**MIT** © wangdong233。デスクトップのヘルバープロセスおよびブラウザエンジンの依存関係はすべて MIT / Apache-2.0 です——企業利用にも安全です。

> 内部アーキテクチャ、設計原則、クロスプラットフォームの境界線、開発コマンドについて知りたい場合は、[ARCHITECTURE.md](./ARCHITECTURE.md) と [`doc/TROUBLESHOOTING.md`](./doc/TROUBLESHOOTING.md) を参照してください。

<p align="center">
  <sub><strong>スクリプトを書く</strong>より <strong>口で言う</strong> 方が好きな、すべての人のために作られました。</sub><br>
  <sub>一度インストールすれば——検索も、スクレイプも、ログイン済みページも、デスクトップ操作も、すべてが一言で。</sub>
</p>
